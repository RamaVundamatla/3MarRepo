<Class name="CnEcWildcardSearch" id="6aa5e28b-daa1-4b91-ad44-9f4366b6b015">
    <superclass name="CnEcSearch"/>
    <transient/>
    <persistentAllowed/>
    <sharedTransientAllowed/>
    <subclassPersistentAllowed/>
    <subclassSharedTransientAllowed/>
    <subclassTransientAllowed/>
    <transientAllowed/>
    <access>protected</access>
    <text>___________________________________________________________________________

              P R O P R I E T A R Y   N O T I C E

This software, in whole and in every part, is proprietary to Jade Software
Corporation Limited at Christchurch, New Zealand and is not to be
reproduced, used or disclosed except upon prior written permission of Jade
Software Corporation Limited.

  COPYRIGHT © JADE SOFTWARE CORPORATION LIMITED 2004 - ALL RIGHTS RESERVED
____________________________________________________________________________</text>
    <DbClassMap file="basesearch"/>
    <Constant name="ASTERISK" id="c8872df6-7cac-48d2-9b8b-edd3cadd6276">
        <source>"*"</source>
        <type name="String"/>
    </Constant>
    <Constant name="QUESTION_MARK" id="cb10fa5f-947e-41ed-8150-3bbf4add4f27">
        <source>"?"</source>
        <type name="String"/>
    </Constant>
    <ImplicitInverseRef name="allSearchResults" id="1a6b4082-be4b-4f8c-b716-9f26c5ea8a2a">
        <memberTypeInverse/>
        <type name="CnEcArray"/>
        <access>readonly</access>
    </ImplicitInverseRef>
    <ImplicitInverseRef name="allSearchValues" id="f9d42914-678d-4b41-95e4-5afde7ca8d64">
        <memberTypeInverse/>
        <type name="CnEcArray"/>
        <access>readonly</access>
    </ImplicitInverseRef>
    <ImplicitInverseRef name="allWildcardTokens" id="4cc3c579-fec1-43f4-9b9c-d0e6c8c0b00e">
        <memberTypeInverse/>
        <type name="CnEcArray"/>
        <access>protected</access>
    </ImplicitInverseRef>
    <PrimAttribute name="foundPos" id="a9eeebce-b072-48ac-8b7b-73348bf3228e">
        <embedded/>
        <type name="Integer"/>
        <access>readonly</access>
    </PrimAttribute>
    <PrimAttribute name="isCaseSensitive" id="0cb560bf-5a0e-4554-aaff-f7f9af8ce47e">
        <embedded/>
        <type name="Boolean"/>
        <access>readonly</access>
    </PrimAttribute>
    <PrimAttribute name="isExactMatch" id="8d579fe4-23eb-4ed8-b77f-2f8dcb257f32">
        <embedded/>
        <type name="Boolean"/>
    </PrimAttribute>
    <JadeMethod name="addMultipleSearchValues" id="6e69f48a-a08a-49e8-84cc-6706105e170f">
        <source>addMultipleSearchValues(pAllSearchValues:CnEcArray);
// --------------------------------------------------------------------------------
// Created:		21 January 2002 by cnwdad1
//
// Purpose:		Adds multiple search strings to the allSearchValues array.
//
// Parameters:	pAllSearchValues - The array of strings to be added to the value array.
//
// Returns:		Nothing
//
// Changed On	Changed By	Reason
// ----------	----------	-------------------------------------------------------
//
// --------------------------------------------------------------------------------
vars
	searchStringObj	: CnEcArrayObject;
	
begin
	self.allSearchValues.purge();
	
	foreach searchStringObj in pAllSearchValues do
		self.allSearchValues.addString(searchStringObj.stringValue);
	endforeach;
end;</source>
        <Parameter name="pAllSearchValues" type="CnEcArray"/>
    </JadeMethod>
    <JadeMethod name="addSearchValue" id="2bb1a604-9686-476c-9b33-99c803a85529">
        <source>addSearchValue(pSearchValue:String);
// --------------------------------------------------------------------------------
// Created:		21 January 2002 by cnwdad1
//
// Purpose:		Adds the specified search string to the allSearchValues array.
//
// Parameters:	pSearchValue - The string to be added to the value array.
//
// Returns:		Nothing
//
// Changed On	Changed By	Reason
// ----------	----------	-------------------------------------------------------
//
// --------------------------------------------------------------------------------
begin
	self.allSearchValues.addString(pSearchValue);
end;</source>
        <Parameter name="pSearchValue" type="String"/>
    </JadeMethod>
    <JadeMethod name="clearSearchValues" id="6a76c642-1ab2-468f-a0b6-2defdf225398">
        <source>clearSearchValues();
// --------------------------------------------------------------------------------
// Created:		21 January 2002 by cnwdad1
//
// Purpose:		Clears all search string values from the allSearchValues array.
//
// Parameters:	None
//
// Returns:		Nothing
//
// Changed On	Changed By	Reason
// ----------	----------	-------------------------------------------------------
//
// --------------------------------------------------------------------------------
begin
	self.allSearchValues.purge();
end;</source>
    </JadeMethod>
    <JadeMethod name="doMultiSearch" id="93761ea5-e0b0-44a4-a862-1fb176f8b667">
        <updating/>
        <source>doMultiSearch():Boolean updating;
// --------------------------------------------------------------------------------
// Created:		21 January 2002 by cnwdad1
//
// Purpose:		Searches all string in the allSearchValues array, using the wildcards
//				that have been set up using the parseTokens() method.
//
//				All search values with a successful match will be added to the
//				allSearchResults array.
//
// Parameters:	None
//
// Returns:		True if any values were successfully matched, otherwise false.
//
// Changed On	Changed By	Reason
// ----------	----------	-------------------------------------------------------
//
// --------------------------------------------------------------------------------
vars
	searchStringObj	: CnEcArrayObject;
	isSuccessful	: Boolean;
	
begin
	isSuccessful	:=	false;
	self.allSearchResults.purge();
	
	foreach searchStringObj in self.allSearchValues do
		if self.doSearch(searchStringObj.stringValue) then
			self.allSearchResults.add(searchStringObj);
			
			if (not isSuccessful) then
				isSuccessful	:=	true;
			endif;
		endif;
	endforeach;
epilog
	return isSuccessful;
end;</source>
        <ReturnType type="Boolean"/>
    </JadeMethod>
    <JadeMethod name="doSearch" id="4aa36d8d-2c79-40e8-ae05-e5700a7f5399">
        <updating/>
        <source>doSearch(pInString:String):Boolean updating;
// --------------------------------------------------------------------------------
// Created:		21 January 2002 by cnwdad1
//
// Purpose:		Searches the specified string using the wildcards that have been
//				set up using the parseTokens() method.
//
// Parameters:	pInString - The string value to search.
//
// Returns:		True if the search was successful, otherwise false.
//
// Changed On	Changed By	Reason
// ----------	----------	-------------------------------------------------------
//
// --------------------------------------------------------------------------------
vars
	searchFor	: String;
	posIndex	: Integer;
	charPos		: Integer;
	tokenChar	: String;
	allTokens	: CnEcArray;
	tokenCount	: Integer;
	isScanAhead	: Boolean;
	isNotMatch	: Boolean;

begin
	self.foundPos	:=	0;
	
	if self.isCaseSensitive then
		searchFor	:=	pInString.trimBlanks();
	else
		searchFor	:=	pInString.trimBlanks.toUpper();
	endif;

	if searchFor.length() = 0 then
		return false;
	endif;
	
	if searchFor.length() &gt; 1 and searchFor[1:1] = "!" then
		isNotMatch	:=	true;
		searchFor	:=	searchFor[2:end];
	else
		isNotMatch	:=	false;
	endif;
	
	allTokens	:=	self.allWildcardTokens;
	tokenCount	:=	allTokens.size();

	if tokenCount = 0 then
		return false;
	endif;

	isScanAhead	:=	false;
	charPos		:=	1;
	posIndex	:=	1;

	while (posIndex &lt;= tokenCount) do
		tokenChar	:=	allTokens[posIndex].stringValue.trimBlanks();

		if tokenChar = self.ASTERISK then
			if posIndex = tokenCount then
				self.foundPos	:=	posIndex;
				return true;
			else
				isScanAhead	:=	true;
			endif;

		elseif tokenChar = self.QUESTION_MARK then
			charPos	:=	charPos + 1;
		else
			if isScanAhead then
				isScanAhead	:=	false;
				charPos		:=	searchFor.pos(tokenChar, charPos);

				if charPos = 0 then
					return false;
				else
					charPos	:=	charPos + tokenChar.length();
				endif;
			else
				if charPos &gt; searchFor.length() then
					return false;
				else
					if tokenChar = searchFor[charPos:tokenChar.length()] then
						charPos	:=	charPos + tokenChar.length();
					else
						return false;
					endif;
				endif;
			endif;
		endif;

		posIndex	:=	posIndex + 1;
	endwhile;

	if charPos &lt;= searchFor.length() then
		return false;
	else
		self.foundPos	:=	posIndex;
		return true;
	endif;
end;</source>
        <Parameter name="pInString" type="String"/>
        <ReturnType type="Boolean"/>
    </JadeMethod>
    <JadeMethod name="parseWildcards" id="ade27428-7482-42ac-9950-c8374619a0a7">
        <updating/>
        <source>parseWildcards(pWildcardString:String; pIsCaseSensitive:Boolean):Boolean updating;
// --------------------------------------------------------------------------------
// Created:		21 January 2002 by cnwdad1
//
// Purpose:		This method will split a wildcard search string into its individual
//				tokens, and create an array element for each token.  It will also
//				default to having an * at the end of the search string.
//
//				eg. If you search for "Ra?gi R*gir*", the array will have the
//				following elements:
//
//				1) Ra
//				2) ?
//				3) gi R
//				4) *
//				5) gir
//				6) *
//
// Parameters:	pWildcardString - The wildcard search string.
//				pIsCaseSensitive - Determines whether the pattern matching will
//					be case-sensitive.
//
// Returns:		True if the wildcards were sucessfully parsed, otherwise false.
//
// Changed On	Changed By	Reason
// ----------	----------	-------------------------------------------------------
//
// --------------------------------------------------------------------------------
vars
	wildcards		: String;
	charPos			: Integer;
	length			: Integer;
	character		: String;
	startPos		: Integer;
	endPos			: Integer;
	allTokens		: CnEcArray;

begin
	self.isCaseSensitive	:=	pIsCaseSensitive;
	
	if self.isCaseSensitive then
		wildcards	:=	pWildcardString.trimBlanks();
	else
		wildcards	:=	pWildcardString.trimBlanks().toUpper();
	endif;

	if wildcards.length = 0 then
		return false;
	endif;

	// Check whether the string has any wildcard characters.

	charPos	:=	wildcards.pos(self.ASTERISK, 1);

	if charPos = 0 then
		charPos	:=	wildcards.pos(self.QUESTION_MARK, 1);

		if charPos = 0 and not self.isExactMatch then
			// If there's no wildcard characters in the search pattern, then
			// we want to return everything which exactly matches, or begins
			// with the search string.
			wildcards	:=	wildcards &amp; self.ASTERISK;
		endif;
	endif;

	allTokens	:=	self.allWildcardTokens;
	allTokens.purge();

	length		:=	wildcards.length();
	startPos	:=	0;
	charPos		:=	1;

	while (charPos &lt;= length) do
		character	:=	wildcards[charPos];

		if character = self.ASTERISK or character = self.QUESTION_MARK then
			if startPos &gt; 0 then
				endPos		:=	charPos - startPos;
				allTokens.addString(wildcards[startPos:endPos]);	// Add token prior to this wildcard characrer.
				startPos	:=	0;
			endif;

			allTokens.addString(wildcards[charPos:1]);			// Add this wildcard character.
		elseif startPos = 0 then
			startPos	:=	charPos;
		endif;

		charPos	:=	charPos + 1;
	endwhile;

	if startPos &gt; 0 then
		endPos	:=	charPos - 1;
		allTokens.addString(wildcards[startPos:endPos]);			// Add the token of the remainder of the string.
	endif;

	// Default to having * at the end of the search string.

// cnwdad1 04/06/2004 - The developer can now specifiy whether to put the "*" on the end
// of the search string.
//	if allTokens.size() &lt;&gt; null and allTokens.last().stringValue &lt;&gt; self.ASTERISK then
//		allTokens.addString(self.ASTERISK);
//	endif;
	if not self.isExactMatch		// Allow developer to override behaviour.
	and allTokens.size() &lt;&gt; 0
	and allTokens.last().stringValue &lt;&gt; self.ASTERISK
	then
		allTokens.addString(self.ASTERISK);
	endif;
	
	return true;
end;</source>
        <Parameter name="pWildcardString" type="String"/>
        <Parameter name="pIsCaseSensitive" type="Boolean"/>
        <ReturnType type="Boolean"/>
    </JadeMethod>
</Class>
