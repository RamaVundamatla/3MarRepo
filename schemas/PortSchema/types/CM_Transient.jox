<Class name="CM_Transient" id="f6438a12-a3d0-437c-bfab-b171b760a170">
    <superclass name="Object"/>
    <transient/>
    <subclassPersistentAllowed/>
    <subclassSharedTransientAllowed/>
    <subclassTransientAllowed/>
    <abstract/>
    <DbClassMap file="prenotes"/>
    <JadeMethod name="causeEvent" id="1d428e77-becf-48fa-8f81-30f9ee39909a">
        <source>causeEvent(eventType: Integer; immediate: Boolean; userInfo: Any);
// ---------------------------------------------------------------------------
// Created:     15 May 2003 by cnwrs2
//
// Purpose:     
//
// Parameters:  None
//
// Returns:     Nothing
//
// Changed On   Changed By  Reason
// ----------   ----------  --------------------------------------------------
//
// ---------------------------------------------------------------------------
vars

begin

	inheritMethod(eventType, immediate, userInfo);

end;</source>
        <Parameter name="eventType" type="Integer"/>
        <Parameter name="immediate" type="Boolean"/>
        <Parameter name="userInfo" type="Any"/>
    </JadeMethod>
    <JadeMethod name="create" id="1dcae2ce-293d-4040-8d1e-5fd418eda583">
        <updating/>
        <source>create() updating;
// ---------------------------------------------------------------------------
// Created:     18 January 2001 by cnwrs2
//
// Purpose:     
//
// Parameters:  None
//
// Returns:     Nothing
//
// Changed On   Changed By  Reason
// ----------   ----------  --------------------------------------------------
//
// ---------------------------------------------------------------------------
vars

begin

	if (not self.isTransient) then
		app.setContinueCMExceptions(false);
		app.raiseCMException1(CannotCreatePersistentMdlTrans, self.class.String, null, null);
	endif;

end;</source>
    </JadeMethod>
    <JadeMethod name="exportMeXML" id="916463ba-58a8-46fa-aab6-94b7fcb01b18">
        <source>exportMeXML(_file:File; _objArray:ObjectArray io; _exportReferences, _exportCollections : Boolean);
// ---------------------------------------------------------------------------
// Created:     09 February 2004	cnwth2
//
// Purpose:     
//			 
// ----------   ----------  --------------------------------------------------
vars
	property	: Property;
	propColl	: PropertyNDict;
	object		: Object;
	str, message: String;
	boolean		: Boolean;
	int: Integer;
	time: Time;
begin
	// check if we have already been called. if so bugger off. 
	if _objArray.includes(self) then
		return;
	else
		_objArray.add(self);
	endif;
	create propColl transient;
	self.class.getPropertiesAllSuperclass(propColl);
	_file.writeLine("&lt;" &amp; self.class.name &amp; "&gt;");
	_file.writeLine("&lt;" &amp; self.class.name &amp; "Attributes&gt;");
	
	foreach property in propColl do
		if (not property.isKindOf(Attribute)) or
		   (property.virtual) then
			continue;
		endif;
		message := null;
		if property.type = StringArray then
			foreach str in self.getPropertyValue(property.getName).StringArray do
				message := message &amp; str &amp; ':';
			endforeach;
			message := message[1: message.length -1];
			_file.writeLine("&lt;"&amp;property.getName&amp;"&gt;"&amp;message&amp;"&lt;/"&amp;property.getName&amp;"&gt;");
		elseif property.type = CM_IntegerArray then
			foreach int in self.getPropertyValue(property.getName).CM_IntegerArray do
				message := message &amp; int.String &amp; ':';
			endforeach;
			message := message[1: message.length -1];
			_file.writeLine("&lt;"&amp;property.getName&amp;"&gt;"&amp;message&amp;"&lt;/"&amp;property.getName&amp;"&gt;");
		elseif property.type = IntegerArray then
			foreach int in self.getPropertyValue(property.getName).IntegerArray do
				message := message &amp; int.String &amp; ':';
			endforeach;
			message := message[1: message.length -1];
			_file.writeLine("&lt;"&amp;property.getName&amp;"&gt;"&amp;message&amp;"&lt;/"&amp;property.getName&amp;"&gt;");
		elseif property.type = TimeArray then
			foreach time in self.getPropertyValue(property.getName).TimeArray do
				message := message &amp; time.String &amp; ':';
			endforeach;
			message := message[1: message.length -1];
			_file.writeLine("&lt;"&amp;property.getName&amp;"&gt;"&amp;message&amp;"&lt;/"&amp;property.getName&amp;"&gt;");
		elseif property.type = BooleanArray then
			foreach boolean in self.getPropertyValue(property.getName).BooleanArray do
				message := message &amp; boolean.String &amp; ':';
			endforeach;
			message := message[1: message.length -1];
			_file.writeLine("&lt;"&amp;property.getName&amp;"&gt;"&amp;message&amp;"&lt;/"&amp;property.getName&amp;"&gt;");
		else
			_file.writeLine("&lt;"&amp;property.getName&amp;"&gt;"&amp;self.getPropertyValue(property.getName).String.trimBlanks&amp;"&lt;/"&amp;property.getName&amp;"&gt;");
		endif;
	endforeach;
	_file.writeLine("&lt;/" &amp; self.class.name &amp; "Attributes&gt;");
	
	if _exportReferences then
		_file.writeLine('&lt;' &amp; self.class.name &amp; 'References&gt;');
		self.exportMyReferencesXML(_file, _objArray);
		_file.writeLine('&lt;/' &amp; self.class.name &amp; 'References&gt;');
	endif;
	
	if _exportCollections then
		_file.writeLine('&lt;' &amp; self.class.name &amp; 'Collections&gt;');
		self.exportMyCollectionsXML(_file, _objArray);
		_file.writeLine('&lt;/' &amp; self.class.name &amp; 'Collections&gt;');
	endif;
	
	_file.writeLine("&lt;/"&amp;self.class.name&amp;"&gt;");


epilog
	delete propColl;
end;</source>
        <Parameter name="_file" type="File"/>
        <Parameter name="_objArray" type="ObjectArray" usage="io"/>
        <Parameter name="_exportReferences" type="Boolean"/>
        <Parameter name="_exportCollections" type="Boolean"/>
    </JadeMethod>
    <JadeMethod name="exportMyCollectionsXML" id="63f7db41-fa26-41f8-8f08-a32fafc17ece">
        <source>exportMyCollectionsXML(_file : File; _objArray : ObjectArray io);
// ---------------------------------------------------------------------------
// Created:     09 February 2004	cnwth2
//
// Purpose:     
//			 
// ----------   ----------  --------------------------------------------------
vars

begin


end;</source>
        <Parameter name="_file" type="File"/>
        <Parameter name="_objArray" type="ObjectArray" usage="io"/>
    </JadeMethod>
    <JadeMethod name="exportMyReferencesXML" id="2059399e-9910-4b80-ad16-d2709cb60058">
        <source>exportMyReferencesXML(_file : File; _objArray : ObjectArray io);
// ---------------------------------------------------------------------------
// Created:     09 February 2004	cnwth2
//
// Purpose:     
//			 
// ----------   ----------  --------------------------------------------------
vars

begin


end;</source>
        <Parameter name="_file" type="File"/>
        <Parameter name="_objArray" type="ObjectArray" usage="io"/>
    </JadeMethod>
    <JadeMethod name="importMeXML" id="d1c6f5e1-e052-4831-be40-3e5055c3c298">
        <updating/>
        <source>importMeXML(_classBlock : String) updating;
// ---------------------------------------------------------------------------
// Created:     10 February 2004	cnwth2
//
// Purpose:     
//			 
// ----------   ----------  --------------------------------------------------
vars
	attributeBlock,
	referencesBlock,
	collectionsBlock,
	subScript,
	attributeName	: String;
	attributeValue	: Any;
	prop			: Property;
	propColl		: PropertyNDict;
	tempStrArray	: StringArray;
	tempIntArray	: IntegerArray;
	tempTimeArray	: TimeArray;
	strLength,
	pos				: Integer;
begin
	attributeBlock := _classBlock.cnEcGetTagValueOfName(self.class.name &amp; 'Attributes');		
	referencesBlock := _classBlock.cnEcGetTagValueOfName(self.class.name &amp; 'References');
	collectionsBlock := _classBlock.cnEcGetTagValueOfName(self.class.name &amp; 'Collections');
			
	if attributeBlock = null then
		app.raiseUnexpectedProgramError(class.name, method.name, 'Attribute block is null');
		return;
	endif;
	
	create propColl transient;
	self.class.getPropertiesAllSuperclass(propColl);
	
	foreach prop in propColl do
		if (not prop.isKindOf(Attribute)) or
		   (prop.virtual) then
			continue;
		endif;
		
		attributeName := prop.name;
		attributeValue := attributeBlock.cnEcGetTagValueOfName(attributeName);

		if (not self.importMeXMLValidateProperty(attributeName, attributeValue)) then
			continue;
		endif;
	
		if prop.type = StringArray then
			pos := 1;
			strLength := attributeValue.String.length;
			tempStrArray := self.getPropertyValue(prop.name).StringArray;
			while pos &lt;= strLength do
				tempStrArray.add(attributeValue.String.getNextToken(pos));
			endwhile;
		elseif prop.type = IntegerArray then
			pos := 1;
			strLength := attributeValue.String.length;
			tempIntArray := self.getPropertyValue(prop.name).IntegerArray;
			while pos &lt;= strLength do
				tempIntArray.add(attributeValue.String.getNextToken(pos).Integer);
			endwhile;
		elseif prop.type = TimeArray then
			pos := 1;
			strLength := attributeValue.String.length;
			tempTimeArray := self.getPropertyValue(prop.name).TimeArray;
			while pos &lt;= strLength do
				tempTimeArray.add(attributeValue.String.getNextToken(pos).Time);
			endwhile;
		elseif prop.type = Boolean then
			self.setPropertyValue(attributeName, attributeValue.Boolean);
		elseif prop.type = Character then
			self.setPropertyValue(attributeName, attributeValue.Character);
		elseif prop.type = Date then
			self.setPropertyValue(attributeName, attributeValue.Date);
		elseif prop.type = Decimal then
			self.setPropertyValue(attributeName, attributeValue.Decimal);
		elseif prop.type = Integer then
			self.setPropertyValue(attributeName, attributeValue.Integer);
		elseif prop.type = Point then
			self.setPropertyValue(attributeName, attributeValue.Point);
		elseif prop.type = Real then
			self.setPropertyValue(attributeName, attributeValue.Real);
		elseif prop.type = Time then
			self.setPropertyValue(attributeName, attributeValue.Time);
		elseif prop.type = TimeStamp then
			self.setPropertyValue(attributeName, attributeValue.TimeStamp);
		elseif prop.type = Binary then
			self.setPropertyValue(attributeName, attributeValue.Binary);
		else
			self.setPropertyValue(attributeName, attributeValue);
		endif;		
	endforeach;
	
	if referencesBlock &lt;&gt; null then
		self.importMyReferencesXML(referencesBlock);
	endif;
	
	if collectionsBlock &lt;&gt; null then
		self.importMyCollectionsXML(collectionsBlock);
	endif;
epilog
	delete propColl	;
end;</source>
        <Parameter name="_classBlock" type="String"/>
    </JadeMethod>
    <JadeMethod name="importMeXMLValidateProperty" id="1df48ec9-a838-4eab-836c-d3efd431fc71">
        <updating/>
        <source>importMeXMLValidateProperty(_propertyName : String; _propertyValue : Any) : Boolean protected, updating;
// ---------------------------------------------------------------------------
// Created:     12 February 2004	cnwth2
//
// Purpose:     
//			 
// ----------   ----------  --------------------------------------------------
vars

begin
	//re-implement as neccessary
	if _propertyValue = null then
		return false;
	endif;

	return true;
end;</source>
        <access>protected</access>
        <Parameter name="_propertyName" type="String"/>
        <Parameter name="_propertyValue" type="Any"/>
        <ReturnType type="Boolean"/>
    </JadeMethod>
    <JadeMethod name="importMyCollectionsXML" id="6c64dd40-5474-476c-864d-8d0d19e88a42">
        <updating/>
        <source>importMyCollectionsXML(_collectionsBlock : String) updating;
// ---------------------------------------------------------------------------
// Created:     10 February 2004	cnwth2
//
// Purpose:     
//			 
// ----------   ----------  --------------------------------------------------
vars

begin
	//re-implement as neccessary
	//what is done here obviously depends on what your exportMyCollections outputs



end;</source>
        <Parameter name="_collectionsBlock" type="String"/>
    </JadeMethod>
    <JadeMethod name="importMyReferencesXML" id="0563beb7-9c75-46f1-bdb0-183d528ec2e2">
        <updating/>
        <source>importMyReferencesXML(_referencesBlock : String) updating;
// ---------------------------------------------------------------------------
// Created:     10 February 2004	cnwth2
//
// Purpose:     
//			 
// ----------   ----------  --------------------------------------------------
vars

begin
	//re-implement as neccessary
	//what is done here obviously depends on what your exportMyReferences outputs

end;</source>
        <Parameter name="_referencesBlock" type="String"/>
    </JadeMethod>
</Class>
