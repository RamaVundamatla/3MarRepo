<GUIClass name="CMUI_SplitContainer" id="5d1c6536-5431-44e8-9d39-a3290b3a8e29">
    <superclass name="BaseControl"/>
    <transient/>
    <sharedTransientAllowed/>
    <subclassSharedTransientAllowed/>
    <subclassTransientAllowed/>
    <transientAllowed/>
    <text>___________________________________________________________________________

              P R O P R I E T A R Y   N O T I C E

This software, in whole and in every part, is proprietary to Jade Software
Corporation Limited at Christchurch, New Zealand and is not to be
reproduced, used or disclosed except upon prior written permission of Jade
Software Corporation Limited.

  COPYRIGHT © JADE SOFTWARE CORPORATION LIMITED 2002 - ALL RIGHTS RESERVED
____________________________________________________________________________

Using this control, a developer can easily add a number of "resizable" controls to a form,
and have "splitter" bars between then available at runtime to allow the users to manually
resize the child controls as required.

As an example, a listbox on one side and a "thumbnail" table displaying details of the currently
selected listbox entry on the other can both be independently resized as necessary by the user.

After painting the control on the form, you can paint any two "child" controls on top of it,
and optionally specify (using the leftToRightRatio property) the initial percentage which the left 
hand or top control will occupy at form load time.  If no ratio is specified, then the initial ratio will
maintain the proportions of the widths/heights of the painted controls.

You must paint no more than two controls onto the container, otherwise an exception will be raised at
form load time.  If you require more than one split, then simply make one of the two child controls 
another CMUI_SplitContainer, and have it containing the second and third resizable children.

At runtime, the control resizes the two children controls to fill the boundaries of the control,
adding a splitter bar between them which can be dragged by the mouse to the left or right, or from 
top to botton when the split is a horizontal one.

If either of the two child controls is invisible, then the splitter bar will not be created, and 
the visible control will be resized to fill the container.


Methods
-------

1. setLeftToRightRatio(integer);

	Call this method to set the value indicating the percent of the Split Container that is to
	be occupied by the left-hand or top child control.  The remainder of the control will be occupied 
	by the other control.

2. setupChildControls;

	Call this method if you have made runtime changes to the child controls that will affect the 
	appearance of the Split Container.  eg. if you have made one of the child controls invisible, or
	added or deleted one of the child controls.  This method will rebuild the contents of the control,
	and add/remove a splitter bar as necessary.

3. CnEcResizeContents;

	Call this method if you have made runtime changes to the dimensions of the Split Container, and
	it will resize the contained controls to retain the proportions of the child controls before the 
	resize occurred.  This method should be called by a form after a resize.


Design-Time Properties
----------------------

1. leftToRightRatio (Percent as integer)

	Value indicating the percent of the Split Container that is to be occupied by the left-hand or top
	child control.  The remainder of the control will be occupied by the other control.
	If left as the default (zero) then the ratios will be set at the relative proportions of the 
	widths/heights of the painted child controls.
	(refer to splitType below)

2. isSplitHorizontal (boolean)

	Indicates whether the split between the child controls is vertical (side by side) or horizontal
	(one above the other).  Default is false, or vertical split.
	
3. splitType (Integer - Class Constants SplitType_Ratio (0), SplitType_LockLeft (1), SplitType_LockRight (2)

	Determines what method is used on a resize of the container to move the splitter bar.
	Ratio		: The size of the left and right panes are adjusted to maintain the same ratio.
	LockLeft	: The size of the left pane is fixed and the size of the right pane is adjusted.
	LockRight	: The size of the right pane is fixed and the size of the left pane is adjusted.
	
	For LockLeft and LockRight there is a minimum size which the variable pane won't get adjusted less than.  If
	this size is reached the fixed size pane will be resized.  If the user subsequently resizes the control
	so that the fixed pane can be shown at its set size it will be.

Run-Time Properties
-------------------

1. leftToRightRatio (Percent as integer)

	Read-only value indicating the percent of the Split Container that is to be occupied by the 	
	left-hand child control.  The remainder of the control will be occupied by the other control.
	Not used to resize if the splitType &lt;&gt; SplitType_Ratio.


Events
------

1. splitterMove(pSplitContainer : CMUI_SplitContainer input);

	This event is called by the control after a user mouse action has changed the position of
	the splitter bar.  This allows the form to trap the action, and perform some other user 
	logic.</text>
    <DbClassMap file="_usergui"/>
    <Constant name="SPLITTYPE_LOCKLEFT" id="4613cb7b-a2ef-4fc2-8f6b-b8236920a5c1">
        <source>1</source>
        <type name="Integer"/>
    </Constant>
    <Constant name="SPLITTYPE_LOCKRIGHT" id="1aaa984c-2318-458e-88c2-24179b6c8172">
        <source>2</source>
        <type name="Integer"/>
    </Constant>
    <Constant name="SPLITTYPE_RATIO" id="05c6f9f5-d273-47ff-8352-af3c661776f3">
        <source>0</source>
        <type name="Integer"/>
    </Constant>
    <PrimAttribute name="distanceFromBottom" id="3391aba0-1292-48a0-a563-9b0278ed55e9">
        <embedded/>
        <type name="Real"/>
        <access>readonly</access>
    </PrimAttribute>
    <PrimAttribute name="distanceFromRight" id="8bd7b93d-ab98-4249-ae2b-92c2daa2f912">
        <embedded/>
        <type name="Real"/>
        <access>readonly</access>
    </PrimAttribute>
    <PrimAttribute name="isSplitHorizontal" id="9c88a689-9365-44c2-b8a3-f1440b5b75ea">
        <embedded/>
        <type name="Boolean"/>
    </PrimAttribute>
    <PrimAttribute name="leftToRightRatio" id="401e92b3-e981-488d-bdea-bce40993b686">
        <embedded/>
        <type name="Integer"/>
        <access>readonly</access>
    </PrimAttribute>
    <PrimAttribute name="lockBottom" id="340507d4-9fc5-4003-9133-6fd913e5e9e9">
        <embedded/>
        <type name="Boolean"/>
    </PrimAttribute>
    <PrimAttribute name="lockRight" id="5a97f777-7ed7-457e-8ccb-30feb72fe7d4">
        <embedded/>
        <type name="Boolean"/>
    </PrimAttribute>
    <PrimAttribute name="minimumHeight" id="02dcc22c-5bfc-4e1d-bd43-c5b229b2b34e">
        <embedded/>
        <type name="Integer"/>
        <access>readonly</access>
    </PrimAttribute>
    <PrimAttribute name="minimumWidth" id="59ab9853-2ad0-41f5-ac7b-003adcae4495">
        <embedded/>
        <type name="Integer"/>
        <access>readonly</access>
    </PrimAttribute>
    <ImplicitInverseRef name="myHorizontalSplitter" id="773ef33e-43ae-4dde-ab40-75683747bad0">
        <embedded/>
        <type name="CnEcHorizontalSplitter"/>
        <access>protected</access>
    </ImplicitInverseRef>
    <ImplicitInverseRef name="myLeftControl" id="a8eb1a3d-ed27-4ef7-984e-36b3bef4b9a7">
        <embedded/>
        <type name="Control"/>
        <access>protected</access>
    </ImplicitInverseRef>
    <ImplicitInverseRef name="myRightControl" id="32c335ed-e1a5-4819-bcff-6dfa6bbb7e51">
        <embedded/>
        <type name="Control"/>
        <access>protected</access>
    </ImplicitInverseRef>
    <ImplicitInverseRef name="myVerticalSplitter" id="cb117448-aea2-4ff0-b43f-08e4db203d06">
        <embedded/>
        <type name="CnEcVerticalSplitter"/>
        <access>protected</access>
    </ImplicitInverseRef>
    <PrimAttribute name="pinnedPosition" id="319b3c09-9e5a-4409-bb1a-a6eb33d873b1">
        <embedded/>
        <type name="Real"/>
        <access>readonly</access>
    </PrimAttribute>
    <PrimAttribute name="splitType" id="8bebda89-81d3-420e-9399-808c52eaa83d">
        <embedded/>
        <type name="Integer"/>
    </PrimAttribute>
    <PrimAttribute name="stretchHeight" id="b1dc83a7-78a0-498b-b576-db34b9b92c5b">
        <embedded/>
        <type name="Boolean"/>
    </PrimAttribute>
    <PrimAttribute name="stretchWidth" id="f97ba1e5-7b03-4f49-b14a-00495b28b045">
        <embedded/>
        <type name="Boolean"/>
    </PrimAttribute>
    <PrimAttribute name="useRatio" id="33bcd714-28b3-4c7c-aa15-7405f22b7c3a">
        <embedded/>
        <type name="Boolean"/>
        <access>protected</access>
    </PrimAttribute>
    <JadeMethod name="canHaveAsChild" id="ff17521e-71c1-4df6-919f-3732f7171568">
        <source>canHaveAsChild(pProposedChild: Control; pRejectionReason: String output): Boolean;

begin
	if (not inheritMethod(pProposedChild, pRejectionReason)) then
		return false;
	endif; 

	if pProposedChild.isKindOf(BaseControl)
	or pProposedChild.isKindOf(Folder)
	or pProposedChild.isKindOf(Frame)
	or pProposedChild.isKindOf(GroupBox)
	or pProposedChild.isKindOf(ListBox)
	or pProposedChild.isKindOf(Picture)
	or pProposedChild.isKindOf(Table)
	or pProposedChild.isKindOf(TextBox)
	or pProposedChild.isKindOf(JadeEditor) // ### TFS 52202 08Nov2021 NAVPW1
	then
		return true;
	else
		pRejectionReason	:=	pProposedChild.class().name &amp; " controls not allowed in a CMUI_SplitContainer";
		return false;
	endif;
end;</source>
        <Parameter name="pProposedChild" type="Control"/>
        <Parameter name="pRejectionReason" type="String" usage="output"/>
        <ReturnType type="Boolean"/>
    </JadeMethod>
    <JadeMethod name="canResize" id="dda00c4e-8483-481d-9aec-c7069a584b8b">
        <updating/>
        <source>canResize(_xOffset, _yOffset : Integer; _minWidth, _minHeight : Integer output): Boolean updating;
//28 September 2009	cnwdw1	
//
vars

begin
	//if self.myLeftControl.CMUI_Frame.getMinimumWidth &gt;= self.myLeftControl.width then
	if self.myLeftControl.width + _xOffset &lt;= self.myLeftControl.getMinimumWidth then
		_minWidth := self.myLeftControl.getMinimumWidth;
		self.setUseRatio(false);
		return false;
	endif;
	if self.myRightControl.width - _xOffset &lt;= self.myRightControl.getMinimumWidth then
		_minWidth := self.width.Integer - self.myRightControl.getMinimumWidth;
		self.setUseRatio(false);
		return false;
	endif;

	return inheritMethod(_xOffset, _yOffset, _minWidth, _minHeight);


end;</source>
        <Parameter name="_xOffset" type="Integer"/>
        <Parameter name="_yOffset" type="Integer"/>
        <Parameter name="_minWidth" type="Integer" usage="output"/>
        <Parameter name="_minHeight" type="Integer" usage="output"/>
        <ReturnType type="Boolean"/>
    </JadeMethod>
    <JadeMethod name="cnEcResizeContents" id="60ba66cc-691d-4117-a4fb-3ce39053727f">
        <updating/>
        <source>cnEcResizeContents() updating;

begin
	self.resizeChildrenAndSplitter();
end;</source>
    </JadeMethod>
    <JadeMethod name="cnEc_MouseUp" id="95aa65ad-8411-487e-9e2c-2f9eabd3f0a1">
        <updating/>
        <source>cnEc_MouseUp(wndw: Window input; button: Integer; shift: Integer; x: Real; y: Real) updating, protected;

// Called by the splitter bar when the user mouse action is complete

begin
	// Calculate and set the new ratio, and resize the child controls

	if self.isSplitHorizontal then
		self.leftToRightRatio := (self.myHorizontalSplitter.top / self.clientHeight * 100).Integer;
	else
		self.leftToRightRatio := (self.myVerticalSplitter.left / self.clientWidth * 100).Integer;
	endif;
	
	self.pinnedPosition	:=	0;
	self.useRatio := true;

	self.resizeChildrenAndSplitter();

	// Invoke any form logic required after the move
	
	self.splitterMove(self);

	inheritMethod(wndw, button, shift, x, y);
end;</source>
        <access>protected</access>
        <Parameter name="wndw" type="Window" usage="input"/>
        <Parameter name="button" type="Integer"/>
        <Parameter name="shift" type="Integer"/>
        <Parameter name="x" type="Real"/>
        <Parameter name="y" type="Real"/>
    </JadeMethod>
    <JadeMethod name="getControlByName" id="aa769c01-3b71-430a-b8d8-70056e0ceec8">
        <source>getControlByName(_name : String) : Control;
//--------------------------------------------------
// Created:	02 October 2003 By: cnwgd1
//--------------------------------------------------
vars

begin
	if _name = null then
		return null;
	endif;

	if self.myRightControl &lt;&gt; null and
	   self.myRightControl.name = _name then
		return self.myRightControl;
	endif;
	
	if self.myLeftControl &lt;&gt; null and
	   self.myLeftControl.name = _name then
		return self.myLeftControl;
	endif;
	
	return null;
				
end;</source>
        <Parameter name="_name" type="String"/>
        <ReturnType type="Control"/>
    </JadeMethod>
    <JadeMethod name="getMinimumWidth" id="14709452-5837-4485-9941-22aa6133dfc8">
        <updating/>
        <source>getMinimumWidth(): Integer updating;
//29 September 2009	cnwml2


vars

begin
	
	if self.myLeftControl &lt;&gt; null and self.myRightControl &lt;&gt; null then
		self.minimumWidth := self.myLeftControl.getMinimumWidth + self.myRightControl.getMinimumWidth;
	else
		self.minimumWidth := null;
	endif;
	
	return minimumWidth;

epilog

end;</source>
        <ReturnType type="Integer"/>
    </JadeMethod>
    <JadeMethod name="getMyLeftControl" id="9f308830-7e6a-4a1e-a008-a896233630a3">
        <source>getMyLeftControl() :Control;
//22 October 2009	cnwml2


vars

begin
	return self.myLeftControl;
epilog

end;</source>
        <ReturnType type="Control"/>
    </JadeMethod>
    <JadeMethod name="getMyRightControl" id="73702db4-b492-46ed-ad44-a0210b709fcf">
        <source>getMyRightControl() : Control;
//--------------------------------------------------
// Created:	02 October 2003 By: cnwgd1
//--------------------------------------------------
vars

begin
	return self.myRightControl;			
end;</source>
        <ReturnType type="Control"/>
    </JadeMethod>
    <JadeMethod name="getXMLStrForTablePositions" id="7c386e1a-ecc6-4076-a2c4-b033d33a2464">
        <source>getXMLStrForTablePositions(): String;
/*

Purpose:			

Parameters:			

Return Value:		

Date		Who		Reason
15 Dec 2025	navah1	Initial creation
15 Dec 2025	navah1	MTN-36400: CUX - If the user adjusts the bar at the bottom in the Voyage Operations Live screen the change doesnt remain if the screen is closed and reopened
*/
vars
	xmlTagSplitter,
	xmlTagRatio	: String; 
begin
	xmlTagSplitter := CM_BaseForm.XMLTag_Splitter;
	xmlTagRatio := CM_BaseForm.XMLTag_Ratio;
	return	global.formatAsXMLPrefix(xmlTagSplitter &amp; self.name) &amp;
			global.formatAsXML(xmlTagRatio, self.leftToRightRatio.String) &amp;
			global.formatAsXMLSuffix(xmlTagSplitter &amp; self.name);
epilog
end;</source>
        <ReturnType type="String"/>
    </JadeMethod>
    <JadeMethod name="hideLeftControl" id="57281e6f-f4ca-4119-ae7c-44ee45d57cbe">
        <updating/>
        <source>hideLeftControl(_doResize : Boolean) updating;
// ---------------------------------------------------------------------------
// Created:     14 November 2000 by cnwrs2
//
// Purpose:     Hides the left-hand control, plus the splitter bar.
//
// Parameters:  None
//
// Returns:     Nothing
//
// Changed On   Changed By  Reason
// ----------   ----------  --------------------------------------------------
//
// ---------------------------------------------------------------------------
begin

	if self.myLeftControl &lt;&gt; null then
		self.myLeftControl.visible	:=	false;
	endif;
	
	if self.isSplitHorizontal then
		self.myHorizontalSplitter.visible	:=	false;
	else
		self.myVerticalSplitter.visible		:=	false;
	endif;

	if _doResize then
		self.setLeftToRightRatioAndResize(0);
	else
		self.setLeftToRightRatio(0);
	endif;
end;</source>
        <Parameter name="_doResize" type="Boolean"/>
    </JadeMethod>
    <JadeMethod name="hideRightControl" id="b40e7dc4-3645-4de8-aa9c-665687bcb4d9">
        <updating/>
        <source>hideRightControl(_doResize : Boolean) updating;
// ---------------------------------------------------------------------------
// Created:     19 December 2000 by cnwrs2
//
// Purpose:     Hides the right-hand control, plus the splitter bar.
//
// Parameters:  None
//
// Returns:     Nothing
//
// Changed On   Changed By  Reason
// ----------   ----------  --------------------------------------------------
//
// ---------------------------------------------------------------------------
begin
	if self.myRightControl &lt;&gt; null then
		self.myRightControl.visible	:=	false;
	endif;
	
	if self.isSplitHorizontal then
		if self.myHorizontalSplitter &lt;&gt; null then
			self.myHorizontalSplitter.visible	:=	false;
		endif;
	else
		if self.myVerticalSplitter &lt;&gt; null then
			self.myVerticalSplitter.visible		:=	false;
		endif;
	endif;

	if _doResize then
		self.setLeftToRightRatioAndResize(100);
	else
		self.setLeftToRightRatio(100);
	endif;

end;</source>
        <Parameter name="_doResize" type="Boolean"/>
    </JadeMethod>
    <JadeMethod name="initializeSplitter" id="97f35e0e-83ec-4539-ab61-13ce132e48e0">
        <updating/>
        <source>initializeSplitter() protected, updating;
// Purpose:		Initial setup for the vertical splitter bar.

vars
	theVertical		: CnEcVerticalSplitter;
	theHorizontal	: CnEcHorizontalSplitter;

begin
	if self.isSplitHorizontal then
		theHorizontal	:=	self.myHorizontalSplitter;

		if theHorizontal = null then
			create theHorizontal transient;
		endif;

		theHorizontal.parent	:=	self;
		self.form.addControl(theHorizontal);
   	else
		theVertical	:=	self.myVerticalSplitter;

		if theVertical = null then
			create theVertical transient;
		endif;

		theVertical.parent	:=	self;
		self.form.addControl(theVertical);
 	endif;
 
epilog
	if isSplitHorizontal then
		self.myHorizontalSplitter	:=	theHorizontal;	
	else
		self.myVerticalSplitter		:=	theVertical;
	endif;
end;</source>
        <access>protected</access>
    </JadeMethod>
    <JadeMethod name="leftToRightRatio" id="79efb472-a3db-42f5-9113-814f92e52e08">
        <methodInvocation>mapping</methodInvocation>
        <source>leftToRightRatio(set: Boolean; _value: Integer io) mapping, protected;

begin
	if set then
		if _value &lt; 0 or _value &gt; 100 then
			_value	:=	50;
		endif;
	endif;
end;</source>
        <access>protected</access>
        <Parameter name="set" type="Boolean"/>
        <Parameter name="_value" type="Integer" usage="io"/>
    </JadeMethod>
    <JadeMethod name="lockBottom" id="3d475c05-80f3-4baf-81e8-6852b6446caf">
        <methodInvocation>mapping</methodInvocation>
        <updating/>
        <source>lockBottom(set: Boolean; _value: Boolean io) updating, mapping;

begin
	if set and _value then
		if stretchHeight then
			stretchHeight	:=	false;
		endif;
	endif;
end;</source>
        <Parameter name="set" type="Boolean"/>
        <Parameter name="_value" type="Boolean" usage="io"/>
    </JadeMethod>
    <JadeMethod name="lockRight" id="fda32d7c-9d60-4bc0-b4d5-160dc69ce0ff">
        <methodInvocation>mapping</methodInvocation>
        <updating/>
        <source>lockRight(set: Boolean; _value: Boolean io) mapping, updating;

begin
	if set and _value then
		if stretchWidth then
			stretchWidth	:=	false;
		endif;
	endif;
end;</source>
        <Parameter name="set" type="Boolean"/>
        <Parameter name="_value" type="Boolean" usage="io"/>
    </JadeMethod>
    <JadeMethod name="repositionAfterResize" id="4da50c88-1e90-4044-9d63-258c36aa7036">
        <updating/>
        <source>repositionAfterResize() updating;

vars
	newLeft		: Real;
	newTop		: Real;
	newWidth	: Real;
	newHeight	: Real;
   	parentWidth,
   	parentHeight	: Real;
	
begin

	if self.parent.isKindOf(Form) then
		parentWidth  := self.parent.Form.clientWidth;
		parentHeight := self.parent.Form.clientHeight;
	elseif self.parent.isKindOf(Control) then
		parentWidth  := self.parent.Control.clientWidth;
		parentHeight := self.parent.Control.clientHeight;
	else
		parentWidth  := self.parent.width;
		parentHeight := self.parent.height;
	endif;

	newLeft		:=	self.left;
	newTop		:=	self.top;
	newWidth	:=	self.width;
	newHeight	:=	self.height;

	if self.lockRight then
		newLeft	:=	(parentWidth - newWidth - self.distanceFromRight).max(0);
	endif;
	
	if self.lockBottom then
		newTop	:=	(parentHeight - newHeight - self.distanceFromBottom).max(0);
	endif;
	
	if self.stretchWidth then
		newWidth	:=	(parentWidth - newLeft - self.distanceFromRight).max(0);
	endif;
	
	if self.stretchHeight then
		newHeight	:=	(parentHeight - newTop - self.distanceFromBottom).max(0);
	endif;
	
	if newLeft &lt;&gt; self.left
	or newTop &lt;&gt; self.top
	or newWidth &lt;&gt; self.width
	or newHeight &lt;&gt; self.height
	then 
		self.move(newLeft, newTop, newWidth, newHeight);
		self.cnEcResizeContents();
	endif;
end;</source>
    </JadeMethod>
    <JadeMethod name="resizeChildren" id="74f0d471-3491-4d6f-b4ba-450febdcec08">
        <source>resizeChildren(pParent : Window) protected;
// ---------------------------------------------------------------------------
// Created:     16 November 2000 by cnwrs2
//
// Purpose:     Resizes the receiver's child controls, typically after the
//				receiver itself has been resized.
//
// Parameters:  pParent - The receiver's parent window.
//
// Returns:     Nothing
//
// Changed On   Changed By  Reason
// ----------   ----------  --------------------------------------------------
//
// ---------------------------------------------------------------------------
vars
	controlIndex	: Integer;
	theControl		: Control;
	
begin
	foreach controlIndex in 1 to self.form.controlCount() do
		theControl	:=	self.form.controls(controlIndex);
		
		if theControl.parent = pParent then
//			theControl.cnEcResizeContents();
			theControl.repositionAfterResize();
		endif;
	endforeach;
end;</source>
        <access>protected</access>
        <Parameter name="pParent" type="Window"/>
    </JadeMethod>
    <JadeMethod name="resizeChildrenAndSplitter" id="3fca8d0e-af4e-414f-9567-f8dcf15e7436">
        <updating/>
        <source>resizeChildrenAndSplitter() updating, protected;
//Purpose:		Positions and sizes the left and right controls, and the vertical splitter
//				on the control
//
//14 Nov 00	cnwrs2	Add code to set seperation to 0 if splitter is not visible.
//Oct 02 cnwrs2 - Changes to cater for splitType
vars
	separation	: Integer;
	leftSize	: Real;
	rightSize	: Real;
	
begin

	self.resizeSplitterGetNewSizes(leftSize, rightSize, separation);
	if leftSize &lt; 5 and leftSize &lt;&gt; 0 then
		if self.isSplitHorizontal and self.myLeftControl &lt;&gt; null then
			leftSize := self.height / 2;
		elseif not self.isSplitHorizontal and self.myLeftControl &lt;&gt; null then
			leftSize := self.width / 3;
		endif;
	endif;

	if self.isSplitHorizontal then
	 	// Handle horizontal split

		if self.myLeftControl &lt;&gt; null then
			self.myLeftControl.move(0, 0, self.clientWidth, leftSize);
			self.myLeftControl.cnEcResizeContents();

			// removed special treatment of a Frame - not sure why this is done (rjs 4Jul03) - also below
			//if self.myLeftControl.isKindOf(Frame) then
			//	self.resizeChildren(self.myLeftControl);
			//endif;
		endif;

		if self.myRightControl &lt;&gt; null then
			self.myRightControl.move(0, leftSize + separation, self.clientWidth, rightSize);
			self.myRightControl.cnEcResizeContents();
			
			//if self.myRightControl.isKindOf(Frame) then
			//	self.resizeChildren(self.myRightControl);
			//endif;
		endif;

		if self.myHorizontalSplitter &lt;&gt; null then
			self.myHorizontalSplitter.move(0, leftSize, self.clientWidth, self.myHorizontalSplitter.height);
			self.myHorizontalSplitter.zOrder(1);
		endif;
	else
		// Handle vertical split

		if self.myLeftControl &lt;&gt; null then
			self.myLeftControl.move(0, 0, leftSize, self.clientHeight);
			self.myLeftControl.cnEcResizeContents();

			//if self.myLeftControl.isKindOf(Frame) then
			//	self.resizeChildren(self.myLeftControl);
			//endif;
		endif;

		if self.myRightControl &lt;&gt; null then
			self.myRightControl.move(	leftSize + separation,
										0,
										rightSize,
										self.clientHeight);

			self.myRightControl.cnEcResizeContents();
			
			//if self.myRightControl.isKindOf(Frame) then
			//	self.resizeChildren(self.myRightControl);
			//endif;
		endif;

		if self.myVerticalSplitter &lt;&gt; null then
			self.myVerticalSplitter.move(leftSize, 0, self.myVerticalSplitter.width, self.clientHeight);
			self.myVerticalSplitter.zOrder(1);
		endif;
	endif;

end;</source>
        <access>protected</access>
    </JadeMethod>
    <JadeMethod name="resizeChildrenAndSplitterOLD" id="23110d65-7142-48df-9eb8-9c6cc9e89326">
        <updating/>
        <source>resizeChildrenAndSplitterOLD(_fromResize : Boolean) updating, protected;
//Purpose:		Positions and sizes the left and right controls, and the vertical splitter
//				on the control
//
//14 Nov 00	cnwrs2	Add code to set seperation to 0 if splitter is not visible.
//Oct 02 cnwrs2 - old method kept in case need to refer to after change to add splitType.
vars
	ratio		: Decimal[4,2];
	separation	: Integer;

begin

	ratio	:=	self.leftToRightRatio / 100;

	if self.isSplitHorizontal then
	 	// Handle horizontal split

		if self.myHorizontalSplitter = null or (not self.myHorizontalSplitter.visible) then
			separation	:=	0;
		else
			separation	:=	self.myHorizontalSplitter.height.Integer;
		endif;

		if self.myLeftControl &lt;&gt; null then
			self.myLeftControl.move(0, 0, self.clientWidth, (self.clientHeight * ratio) - (separation div 2));
			self.myLeftControl.cnEcResizeContents();
			
			if self.myLeftControl.isKindOf(Frame) then
				self.resizeChildren(self.myLeftControl);
			endif;
		endif;

		if (self.myRightControl &lt;&gt; null) and (self.myLeftControl &lt;&gt; null) then
			self.myRightControl.move(self.myLeftControl.left, self.myLeftControl.height + separation, self.myLeftControl.width, self.clientHeight - self.myLeftControl.height - separation);
			self.myRightControl.cnEcResizeContents();
			
			if self.myRightControl.isKindOf(Frame) then
				self.resizeChildren(self.myRightControl);
			endif;
		endif;

		if (self.myHorizontalSplitter &lt;&gt; null) and (self.myLeftControl &lt;&gt; null) then
			self.myHorizontalSplitter.move(self.myLeftControl.left, self.myLeftControl.top + self.myLeftControl.height, self.myLeftControl.width, self.myHorizontalSplitter.height);
			self.myHorizontalSplitter.zOrder(1);
		endif;

	else
		// Handle vertical split

		if self.myVerticalSplitter = null or (not self.myVerticalSplitter.visible) then
			separation	:=	0;
		else
			separation	:=	self.myVerticalSplitter.width.Integer;
		endif;

		if self.myLeftControl &lt;&gt; null then
			self.myLeftControl.move(0, 0, (self.clientWidth * ratio) - (separation div 2), self.clientHeight);
			self.myLeftControl.cnEcResizeContents();
			
			if self.myLeftControl.isKindOf(Frame) then
				self.resizeChildren(self.myLeftControl);
			endif;
		endif;

		if (self.myRightControl &lt;&gt; null) and (self.myLeftControl &lt;&gt; null) then
			self.myRightControl.move(self.myLeftControl.width + separation, self.myLeftControl.top, self.clientWidth - self.myLeftControl.width - separation, self.myLeftControl.height);
			self.myRightControl.cnEcResizeContents();
			
			if self.myRightControl.isKindOf(Frame) then
				self.resizeChildren(self.myRightControl);
			endif;
		endif;

		if (self.myVerticalSplitter &lt;&gt; null) and (self.myLeftControl &lt;&gt; null) then
			self.myVerticalSplitter.move(self.myLeftControl.left + self.myLeftControl.width, self.myLeftControl.top, self.myVerticalSplitter.width, self.myLeftControl.height.min(self.myRightControl.height));
			self.myVerticalSplitter.zOrder(1);
		endif;

	endif;

end;</source>
        <access>protected</access>
        <Parameter name="_fromResize" type="Boolean"/>
    </JadeMethod>
    <JadeMethod name="resizeSplitterGetNewSizes" id="2ca4e5ee-1959-4277-bd20-a44b20de1fc1">
        <updating/>
        <source>resizeSplitterGetNewSizes(pLeftSize: Real output;
						pRightSize : Real output;
						pSeparation : Integer output) updating, protected;
//Purpose:		Positions and sizes the left and right controls, and the vertical splitter
//				on the control
//
// Ticket 44332 workaround 02/07/2019 jlgdap1
constants
	MIN_SIZE	: Integer = 20;
	
vars
	ratio				: Decimal[4,2];
	leftControlSize		: Real;
	rightControlSize	: Real;
	clientSize			: Real;

begin

	//ratio	:=	self.leftToRightRatio / 100;				// Ticket 44332
	ratio	:=	(self.leftToRightRatio / 100.Decimal);		// Ticket 44332

	if self.isSplitHorizontal then
	 	// Handle horizontal split

		if self.myHorizontalSplitter = null or (not self.myHorizontalSplitter.visible) then
			pSeparation	:=	0;
		else
			pSeparation	:= self.myHorizontalSplitter.height.Integer;
		endif;
		
		clientSize := self.clientHeight;
		
//		if self.myLeftControl &lt;&gt; null then
		if self.myLeftControl &lt;&gt; null and
		   self.myLeftControl.visible then
			leftControlSize		:= self.myLeftControl.height;
		else
			// possibly need to do some calculations here
			leftControlSize		:= 0;
		endif;

//		if self.myRightControl &lt;&gt; null then
		if self.myRightControl &lt;&gt; null and
		   self.myRightControl.visible then
			rightControlSize	:= self.myRightControl.height;
		else
			rightControlSize	:= 0;
		endif;
	else
		if self.myVerticalSplitter = null or (not self.myVerticalSplitter.visible) then
			pSeparation	:=	0;
		else
			pSeparation	:=	self.myVerticalSplitter.width.Integer;
		endif;

		clientSize := self.clientWidth;

//		if self.myLeftControl &lt;&gt; null then
		if self.myLeftControl &lt;&gt; null and
		   self.myLeftControl.visible then
			leftControlSize		:= self.myLeftControl.width;
		else
			// possibly need to do some calculations here
			leftControlSize		:= 0;
		endif;

//		if self.myRightControl &lt;&gt; null then
		if self.myRightControl &lt;&gt; null and
		   self.myRightControl.visible then
			rightControlSize	:= self.myRightControl.width;
		else
			rightControlSize	:= 0;
		endif;
	endif;

	// Calculate new sizes for left and right controls;
	
	if (not self.useRatio) and self.splitType = self.SPLITTYPE_LOCKLEFT then
		if self.pinnedPosition &lt;&gt; 0 and rightControlSize &gt; 0 then
			// Always attempt to reset control to pinned size
			leftControlSize		:=	self.pinnedPosition;
			self.pinnedPosition	:=	0;
		endif;
		
		if leftControlSize + MIN_SIZE &gt; clientSize and
		   rightControlSize &gt; 0 then
			if self.pinnedPosition = 0 then
				self.pinnedPosition	:=	leftControlSize;
			endif;
			pLeftSize	:=	(clientSize - MIN_SIZE) - (pSeparation div 2);
		elseif rightControlSize = 0 then
			pLeftSize	:=	clientSize - pSeparation;
		else
			pLeftSize	:=	leftControlSize;
		endif;
		
		pRightSize	:=	clientSize - pLeftSize - pSeparation;

	elseif (not self.useRatio) and self.splitType = self.SPLITTYPE_LOCKRIGHT then
		if self.pinnedPosition &lt;&gt; 0 and leftControlSize &gt; 0 then
			// Always attempt to reset control to pinned size
			rightControlSize	:=	self.pinnedPosition;
			self.pinnedPosition	:=	0;
		endif;
		
		if rightControlSize + MIN_SIZE &gt; clientSize and
		   leftControlSize &gt; 0 then
			if self.pinnedPosition = 0 then
				self.pinnedPosition := rightControlSize;
			endif;
			
			pRightSize	:=	(clientSize - MIN_SIZE) - (pSeparation div 2);
		elseif leftControlSize = 0 then
			pRightSize	:=	clientSize - pSeparation;
		else
			pRightSize	:=	rightControlSize;
		endif;
		
		pLeftSize	:=	clientSize - pRightSize - pSeparation;
	else
		pLeftSize	:=	(clientSize * ratio) - (pSeparation div 2);
		pRightSize	:=	clientSize - pLeftSize - pSeparation;
	endif;
	
	if self.getMinimumWidth &gt; 0 and self.myVerticalSplitter &lt;&gt; null then
		if self.getMinimumWidth + self.myVerticalSplitter.width + 2 &gt;= self.width  then
			pLeftSize := self.myLeftControl.getMinimumWidth;
			pRightSize := self.myRightControl.getMinimumWidth;
		endif;
	elseif self.getMinimumWidth &gt; 0 and self.myHorizontalSplitter &lt;&gt; null then
		if self.getMinimumWidth + self.myHorizontalSplitter.width + 2 &gt;= self.width  then
			pLeftSize := self.myLeftControl.getMinimumWidth;
			pRightSize := self.myRightControl.getMinimumWidth;
		endif;
	endif;

	
	self.useRatio := false;

end;</source>
        <access>protected</access>
        <Parameter name="pLeftSize" type="Real" usage="output"/>
        <Parameter name="pRightSize" type="Real" usage="output"/>
        <Parameter name="pSeparation" type="Integer" usage="output"/>
    </JadeMethod>
    <JadeMethod name="setDistanceFromBottom" id="c084877f-48f0-41c6-9e57-def81ae0aebe">
        <updating/>
        <source>setDistanceFromBottom(value: Real) updating;

begin

epilog	
	if self.distanceFromBottom &lt;&gt; value then 
		self.distanceFromBottom	:=	value;
	endif;
end;</source>
        <Parameter name="value" type="Real"/>
    </JadeMethod>
    <JadeMethod name="setDistanceFromRight" id="08123241-79c7-41b1-9ea3-e0bccd30673f">
        <updating/>
        <source>setDistanceFromRight(value: Real) updating;

begin

epilog	
	if self.distanceFromRight &lt;&gt; value then 
		self.distanceFromRight	:=	value;
	endif;
end;</source>
        <Parameter name="value" type="Real"/>
    </JadeMethod>
    <JadeMethod name="setLeftControl" id="f4feb0bc-3638-40a3-9e77-0d6bb1427a24">
        <updating/>
        <source>setLeftControl(_control : Control input) updating;
// ---------------------------------------------------------------------------
// Created:     28 August 2003 by cnwrs2
//
// Purpose:     
// ---------------------------------------------------------------------------
vars

begin

	self.myLeftControl := _control;
	if _control &lt;&gt; null then
		_control.parent := self;
	endif;

end;</source>
        <Parameter name="_control" type="Control" usage="input"/>
    </JadeMethod>
    <JadeMethod name="setLeftToRightRatio" id="3a1def44-9e89-4825-ab23-ff905ada85d4">
        <updating/>
        <source>setLeftToRightRatio(value : Integer) updating;

// no resize is done here as it is assumed a general resize of the form will be done later.
begin
	
	self.useRatio := true;
	self.leftToRightRatio := value;

end;</source>
        <Parameter name="value" type="Integer"/>
    </JadeMethod>
    <JadeMethod name="setLeftToRightRatioAndResize" id="7f90aa9c-ae25-4671-8d08-da86e59d4eab">
        <updating/>
        <source>setLeftToRightRatioAndResize(value : Integer) updating;
//Purpose:		Update the ratio (in percent) that the left control uses of the parent
//				control width
begin

	if value &lt;&gt; self.leftToRightRatio then
		self.leftToRightRatio	:=	value;
		self.useRatio := true;
		self.resizeChildrenAndSplitter();
	endif;

end;</source>
        <Parameter name="value" type="Integer"/>
    </JadeMethod>
    <JadeMethod name="setLeftToRightSize" id="42af4140-feb0-4d08-98b6-ff551291cf12">
        <updating/>
        <source>setLeftToRightSize(_leftWidth : Integer) updating;
// ---------------------------------------------------------------------------
// Created:		October 2003 By Andrew Bishop cnwab1
//
// Purpose:		This attempts to set the size  by pixels instead of ratio
//
// ---------------------------------------------------------------------------

vars
	totSize, leftRatio : Integer;
begin
	totSize := self.clientWidth;
	
	leftRatio := ((_leftWidth / totSize) * 100).Integer;
	
	self.setLeftToRightRatio(leftRatio);
end;</source>
        <Parameter name="_leftWidth" type="Integer"/>
    </JadeMethod>
    <JadeMethod name="setLeftToRightSizeAndResize" id="51103e12-d280-4f4c-b760-ea4fb0a18206">
        <updating/>
        <source>setLeftToRightSizeAndResize(_leftWidth : Integer) updating;
// ---------------------------------------------------------------------------
// Created:		October 2003 By Andrew Bishop cnwab1
//
// Purpose:		This attempts to set the size  by pixels instead of ratio
//
// ---------------------------------------------------------------------------

vars
	totSize, leftRatio : Integer;
begin
	//03 Mar 2015	NDL	Par 35733
	if _leftWidth &lt; 0 then
		// probably the max integer oveflow size  i.e. -2,147,483,593
		return;
	endif;

	if self.isSplitHorizontal then
		totSize := self.clientHeight;
	else
		totSize := self.clientWidth;
	endif;
	
	//03 Mar 2015	NDL	Par 35733
	if ( totSize = 0 ) or ( _leftWidth &gt;= totSize ) then
		return;
	endif;
	
	leftRatio := ((_leftWidth / totSize) * 100).Integer + 1;	// add one to force rounded up
	
	self.setLeftToRightRatioAndResize(leftRatio);
end;</source>
        <Parameter name="_leftWidth" type="Integer"/>
    </JadeMethod>
    <JadeMethod name="setRightControl" id="01457871-cf78-4515-8cb1-80b5cee288f3">
        <updating/>
        <source>setRightControl(_control : Control input) updating;
//-----------------------------------------
// Created: October 2003 By: cnwgd1
//-----------------------------------------
vars

begin

	self.myRightControl := _control;
	if _control &lt;&gt; null then
		_control.parent := self;
	endif;

end;</source>
        <Parameter name="_control" type="Control" usage="input"/>
    </JadeMethod>
    <JadeMethod name="setSplitDirection" id="1274ab13-c245-4079-b200-eb0592f2e3c2">
        <updating/>
        <source>setSplitDirection(_isSplitHorizontal : Boolean) updating;
// ---------------------------------------------------------------------------
// Created:     08 August 2003 by cnwrs2
//
// Purpose:     
// ---------------------------------------------------------------------------
vars
	theVertical		: CnEcVerticalSplitter;
	theHorizontal	: CnEcHorizontalSplitter;

begin

	if self.isSplitHorizontal &lt;&gt; _isSplitHorizontal then
		self.isSplitHorizontal := _isSplitHorizontal;
	endif;
	
	if _isSplitHorizontal then
		if self.myVerticalSplitter &lt;&gt; null then
			delete self.myVerticalSplitter;
		endif;
		if self.myHorizontalSplitter = null then
			create theHorizontal transient;
			theHorizontal.parent	:=	self;
			self.form.addControl(theHorizontal);
			self.myHorizontalSplitter := theHorizontal;
		endif;
	else
		if self.myHorizontalSplitter &lt;&gt; null then
			delete self.myHorizontalSplitter;
		endif;
		if self.myVerticalSplitter = null then
			create theVertical transient;
			theVertical.parent	:=	self;
			self.form.addControl(theVertical);
			self.myVerticalSplitter	:= theVertical;
		endif;
	endif;

end;</source>
        <Parameter name="_isSplitHorizontal" type="Boolean"/>
    </JadeMethod>
    <JadeMethod name="setTablePositionsFromXMLStr" id="5de85845-7705-43a1-9f9f-8c0a312d3098">
        <updating/>
        <source>setTablePositionsFromXMLStr(_xml: String) updating;
/*

Purpose:			

Parameters:			

Return Value:		

Date		Who		Reason
15 Dec 2025	navah1	Initial creation
15 Dec 2025	navah1	MTN-36400: CUX - If the user adjusts the bar at the bottom in the Voyage Operations Live screen the change doesnt remain if the screen is closed and reopened
*/

vars
	xmlTagSplitter,
	xmlTagRatio,
	splitterName	: String;
	splitterRatio	: Integer; 
begin
	xmlTagSplitter := CM_BaseForm.XMLTag_Splitter;
	xmlTagRatio := CM_BaseForm.XMLTag_Ratio;
	splitterName := _xml.cnEcGetTagValueOfName(xmlTagSplitter &amp; self.name);
	if splitterName &lt;&gt; null then
		splitterRatio := splitterName.cnEcGetTagValueOfName(xmlTagRatio).Integer;
		self.setLeftToRightRatio(splitterRatio);
    endif;
	
epilog
end;</source>
        <Parameter name="_xml" type="String"/>
    </JadeMethod>
    <JadeMethod name="setUseRatio" id="4b44e384-cb10-4e74-ad0e-cb0cc5afce41">
        <updating/>
        <source>setUseRatio(_value : Boolean)updating;
//29 September 2009	cnwml2


vars

begin
	self.useRatio := _value;
epilog

end;</source>
        <Parameter name="_value" type="Boolean"/>
    </JadeMethod>
    <JadeMethod name="setupChildControls" id="41238bb3-26f9-417a-9af7-2e7d672b70b6">
        <updating/>
        <source>setupChildControls() updating;
// Purpose:	Setup the left and right child controls the children of the receiver, and
//			setup the starting ratio to reflect the painted ratios of the visible child 
//			controls.
//
// Note:	The container control only supports two child controls.
vars
	theControl		: Control;
	controlIndex	: Integer;
	totalSize		: Real;
	errorMessage	: String;

begin
	// First delete any existing splitter bars
	if self.myVerticalSplitter &lt;&gt; null then
		delete self.myVerticalSplitter;
	endif;

	if self.myHorizontalSplitter &lt;&gt; null then
		delete self.myHorizontalSplitter;
	endif;

	// Forget existing controls
	self.myLeftControl	:=	null;
	self.myRightControl	:=	null;

	// Look through the controls of the form for children of self
	foreach controlIndex in 1 to self.form.controlCount do
		theControl := self.form.controls(controlIndex);
		
		if theControl.isKindOf(ScrollBar) or
		   theControl.isKindOf(CnEcVerticalSplitter) or
		   theControl.isKindOf(CnEcHorizontalSplitter) then
		   	continue;
		endif;

		if theControl.parent = self then
			if self.myLeftControl = null then
	    		self.myLeftControl := theControl;
	    	else
	    		if self.myRightControl = null then

					// check the position child controls and set appropriately 
		    		if (not self.isSplitHorizontal) and 
		    		   (theControl.left &gt; self.myLeftControl.left) or 
		    		   self.isSplitHorizontal and (theControl.top &gt; myLeftControl.top)
		    			then
		    			self.myRightControl	:=	theControl;
		    		else
		    			self.myRightControl	:=	self.myLeftControl;
		    			self.myLeftControl	:=	theControl;
		    		endif;
				else
					errorMessage	:=	self.class().name &amp; '::' &amp; method.name &amp;
										"  Error&gt;&gt; you can only have 2 child controls.";
					app.msgBox(errorMessage, app.getErrorString(Error), MsgBox_OK_Only);
				endif;
			endif;
	 	endif;
	endforeach;

	if self.myRightControl = null then
		// write self.class().name &amp; '::' &amp; method.name &amp; '  Error&gt;&gt; you must have 2 child controls';
		// return added to avoid null object ref if cloning at run time (RJS 7Feb01)
		return;
	endif;

	if self.myLeftControl.visible and self.myRightControl.visible then
		// Setup the ratios of the children based on their current widths/heights if not
		//  defined in Painter
	 	if self.leftToRightRatio = 0 then
	 		if self.isSplitHorizontal then
	 			totalSize				:=	self.myLeftControl.height + self.myRightControl.height;
				self.leftToRightRatio	:=	(self.myLeftControl.height / totalSize * 100).Integer;
 			else
				totalSize				:=	self.myLeftControl.width + self.myRightControl.width;
				self.leftToRightRatio	:=	(self.myLeftControl.width / totalSize * 100).Integer;
			endif;
 		endif;
 	else
 		if self.myLeftControl.visible then
 			self.leftToRightRatio	:=	100;
 		else
			self.leftToRightRatio	:=	0;
		endif;
	endif;
	
	// Following was moved from windowCreated to allow for control to be cloned at runtime (RJS 7Feb01).
	if (self.myLeftControl &lt;&gt; null) and
	   self.myLeftControl.visible and
	   (self.myRightControl &lt;&gt; null) and
	   self.myRightControl.visible	then
		self.setSplitDirection(self.isSplitHorizontal);
	endif;
end;</source>
    </JadeMethod>
    <JadeMethod name="setupChildControlsLeftRight" id="137e9f42-3596-4f04-bdce-73d5969c2fa3">
        <updating/>
        <source>setupChildControlsLeftRight(_lControl : Control input;_rControl : Control input) updating;
// ---------------------------------------------------------------------------
// Created:		October 2003 By Andrew Bishop cnwab1
//
// Purpose:	Setup the left and right child controls the children of the receiver, and
//			setup the starting ratio to reflect the painted ratios of the visible child 
//			controls.
//
// Note:	The container control only supports two child controls.
vars
	theControl		: Control;
	controlIndex	: Integer;
	totalSize		: Real;
	errorMessage	: String;

begin
	// First delete any existing splitter bars
	if self.myVerticalSplitter &lt;&gt; null then
		delete self.myVerticalSplitter;
	endif;

	if self.myHorizontalSplitter &lt;&gt; null then
		delete self.myHorizontalSplitter;
	endif;

	// Forget existing controls
	self.myLeftControl	:=	_lControl;
	self.myRightControl	:=	_rControl;


	if self.myRightControl = null then
		// write self.class().name &amp; '::' &amp; method.name &amp; '  Error&gt;&gt; you must have 2 child controls';
		// return added to avoid null object ref if cloning at run time (RJS 7Feb01)
		return;
	endif;

	if self.myLeftControl.visible and self.myRightControl.visible then
		// Setup the ratios of the children based on their current widths/heights if not
		//  defined in Painter
	 	if self.leftToRightRatio = 0 then
	 		if self.isSplitHorizontal then
	 			totalSize				:=	self.myLeftControl.height + self.myRightControl.height;
				self.leftToRightRatio	:=	(self.myLeftControl.height / totalSize * 100).Integer;
 			else
				totalSize				:=	self.myLeftControl.width + self.myRightControl.width;
				self.leftToRightRatio	:=	(self.myLeftControl.width / totalSize * 100).Integer;
			endif;
 		endif;
 	else
 		if self.myLeftControl.visible then
 			self.leftToRightRatio	:=	100;
 		else
			self.leftToRightRatio	:=	0;
		endif;
	endif;
	
	// Following was moved from windowCreated to allow for control to be cloned at runtime (RJS 7Feb01).
	if (self.myLeftControl &lt;&gt; null) and
	   self.myLeftControl.visible and
	   (self.myRightControl &lt;&gt; null) and
	   self.myRightControl.visible	then
		self.setSplitDirection(self.isSplitHorizontal);
	endif;
end;</source>
        <Parameter name="_lControl" type="Control" usage="input"/>
        <Parameter name="_rControl" type="Control" usage="input"/>
    </JadeMethod>
    <JadeMethod name="showLeftControl" id="bcb497a8-9cc7-4415-8c66-6815a1804236">
        <updating/>
        <source>showLeftControl(pRatio : Integer; _doResize : Boolean) updating;
// ---------------------------------------------------------------------------
// Created:     14 November 2000 by cnwrs2
//
// Purpose:     Make visible the receiver's left-hand control and spliter bar.
//
// Parameters:  pRatio - The ratio (in percent) that the left control uses of
//					the parent control's width.
//
// Returns:     Nothing
//
// Changed On   Changed By  Reason
// ----------   ----------  --------------------------------------------------
//
// ---------------------------------------------------------------------------
begin
	if self.isSplitHorizontal then
		self.myHorizontalSplitter.visible	:=	true;
	else
		self.myVerticalSplitter.visible		:=	true;
	endif;

	if self.myLeftControl &lt;&gt; null then
		self.myLeftControl.visible	:=	true;
	endif;

	if _doResize then
		self.setLeftToRightRatioAndResize(pRatio);
	else
		self.setLeftToRightRatio(pRatio);
	endif;

end;</source>
        <Parameter name="pRatio" type="Integer"/>
        <Parameter name="_doResize" type="Boolean"/>
    </JadeMethod>
    <JadeMethod name="showLeftControlAndSize" id="062c0519-7379-429a-b042-5569be1910bb">
        <updating/>
        <source>showLeftControlAndSize(_size : Integer) updating;
// ---------------------------------------------------------------------------
// Created:     14 November 2000 by cnwrs2
//
// Purpose:     Make visible the receiver's left-hand control and spliter bar.
//
// Parameters:  _size - The ratio (in percent) that the left control uses of
//					the parent control's width.
//
// Returns:     Nothing
//
// Changed On   Changed By  Reason
// ----------   ----------  --------------------------------------------------
//
// ---------------------------------------------------------------------------
begin
	if self.isSplitHorizontal then
		self.myHorizontalSplitter.visible	:=	true;
	else
		self.myVerticalSplitter.visible		:=	true;
	endif;

	if self.myLeftControl &lt;&gt; null then
		self.myLeftControl.visible	:=	true;
	endif;

	self.setLeftToRightSizeAndResize(_size);
end;</source>
        <Parameter name="_size" type="Integer"/>
    </JadeMethod>
    <JadeMethod name="showRightControl" id="58fd06aa-85aa-4b39-a275-12a356c240a4">
        <updating/>
        <source>showRightControl(pRatio : Integer; _doResize : Boolean) updating;
// ---------------------------------------------------------------------------
// Created:     19 December 2000 by cnwrs2
//
// Purpose:     Make visible the receiver's right-hand control and spliter bar.
//
// Parameters:  pRatio - The ratio (in percent) that the right control uses of
//					the parent control's width.
//
// Returns:     Nothing
//
// Changed On   Changed By  Reason
// ----------   ----------  --------------------------------------------------
//
// ---------------------------------------------------------------------------
begin
	if self.isSplitHorizontal then
		self.myHorizontalSplitter.visible	:=	true;
	else
		self.myVerticalSplitter.visible		:=	true;
	endif;

	if self.myRightControl &lt;&gt; null then
		self.myRightControl.visible	:=	true;
	endif;

	if _doResize then
		self.setLeftToRightRatioAndResize(100 - pRatio);
	else
		self.setLeftToRightRatio(100 - pRatio);
	endif;

end;</source>
        <Parameter name="pRatio" type="Integer"/>
        <Parameter name="_doResize" type="Boolean"/>
    </JadeMethod>
    <JadeMethod name="sourceCompareResizeContents" id="d4b147b6-a4f3-41d1-986a-7455778a8538">
        <updating/>
        <source>sourceCompareResizeContents() updating;
/*

Purpose:			

Parameters:			

Return Value:		

Date		Who		Reason
05 March 2025	navpw1	Initial creation

*/

vars
separation	: Integer;
	leftSize	: Real;
	rightSize	: Real;
	
begin
	self.resizeSplitterGetNewSizes(leftSize, rightSize, separation);
	if leftSize &lt; 5 and leftSize &lt;&gt; 0 then
		if self.isSplitHorizontal and self.myLeftControl &lt;&gt; null then
			leftSize := (self.height / 2)-24;
		elseif not self.isSplitHorizontal and self.myLeftControl &lt;&gt; null then
			leftSize := (self.width / 3)-24;
		endif;
	endif;

	if self.isSplitHorizontal then
	 	// Handle horizontal split

		if self.myLeftControl &lt;&gt; null then
			self.myLeftControl.move(0, 0, self.clientWidth, leftSize);
			self.myLeftControl.cnEcResizeContents();

			// removed special treatment of a Frame - not sure why this is done (rjs 4Jul03) - also below
			//if self.myLeftControl.isKindOf(Frame) then
			//	self.resizeChildren(self.myLeftControl);
			//endif;
		endif;

		if self.myRightControl &lt;&gt; null then
			self.myRightControl.move(0, leftSize + separation, self.clientWidth, rightSize);
			self.myRightControl.cnEcResizeContents();
			
			//if self.myRightControl.isKindOf(Frame) then
			//	self.resizeChildren(self.myRightControl);
			//endif;
		endif;

		if self.myHorizontalSplitter &lt;&gt; null then
			self.myHorizontalSplitter.move(0, leftSize, self.clientWidth, self.myHorizontalSplitter.height);
			self.myHorizontalSplitter.zOrder(1);
		endif;
	else
		// Handle vertical split

		if self.myLeftControl &lt;&gt; null then
			self.myLeftControl.move(0, 0, leftSize, self.clientHeight);
			self.myLeftControl.cnEcResizeContents();

			//if self.myLeftControl.isKindOf(Frame) then
			//	self.resizeChildren(self.myLeftControl);
			//endif;
		endif;

		if self.myRightControl &lt;&gt; null then
			self.myRightControl.move(	leftSize + separation,
										0,
										rightSize,
										self.clientHeight);

			self.myRightControl.cnEcResizeContents();
			
			//if self.myRightControl.isKindOf(Frame) then
			//	self.resizeChildren(self.myRightControl);
			//endif;
		endif;

		if self.myVerticalSplitter &lt;&gt; null then
			self.myVerticalSplitter.move(leftSize, 0, self.myVerticalSplitter.width, self.clientHeight);
			self.myVerticalSplitter.zOrder(1);
		endif;
	endif;
	
end;</source>
    </JadeMethod>
    <ExternalMethod name="splitterMove" id="381e0b12-1cbc-4bf8-b871-7c79d8fc67a3">
        <updating/>
        <entrypoint>CallControlEvent</entrypoint>
        <library name="jadpmap"/>
        <source>splitterMove(pSplitContainer: CMUI_SplitContainer input) is "CallControlEvent" in "jadpmap" updating, number = 1018;</source>
        <Parameter name="pSplitContainer" type="CMUI_SplitContainer" usage="input"/>
    </ExternalMethod>
    <JadeMethod name="stretchHeight" id="69cb6ead-9291-40e7-bca0-45a167da10a4">
        <methodInvocation>mapping</methodInvocation>
        <updating/>
        <source>stretchHeight(set: Boolean; _value: Boolean io) mapping, updating;

begin
	if set and _value then
		if self.lockBottom then
			self.lockBottom	:=	false;
		endif;
	endif;
end;</source>
        <Parameter name="set" type="Boolean"/>
        <Parameter name="_value" type="Boolean" usage="io"/>
    </JadeMethod>
    <JadeMethod name="stretchWidth" id="773890f6-36bc-4f7e-b390-51920643e6e0">
        <methodInvocation>mapping</methodInvocation>
        <updating/>
        <source>stretchWidth(set: Boolean; _value: Boolean io) mapping, updating;

begin
    if set and _value then
    	if self.lockRight then
    		self.lockRight	:=	false;
    	endif;
   	endif;
end;</source>
        <Parameter name="set" type="Boolean"/>
        <Parameter name="_value" type="Boolean" usage="io"/>
    </JadeMethod>
    <JadeMethod name="windowCreated" id="c4d23eef-8bd0-4b62-bd61-aec517d7d476">
        <updating/>
        <source>windowCreated(cntrl: Control input; persistCntrl: Control) updating;
// ---------------------------------------------------------------------------
// Created:     19 December 2000 by cnwrs2
//
// Changed On   Changed By  Reason
// ----------   ----------  --------------------------------------------------
//
// ---------------------------------------------------------------------------
begin
	inheritMethod(cntrl, persistCntrl);

	if self.isInPainter then
		self.borderStyle	:=	BorderStyle_Single;
	else	
		self.borderStyle	:=	BorderStyle_None;
		self.setupChildControls();
// assume that a resize will be done later - may need to set a flag for this in the painter?? - rjs
//		self.resizeChildrenAndSplitter(false);
	endif;

	self.transparent	:=	false;
	self.backColor		:=	self.getSystemColor(Color_3DFace);

/*
	if self.lockRight or self.stretchWidth then
 		self.distanceFromRight := persistCntrl.parent.width - persistCntrl.width - persistCntrl.left;
 	endif;

	if self.lockBottom or self.stretchHeight then
		self.distanceFromBottom := persistCntrl.parent.height - persistCntrl.height - persistCntrl.top;
	endif;
*/
end;</source>
        <Parameter name="cntrl" type="Control" usage="input"/>
        <Parameter name="persistCntrl" type="Control"/>
    </JadeMethod>
    <DevControlTypes name="CMUI_SplitContainer">
        <windowClass>AlBaseControl</windowClass>
        <DevControlProperties name="backBrush">
            <cntrlType>picture</cntrlType>
        </DevControlProperties>
        <DevControlProperties name="backBrushStyle">
            <cntrlType>list</cntrlType>
            <optionsList>
                <value>0 - tile</value>
                <value>1 - stretch</value>
                <value>2 - center</value>
                <value>3 - stretch proportional</value>
            </optionsList>
        </DevControlProperties>
        <DevControlProperties name="backColor">
            <cntrlType>color</cntrlType>
        </DevControlProperties>
        <DevControlProperties name="borderColorSingle">
            <cntrlType>color</cntrlType>
        </DevControlProperties>
        <DevControlProperties name="borderStyle">
            <cntrlType>list</cntrlType>
            <optionsList>
                <value>0 - None</value>
                <value>1 - Fixed single</value>
            </optionsList>
        </DevControlProperties>
        <DevControlProperties name="bubbleHelp">
            <cntrlType>multilined-string</cntrlType>
        </DevControlProperties>
        <DevControlProperties name="clipControls">
            <cntrlType>boolean</cntrlType>
        </DevControlProperties>
        <DevControlProperties name="description">
            <cntrlType>multilined-string</cntrlType>
        </DevControlProperties>
        <DevControlProperties name="distanceFromBottom">
            <cntrlType>signed-real</cntrlType>
        </DevControlProperties>
        <DevControlProperties name="distanceFromRight">
            <cntrlType>signed-real</cntrlType>
        </DevControlProperties>
        <DevControlProperties name="dragCursor">
            <cntrlType>picture</cntrlType>
        </DevControlProperties>
        <DevControlProperties name="enabled">
            <cntrlType>boolean</cntrlType>
        </DevControlProperties>
        <DevControlProperties name="focusBackColor">
            <cntrlType>color</cntrlType>
        </DevControlProperties>
        <DevControlProperties name="focusForeColor">
            <cntrlType>color</cntrlType>
        </DevControlProperties>
        <DevControlProperties name="fontBold">
            <cntrlType>boolean</cntrlType>
        </DevControlProperties>
        <DevControlProperties name="fontItalic">
            <cntrlType>boolean</cntrlType>
        </DevControlProperties>
        <DevControlProperties name="fontName">
            <cntrlType>font</cntrlType>
        </DevControlProperties>
        <DevControlProperties name="fontSize">
            <cntrlType>font-size</cntrlType>
        </DevControlProperties>
        <DevControlProperties name="fontStrikethru">
            <cntrlType>boolean</cntrlType>
        </DevControlProperties>
        <DevControlProperties name="fontUnderline">
            <cntrlType>boolean</cntrlType>
        </DevControlProperties>
        <DevControlProperties name="foreColor">
            <cntrlType>color</cntrlType>
        </DevControlProperties>
        <DevControlProperties name="height">
            <cntrlType>signed-integer</cntrlType>
        </DevControlProperties>
        <DevControlProperties name="helpContextId">
            <cntrlType>integer</cntrlType>
        </DevControlProperties>
        <DevControlProperties name="helpKeyword">
            <cntrlType>string</cntrlType>
        </DevControlProperties>
        <DevControlProperties name="ignoreSkin">
            <cntrlType>boolean</cntrlType>
        </DevControlProperties>
        <DevControlProperties name="isSplitHorizontal">
            <cntrlType>boolean</cntrlType>
        </DevControlProperties>
        <DevControlProperties name="left">
            <cntrlType>signed-integer</cntrlType>
        </DevControlProperties>
        <DevControlProperties name="leftToRightRatio">
            <cntrlType>signed-integer</cntrlType>
        </DevControlProperties>
        <DevControlProperties name="lockBottom">
            <cntrlType>boolean</cntrlType>
        </DevControlProperties>
        <DevControlProperties name="lockRight">
            <cntrlType>boolean</cntrlType>
        </DevControlProperties>
        <DevControlProperties name="mouseCursor">
            <cntrlType>picture</cntrlType>
        </DevControlProperties>
        <DevControlProperties name="mousePointer">
            <cntrlType>list</cntrlType>
            <optionsList>
                <value> 0 - Default</value>
                <value> 1 - Arrow</value>
                <value> 2 - Cross</value>
                <value> 3 - I-Beam</value>
                <value> 4 - Cursor</value>
                <value> 5 - Size</value>
                <value> 6 - Size NE SW</value>
                <value> 7 - Size N S</value>
                <value> 8 - Size NW SE</value>
                <value> 9 - Size W E</value>
                <value>10 - Up Arrow</value>
                <value>11 - HourGlass</value>
                <value>12 - No Drop</value>
            </optionsList>
        </DevControlProperties>
        <DevControlProperties name="name">
            <cntrlType>name</cntrlType>
        </DevControlProperties>
        <DevControlProperties name="parent">
            <cntrlType>parent</cntrlType>
        </DevControlProperties>
        <DevControlProperties name="parentAspect">
            <cntrlType>list</cntrlType>
            <optionsList>
                <value> 0 - None</value>
                <value> 1 - Stretch Right</value>
                <value> 2 - Stretch Bottom</value>
                <value> 3 - Stretch Both</value>
                <value> 4 - Anchor Right</value>
                <value> 6 - Anchor Right &amp; Stretch Bottom</value>
                <value> 8 - Anchor Bottom</value>
                <value> 9 - Anchor Bottom &amp; Stretch Right</value>
                <value>12 - Anchor Both</value>
            </optionsList>
        </DevControlProperties>
        <DevControlProperties name="parentBottomOffset">
            <cntrlType>signed-integer</cntrlType>
        </DevControlProperties>
        <DevControlProperties name="parentRightOffset">
            <cntrlType>signed-integer</cntrlType>
        </DevControlProperties>
        <DevControlProperties name="relativeHeight">
            <cntrlType>boolean</cntrlType>
        </DevControlProperties>
        <DevControlProperties name="relativeLeft">
            <cntrlType>boolean</cntrlType>
        </DevControlProperties>
        <DevControlProperties name="relativeTop">
            <cntrlType>boolean</cntrlType>
        </DevControlProperties>
        <DevControlProperties name="relativeWidth">
            <cntrlType>boolean</cntrlType>
        </DevControlProperties>
        <DevControlProperties name="scrollBars">
            <cntrlType>list</cntrlType>
            <optionsList>
                <value>0 - None</value>
                <value>1 - Horizontal</value>
                <value>2 - Vertical</value>
                <value>3 - Both</value>
            </optionsList>
        </DevControlProperties>
        <DevControlProperties name="securityLevelEnabled">
            <cntrlType>integer</cntrlType>
        </DevControlProperties>
        <DevControlProperties name="securityLevelVisible">
            <cntrlType>integer</cntrlType>
        </DevControlProperties>
        <DevControlProperties name="show3D">
            <cntrlType>list</cntrlType>
            <optionsList>
                <value>0 - use application default</value>
                <value>1 - Not 3D</value>
                <value>2 - Use 3D</value>
            </optionsList>
        </DevControlProperties>
        <DevControlProperties name="skinCategoryName">
            <cntrlType>string</cntrlType>
        </DevControlProperties>
        <DevControlProperties name="splitType">
            <cntrlType>list</cntrlType>
            <optionsList>
                <value>0 - Ratio</value>
                <value>1 - Lock Left</value>
                <value>2 - Lock Right</value>
            </optionsList>
        </DevControlProperties>
        <DevControlProperties name="stretchHeight">
            <cntrlType>boolean</cntrlType>
        </DevControlProperties>
        <DevControlProperties name="stretchWidth">
            <cntrlType>boolean</cntrlType>
        </DevControlProperties>
        <DevControlProperties name="tabIndex">
            <cntrlType>integer</cntrlType>
        </DevControlProperties>
        <DevControlProperties name="tabStop">
            <cntrlType>boolean</cntrlType>
        </DevControlProperties>
        <DevControlProperties name="tag">
            <cntrlType>string</cntrlType>
        </DevControlProperties>
        <DevControlProperties name="top">
            <cntrlType>signed-integer</cntrlType>
        </DevControlProperties>
        <DevControlProperties name="transparent">
            <cntrlType>boolean</cntrlType>
        </DevControlProperties>
        <DevControlProperties name="visible">
            <cntrlType>boolean</cntrlType>
        </DevControlProperties>
        <DevControlProperties name="width">
            <cntrlType>signed-integer</cntrlType>
        </DevControlProperties>
    </DevControlTypes>
</GUIClass>
