<GUIClass name="CM_AuditViewFormModal" id="d662d041-8170-4639-bffd-1c07a333fd14">
    <superclass name="CM_AuditViewForm"/>
    <transient/>
    <subclassTransientAllowed/>
    <transientAllowed/>
    <JadeMethod name="create" id="c2f8546c-e7c1-4911-b37c-e7b301b0e6de">
        <updating/>
        <source>create() updating;

/*

Purpose:			

Parameters:			

Return Value:		


Date		Who		Reason
09 January 2018	jlgndl1	Initial creation

*/

vars

begin
	
	self.permissionView := PmAdmAuditenquiryView;	
	self.permissionMaint := 0;

epilog

end;</source>
    </JadeMethod>
    <JadeMethod name="translateForm" id="50f16084-5add-4b37-b837-40288935bdac">
        <updating/>
        <source>translateForm() updating;
	// This method implements user-defined locale-based text translation for forms.
	// Created on 17 January 2018 10:21:27.
	//3/11/2019	jlgns1	TFS 3827 XLAT_FormCaptions need to be sorted out
vars
	locale : CM_Locale;
begin
	// Determine the locale for translations.
	locale := app.myCurrentLocale;
	if locale = null then
		locale := app.myRoot.getLocale(StandardTextLocale.padLeadingWith("0", 4), false);
		// if no locale the bail out at this point
		if locale = null then
			return;
		endif;
	endif;
	// The inheritMethod call invokes text translation for controls defined on superforms.
	inheritMethod;
	
	// The following code invokes text translation for controls defined on this form.
	self.translateCaption(Xlat_0022183, locale);
end;</source>
    </JadeMethod>
</GUIClass>
