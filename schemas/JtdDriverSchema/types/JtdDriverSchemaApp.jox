<Class name="JtdDriverSchemaApp" id="06d20f88-7e09-4e79-b836-887ac1d6faec">
    <superclass name="JtdCommonSchemaApp"/>
    <transient/>
    <sharedTransientAllowed/>
    <subclassSharedTransientAllowed/>
    <subclassTransientAllowed/>
    <transientAllowed/>
    <text>************************************************************************

P R O P R I E T A R Y   N O T I C E                    

This software, in whole and in every part, is proprietary to Jade
Software Corporation Limited at Christchurch, New Zealand and is not to
be reproduced, used or disclosed except upon prior written permission of
Jade Software Corporation Limited.

COPYRIGHT © JADE SOFTWARE CORPORATION LIMITED 2002 - ALL RIGHTS RESERVED

************************************************************************</text>
    <DbClassMap file="_usergui"/>
    <ImplicitInverseRef name="jtdMyContext" id="e40276fe-13fa-4b98-a638-6f23b979905f">
        <embedded/>
        <type name="JtdContext"/>
        <access>readonly</access>
    </ImplicitInverseRef>
    <ImplicitInverseRef name="jtdMyGenerationHelper" id="bd2c58f0-4ce3-4709-84ce-414dc67a1731">
        <embedded/>
        <type name="JtdGenerationHelper"/>
    </ImplicitInverseRef>
    <ImplicitInverseRef name="jtdMyTransactionGenerator" id="7176371d-99d0-4093-9094-d0953d383ef4">
        <embedded/>
        <type name="JtdTransactionGenerator"/>
    </ImplicitInverseRef>
    <ImplicitInverseRef name="msgLog" id="dc322522-7230-4475-80f8-491f804dbc44">
        <embedded/>
        <type name="JadeLog"/>
    </ImplicitInverseRef>
    <ImplicitInverseRef name="myLockTarget" id="e2af6600-14a7-4a5b-b54b-a4cc69ea7bed">
        <embedded/>
        <type name="JmtLockTarget"/>
    </ImplicitInverseRef>
    <ImplicitInverseRef name="myUser" id="c389779c-2a45-4456-9b5d-ae1027ddc0e6">
        <embedded/>
        <type name="CM_User"/>
    </ImplicitInverseRef>
    <PrimAttribute name="preventingDeadlocks" id="8e3b52f0-a2d4-49c2-856a-7b69d81a4894">
        <embedded/>
        <type name="Boolean"/>
    </PrimAttribute>
    <PrimAttribute name="processId" id="e009683c-3a43-4fd1-80de-095861cb5615">
        <embedded/>
        <type name="Integer"/>
    </PrimAttribute>
    <PrimAttribute name="profileName" id="db093319-a652-4ae9-af7c-d08231decf88">
        <length>51</length>
        <embedded/>
        <type name="String"/>
    </PrimAttribute>
    <PrimAttribute name="profileSequenceId" id="22f52013-8707-43a2-8413-8c3f6c66587b">
        <embedded/>
        <type name="Integer"/>
    </PrimAttribute>
    <PrimAttribute name="runningBenchmark" id="ee43f2df-5dd1-4425-941f-5d26e45a355e">
        <embedded/>
        <type name="Boolean"/>
    </PrimAttribute>
    <PrimAttribute name="sequenceId" id="5ddab715-6e5c-471c-9e94-739fa12cea92">
        <embedded/>
        <type name="Integer"/>
    </PrimAttribute>
    <PrimAttribute name="stopped" id="88f780bf-49e0-4c7b-aeb0-a99be00b791f">
        <embedded/>
        <type name="Boolean"/>
    </PrimAttribute>
    <JadeMethod name="atcgBtnClickInit" id="9ece586d-0fbd-4c61-a3ec-cacb73048ace">
        <source>atcgBtnClickInit(btnparams: JadeDynamicObject);

vars

begin
	inheritMethod(btnparams);
	terminate;
end;
</source>
        <Parameter name="btnparams" type="JadeDynamicObject"/>
    </JadeMethod>
    <JadeMethod name="atcgControlAppInit" id="70e9f475-2cc8-4b06-b03c-a50e78fd8652">
        <updating/>
        <source>atcgControlAppInit() updating;

vars
	f:Form;
begin
	atcgRunningAtcg:=true;
	create f as currentSchema.getClass("AtcgControlForm");
	f.show;
end;
</source>
    </JadeMethod>
    <JadeMethod name="atcgDynamicContent" id="56c813ba-022e-4eb5-9951-9ae0fc5a0bb6">
        <source>atcgDynamicContent(form:Form; control:Control; row,column:Integer; fromValidation:Boolean):String;
//	This method will be called for any dynamic text fields (Ctrl+Shift+left-click at recording time)
//	The fromValidation parameter will be true if called from a validation method, otherwise it will be false.
//	The row and column parameters will be zero for a non-table textbox.
vars

begin
	//if form.name = "MyForm" and control.name = "txtEnterPin" then
	//	return app.magicPin;
	//endif;
	return null;
end;
</source>
        <Parameter name="form" type="Form"/>
        <Parameter name="control" type="Control"/>
        <Parameter name="row" type="Integer"/>
        <Parameter name="column" type="Integer"/>
        <Parameter name="fromValidation" type="Boolean"/>
        <ReturnType type="String"/>
    </JadeMethod>
    <JadeMethod name="atcgGetControlOptions" id="12e45f70-306d-4c16-901f-4f1867d7df8c">
        <source>atcgGetControlOptions(genSchema,genSuperSchema,genSuperClass,logMessageMethod:String output; targetSchemas,noiseMethods:StringArray input);
//	This is an example of what your version of this method might look like.
begin
	// what schema to generate profile classes into, and its superschema
	genSchema:="JtdDriverSchema";
	genSuperSchema:="JtdCommonSchema";
	
	// what class to generate profile classes under
	genSuperClass:="JmtTransactionGenerator";
	
	// what logging method to use
	logMessageMethod:="logMessage";
	
	// list of schemas to be tracked during recording
	targetSchemas.add("JtdDriverSchema");
	targetSchemas.add("ShipSchema");
	targetSchemas.add("YardSchema");
	targetSchemas.add("GateSchema");
	targetSchemas.add("HmsSchema");
	targetSchemas.add("GeneralCargoSchema");
	targetSchemas.add("InvoiceSchema");
	targetSchemas.add("SysOpsSchema");
	targetSchemas.add("MobileServerSchema");

	// list of methods to ignore - these will not be tracked at all during recording, mainly to reduce unwanted verbosity in the rrmsg.log
	// do not list any control or form event methods here, as that may affect code generation
	noiseMethods.add("isSpecial");
	noiseMethods.add("getYesNo");
	noiseMethods.add("getDateShortFormat");
	noiseMethods.add("getIMEXStringFromIMEXStatus");
	noiseMethods.add("getErrorString");
	noiseMethods.add("zGetTranslatedText");
	noiseMethods.add("getYesBlank");
	noiseMethods.add("temperatureDisplay");
	noiseMethods.add("temperatureDisplayString");
	noiseMethods.add("temperatureConversion");
	noiseMethods.add("getPicLogoPort");
	noiseMethods.add("getPicLogoProduct");
	noiseMethods.add("convertRGBtoHSL");
	noiseMethods.add("excludeRow");
	noiseMethods.add("yaFillOptionToString");
	noiseMethods.add("convertRGBToColor");
	noiseMethods.add("bookingRefStatusEnumToString");
end;
</source>
        <Parameter name="genSchema" type="String" usage="output"/>
        <Parameter name="genSuperSchema" type="String" usage="output"/>
        <Parameter name="genSuperClass" type="String" usage="output"/>
        <Parameter name="logMessageMethod" type="String" usage="output"/>
        <Parameter name="targetSchemas" type="StringArray" usage="input"/>
        <Parameter name="noiseMethods" type="StringArray" usage="input"/>
    </JadeMethod>
    <JadeMethod name="atcgHandleShowModal" id="8f8f2f54-7176-49c1-bd61-139773f7a85b">
        <source>atcgHandleShowModal():Integer;

begin
	if jtdMyTransactionGenerator &lt;&gt; null and (atcgRunningAtcgReplay or runningBenchmark) then
		return jtdMyTransactionGenerator.handleShowModal;  // reimplement in profile class to perform modal actions and return the right code
	else
		return 99999;
	endif;
end;
</source>
        <ReturnType type="Integer"/>
    </JadeMethod>
    <JadeMethod name="atcgRecordAppInit" id="e4b3c929-c6d7-46c2-9a37-5919c942697f">
        <updating/>
        <source>atcgRecordAppInit() updating;
//	The class name below should be a non-abstract superclass of the generated profile classes.
//	It may be the same class declared for genSuperClass in your control options.
begin
	atcgRunningAtcg:=true;
	app.initialize;
	inheritMethod;
	create jtdMyTransactionGenerator as currentSchema.getClass("JmtTransactionGenerator");
	jtdMyTransactionGenerator.startup;
	system.causeEvent(1149, true, "RecordAppReady");
end;
</source>
    </JadeMethod>
    <JadeMethod name="atcgReplayAppInit" id="5e547cc9-c542-4ed6-baa5-686883a5a4ee">
        <updating/>
        <source>atcgReplayAppInit() updating;

vars
	proc:Process;
begin
	atcgRunningAtcg:=true;
	atcgRunningAtcgReplay:=true;
	inheritMethod;
	app.initialize;
	create jtdMyTransactionGenerator as currentSchema.getClass(global.atcgReplayClassName);
	jtdMyTransactionGenerator.startup;
	jtdMyTransactionGenerator.runTest;
	jtdMyTransactionGenerator.stop;
	
	terminate;
epilog
	foreach proc in Process.instances do
		if proc.persistentApp.name = "AtcgControlApp" then //proc &lt;&gt; process then
			proc.causeEvent(1148, true, null);
		endif;
	endforeach;
	delete jtdMyTransactionGenerator;
end;
</source>
    </JadeMethod>
    <JadeMethod name="beginRFIDTagNoteWindowsPC" id="cfe9a53a-d27f-4dfa-9243-4b629eebf0b5">
        <source>beginRFIDTagNoteWindowsPC(_event : Integer;
						_receiver : Object input; 
						_terminal : CM_Terminal):Boolean;
/*
----------   ----------  --------------------------------------------------
Created:	06 September 2006 By cnwth2

Purpose:	_readerContext passed in should be one of:
			
			Reader_Machine
			Reader_WindowsPC
			Reader_GateLane

Changed On   Changed By  Reason
	
----------   ----------  --------------------------------------------------
*/
vars
	tagReaderDict			: CM_RFIDTagReaderByCodeDict;
	tagReader				: CM_RFIDTagReader;
	machineName				: String;
	rtnValue				: Boolean;
begin
	if _terminal = null then
		app.raiseUnexpectedProgramError(class.name, method.name, 'Num Terminal passed in');
		return false;
	endif;
	//th2 replaced the machine name comparison with the user code
	machineName := app.myPassport.myUser.code&amp; "_" &amp; app.sequenceId.String;
	tagReaderDict := _terminal.allRFIDTagReadersByCode;
	//th2 - need a dict by context ? but will be a max of about a dozen readers, so no biggy
	foreach tagReader in tagReaderDict do		
		if tagReader.context &lt;&gt; TagReaderContext_WindowsPC then
			continue;
		endif;
		
		if tagReader.windowsMachineName.toUpper  &lt;&gt; machineName then
			continue;
		endif;
		
		rtnValue := true;
		_receiver.beginNotification(tagReader, _terminal.getTerminalEvent(_event), Response_Continuous, null);
	endforeach;
	return rtnValue;
end;


</source>
        <Parameter name="_event" type="Integer"/>
        <Parameter name="_receiver" type="Object" usage="input"/>
        <Parameter name="_terminal" type="CM_Terminal"/>
        <ReturnType type="Boolean"/>
    </JadeMethod>
    <JadeMethod name="controlLockAcquire" id="2507fb08-200b-4610-976f-199b2f8121f0">
        <updating/>
        <source>controlLockAcquire() updating;

vars
	count:Integer;
begin
	if myLockTarget = null then
		myLockTarget:=JmtLockTarget.firstInstance;
		if myLockTarget = null then
			beginTransaction;
			create myLockTarget;
			commitTransaction;
		endif;
		//preventingDeadlocks:=(getProfileStringAppServer(getIniFileNameAppServer,"JBL","PreventDeadlocks","true") &lt;&gt; "false");
	endif;
	
	//if not preventingDeadlocks then
		return;
	//endif;
	
	// single thread transactions to avoid deadlocks
	while not tryLock(myLockTarget, Reserve_Lock, Session_Duration, LockTimeout_Server_Defined) do
		count:=count+1;
		app.logMessage("unable to get control lock - retry #"&amp;count.String);
	endwhile;
end;
</source>
    </JadeMethod>
    <JadeMethod name="controlLockRelease" id="33c55396-5cdb-4bef-87dd-d0aa5084a16b">
        <updating/>
        <source>controlLockRelease() updating;

begin
	if myLockTarget &lt;&gt; null then
		unlock(myLockTarget);
	endif;
end;
</source>
    </JadeMethod>
    <JadeMethod name="doLogin" id="fbdab2ce-6b53-420a-a3ba-bfc6c2990819">
        <updating/>
        <source>doLogin(): Boolean updating;

vars
	login:CMS_Logon;
	userWindow			: CM_UserWindow;
begin
	create login;
	if myUser &lt;&gt; null then
		login.userObject := myUser;
		login.userID.text := myUser.code;
		beginTransaction;
		myUser.recentPasswords.purge();
		myUser.setPassword('password_1', false);
		myUser.updateNumberLogonAttempts(null);
		myUser.setPropertyValue('status', EnabledNormalUser);
		myUser.allUserWindows.purge();
		myUser.allUserWindowsForStartup.purge();
		myUser.setPropertyValue("isNotAvailableForEntry", false);			//27/08/2018	jlgmd1	If we haven't run ShipOperations yet, then our user might be disabled
		commitTransaction;
		login.password.text := 'password_1';
	else
		login.userObject := self;
	endif;
	login.bOk_click(login.bOk);         
	app.myPassport.setForLogon(myUser);
	beginTransaction;
	myUser.recentPasswords.purge;
	myUser.updateNumberLogonAttempts(null);
	myUser.setPropertyValue('status', EnabledNormalUser);
	
	myUser.allUserWindows.purge;
	myUser.allUserWindowsForStartup.purge;
	
	foreach userWindow in myUser.allUserWindowsForOpen do
		if userWindow.windowClassName &lt;&gt; 'CMUI_SuperAppForm2' then
			userWindow.erase(NORMAL_ERASE);
		endif;
	endforeach;
	
	commitTransaction;
	return true;
epilog
	delete login;
end;</source>
        <ReturnType type="Boolean"/>
    </JadeMethod>
    <JadeMethod name="doLogout" id="a29211a4-cf48-4144-93ab-8b6f701caffc">
        <updating/>
        <source>doLogout(_isLock : Boolean; _isLogoutLogin : Boolean) :String updating;
begin
	delete myPassport;
	delete myController;
	return null;
end;</source>
        <Parameter name="_isLock" type="Boolean"/>
        <Parameter name="_isLogoutLogin" type="Boolean"/>
        <ReturnType type="String"/>
    </JadeMethod>
    <JadeMethod name="getForm" id="a64d6329-aa1a-4ffd-b14a-f2e5fe1f2529">
        <executionLocation>client</executionLocation>
        <source>getForm(formName:String):Form clientExecution;

vars
	f:Form;
begin
	if	formName = "CMS_Progress" 
	or	formName = "CM_TIEnquiryOutput" 
	or	formName = "CM_GenericTablePrint" then
		create f as currentSchema.getClass(formName);
	
	elseif formName = "PrintingDialog"
	or	formName = "SOP_TerminalConfig" then
		create f as CM_BaseForm;
		
	else
		return inheritMethod(formName);
	endif;
	return f;
end;
</source>
        <Parameter name="formName" type="String"/>
        <ReturnType type="Form"/>
    </JadeMethod>
    <JadeMethod name="getIdString" id="0d9e7dde-2c4e-48a4-b66b-4bf8c36a2dc4">
        <source>getIdString():String;

begin
	return profileName&amp;"/"&amp;processId.String&amp;"/"&amp;sequenceId.String&amp;"/"&amp;profileSequenceId.String;
end;
</source>
        <ReturnType type="String"/>
    </JadeMethod>
    <JadeMethod name="getMyTerminal" id="6c845788-f7de-4d66-a6a9-366d4fe940a6">
        <source>getMyTerminal():CM_Terminal;

begin
	return app.myRoot.allTerminals['POE'];
end;
</source>
        <ReturnType type="CM_Terminal"/>
    </JadeMethod>
    <JadeMethod name="getMyTerminalConfig" id="bda7790f-5e46-4ce2-b724-8e7f3ab025fd">
        <source>getMyTerminalConfig(s:String):String;

begin
	return null;
end;
</source>
        <Parameter name="s" type="String"/>
        <ReturnType type="String"/>
    </JadeMethod>
    <JadeMethod name="getProcessProfileName" id="e001ae80-eb15-4142-9bfc-0cebb10b3493">
        <source>getProcessProfileName(proc:Process):String;

vars
	i,j:Integer;
begin
	i:=1;
	return (proc.userInfo&amp;"///@").scanUntil("/",i);
end;
</source>
        <Parameter name="proc" type="Process"/>
        <ReturnType type="String"/>
    </JadeMethod>
    <JadeMethod name="getUserCmdLine" id="3b927c09-150b-4e99-af5b-30c45cf2ced2">
        <updating/>
        <source>getUserCmdLine():String updating;

vars
	s,t:String;
	i,j:Integer;
begin
	if app.isUnicode then
		s:=(call jtdGetCommandLineW);
	else
		s:=(call jtdGetCommandLineA);
	endif;
//	logMessage("&lt;"&amp;s&amp;"&gt;");
	i:=s.toLower.pos(" endjade ",1);
	if i=0 then return null; endif;  // No endJade token
	return s[i+8:end].trimBlanks;
end;</source>
        <ReturnType type="String"/>
    </JadeMethod>
    <JadeMethod name="initialize" id="8928cfc1-3b7c-452f-865a-0a7a4204ff06">
        <updating/>
        <source>initialize() updating;

begin
	inheritMethod;
	//th2 as the app.myPassport.myTerminal reference needs to be set anyway, might be better using this:
	app.myPassport.setMyTerminal(app.myRoot.allTerminals['POE']);
end;
</source>
    </JadeMethod>
    <JadeMethod name="jtdCommonUserFini" id="5c9cde62-94ad-47b2-84bc-d6a25cb36037">
        <updating/>
        <source>jtdCommonUserFini() updating, protected;

begin
	// put application-specific finalize code in here
	app.logMessage(method.qualifiedName);
	finalize;
end;</source>
        <access>protected</access>
    </JadeMethod>
    <JadeMethod name="jtdCommonUserInit" id="d3441c49-cf27-426e-84a0-92c62d7a00b1">
        <updating/>
        <source>jtdCommonUserInit() updating, protected;

vars
	mthCount:Integer;
begin
	runningBenchmark:=true;
	// put application-specific initialization code in here
	if app.jtdMyContext &lt;&gt; null then
		app.jtdMyContext.jtdGetProfileNameAndMthCount(app.profileName, mthCount);
	endif;
	preventingDeadlocks:=true;
	setProcessUserInfo;  // set profileName so everyone can get seq id's
	app.controlLockAcquire;
	setSequenceId;
	setProcessUserInfo;  // set seq id
	app.controlLockRelease;
	preventingDeadlocks:=(getProfileStringAppServer(getIniFileNameAppServer,"JBL","PreventDeadlocks","true") &lt;&gt; "false");
	initialize;
	app.logMessage(method.qualifiedName);
end;</source>
        <access>protected</access>
    </JadeMethod>
    <JadeMethod name="jtdComputerName" id="3d43bb41-6384-4039-ba4a-e2963dc861af">
        <source>jtdComputerName() : String;

vars
	str		: String;
	pos		: Integer;
begin
	//  Strips off domain part of computer name (for Unix hosts)

	str		:= app.computerName;
	if str &lt;&gt; null then
		pos		:= 1;
		return str.scanUntil(".", pos);
	else
		return "NO_COMPUTERNAME";
	endif;
end;</source>
        <ReturnType type="String"/>
    </JadeMethod>
    <JadeMethod name="jtdComputerNameAppServer" id="3bb93ad8-602e-40ca-92f0-41274f4a6bd8">
        <source>jtdComputerNameAppServer() : String;

vars
	str		: String;
	pos		: Integer;
begin
	//  Strips off domain part of computer name (for Unix hosts)

	str		:= app.computerNameAppServer;
	pos		:= 1;
	return str.scanUntil(".", pos);
end;</source>
        <ReturnType type="String"/>
    </JadeMethod>
    <JadeMethod name="jtdFini" id="0ab28cb5-379d-4a4e-9db5-9159a48a9b38">
        <updating/>
        <source>jtdFini() updating, protected;

begin
	self.jtdCommonUserFini;

	if self.jtdMyContext &lt;&gt; null and global.isValidObject(self.jtdMyContext) then
		self.jtdMyContext.jtdAppFini;
		
		delete self.jtdMyContext;
	endif;

	self.jtdMyContext := null;
end;</source>
        <access>protected</access>
    </JadeMethod>
    <JadeMethod name="jtdFiniClone" id="d97e7109-ae77-4b96-9e70-0ec6e45c3688">
        <updating/>
        <source>jtdFiniClone() updating, protected;

begin
	self.jtdCommonUserFini;

	if self.jtdMyContext &lt;&gt; null and global.isValidObject(self.jtdMyContext) then
		self.jtdMyContext.jtdAppFiniClone;
		
		delete self.jtdMyContext;
	endif;

	self.jtdMyContext := null;
end;</source>
        <access>protected</access>
    </JadeMethod>
    <JadeMethod name="jtdFiniExternalClone" id="0fc9f38b-452a-45b5-aa1f-564e646e580f">
        <updating/>
        <source>jtdFiniExternalClone() updating, protected;

begin
	self.jtdCommonUserFini;

	if self.jtdMyContext &lt;&gt; null and global.isValidObject(self.jtdMyContext) then
		self.jtdMyContext.jtdAppFiniExternalClone;
		
		delete self.jtdMyContext;
	endif;

	self.jtdMyContext := null;
end;</source>
        <access>protected</access>
    </JadeMethod>
    <JadeMethod name="jtdGetApplicationType" id="2fde79cb-856a-4069-b9e8-fd6ced544651">
        <source>jtdGetApplicationType() : String;

vars

begin
	if applicationType = "G" then
		return "GUI";
	elseif applicationType = "F" then
		return "GUI_No_Forms";
	elseif applicationType = "S" then
		return "Non_GUI";
	elseif applicationType = "W" then
		return "Web_Enabled";
	elseif applicationType = "N" then
		return "Non_GUI_Web";
	else
		return applicationType;
	endif;
end;</source>
        <ReturnType type="String"/>
    </JadeMethod>
    <JadeMethod name="jtdGetClassAndOid" id="4dc010c8-2575-41e2-9e0e-cae9d6c87e70">
        <source>jtdGetClassAndOid(obj : Object) : String;

vars
 	classNo		: Integer;
	oidStr		: String;
	class		: Class;
	error		: Boolean;
	oid			: String;
begin
	if obj &lt;&gt; null then
		on Exception do jtdObjNotFoundExceptionHandler(exception, error);
		oidStr		:= obj.String;
		if not error then
			return oidStr;
		else
			class	:= getClassForObject(obj);			// refactored for 3.0 using 6.0.18 method
			oid		:= getOidStringForObject(obj);
			oidStr	:= class.name &amp; "/" &amp; oid;
			return oidStr;
		endif;
    	
	    if error then
	    	return "&lt;Unknown class, oid=" &amp; oid &amp; "&gt;";
	    endif;

	else
		return "&lt;null&gt;";
	endif;
end;</source>
        <Parameter name="obj" type="Object"/>
        <ReturnType type="String"/>
    </JadeMethod>
    <JadeMethod name="jtdInit" id="8451c176-2333-456a-b61d-b61807da6c39">
        <updating/>
        <source>jtdInit() updating, protected;

begin
	if self.jtdMyContext &lt;&gt; null and global.isValidObject(self.jtdMyContext) then
		delete self.jtdMyContext;
	endif;
	
	create self.jtdMyContext transient;
	
	self.jtdMyContext.jtdInitialize;
	
	self.jtdMyContext.jtdAppInit(null);
	
	self.jtdCommonUserInit;
end;</source>
        <access>protected</access>
    </JadeMethod>
    <JadeMethod name="jtdInitClone" id="885d9765-c290-4deb-8d81-68ce12247f9c">
        <updating/>
        <source>jtdInitClone(paramObj : Object) updating, protected;

begin
	if self.jtdMyContext &lt;&gt; null and global.isValidObject(self.jtdMyContext) then
		delete self.jtdMyContext;
	endif;
	
	create self.jtdMyContext transient;
	
	self.jtdMyContext.jtdInitialize;
	
	self.jtdMyContext.jtdAppInitClone(getOidStringForObject(paramObj));
	
	self.jtdCommonUserInit;
end;</source>
        <access>protected</access>
        <Parameter name="paramObj" type="Object"/>
    </JadeMethod>
    <JadeMethod name="jtdInitExternalClone" id="d0855b84-1a26-491a-be80-b4ccbd05b6c4">
        <updating/>
        <source>jtdInitExternalClone() updating, protected;

begin
	if self.jtdMyContext &lt;&gt; null and global.isValidObject(self.jtdMyContext) then
		delete self.jtdMyContext;
	endif;
	
	create self.jtdMyContext transient;
	
	self.jtdMyContext.jtdInitialize;
	
	self.jtdMyContext.jtdAppInitExternalClone(null);
	
	self.jtdCommonUserInit;
end;</source>
        <access>protected</access>
    </JadeMethod>
    <JadeMethod name="jtdObjNotFoundExceptionHandler" id="c1cf0afc-661d-48d0-8e17-193b08ab9efe">
        <source>jtdObjNotFoundExceptionHandler(exObj : Exception; error : Boolean output) : Integer protected;

vars

begin
	if exObj.errorCode = 4 		or
	   exObj.errorCode = 1046	or				// invalid class number
	   exObj.errorCode = 1072   or
	   exObj.errorCode = 1090 	or
	   exObj.errorCode = 1092	then
	   	error := true;
		return Ex_Resume_Next;
		
	else
		return Ex_Pass_Back;
	endif;
end;</source>
        <access>protected</access>
        <Parameter name="exObj" type="Exception"/>
        <Parameter name="error" type="Boolean" usage="output"/>
        <ReturnType type="Integer"/>
    </JadeMethod>
    <JadeMethod name="logMessage" id="f7250063-d94c-43b0-a17a-d48d84da6d14">
        <updating/>
        <source>logMessage(s:String) updating;
/*  Uses the JadeLog class to write a message to msg&lt;n&gt;.log  */
// 12 May 2016	cnwns1	TFS 488 - Multi Timezone Support
begin
	if msgLog=null then
		create msgLog;
		msgLog.fileName:="msg";
		msgLog.formatOutput:=false;
//		msgLog.maxFileSize:=1000;  // leave commented out to use value from ini file
	endif;
	msgLog.log(actualTimeServer.logFormat&amp;" "&amp;(getIdString&amp;" ").padBlanks(24)&amp;s&amp;CrLf);// 12 May 2016	cnwns1	TFS 488 - Multi Timezone Support
end;
</source>
        <Parameter name="s" type="String"/>
    </JadeMethod>
    <JadeMethod name="msgBox" id="ca6f4beb-0f08-4755-a5b6-3c2b7db103bf">
        <source>msgBox(msg:String; title:String; flags:Integer):Integer;
//	reimplemented to capture and action msgboxes
begin
	//process.profiler.start;
	//process.profiler.report;
	if atcgRunningAtcg or jtdMyTransactionGenerator.isKindOf(GateDock) /* when running the app from script */ then
		if msg.jtdContains("Are you sure you want to exit") and flags = MsgBox_Question_Mark_Icon + MsgBox_Yes_No + MsgBox_Default_Second and app.atcgCleanStartMode then
			return MsgBox_Return_Yes;
		else
			return inheritMethod(msg, title, flags);  // comment out this line to auto-close msgBoxes
		endif;
	endif;
	
	if msg.jtdContains("Wrong user code or password") then
		return MsgBox_Return_OK;  // don't log it
	endif;
	
	app.logMessage(method.qualifiedName&amp;' msg=&lt;'&amp;msg.jtdReplace(Cr,'\r').jtdReplace(Lf,'\n')&amp;'&gt; title=&lt;'&amp;title&amp;'&gt; flags=&lt;'&amp;flags.String&amp;'&gt;');

	if msg.jtdContains("This form will now be closed") then
		app.logMessage(method.qualifiedName&amp;" -------------------------------------");
		app.logMessage(method.qualifiedName&amp;" Error - This form will now be closed");
		app.logMessage(method.qualifiedName&amp;" -------------------------------------");
	endif;

	if msg.jtdContains("These will now be inspected") then  // this will cause a hang
		app.logMessage(method.qualifiedName&amp;" releasing control lock");
		app.controlLockRelease;
	endif;
	
	if  (flags = MsgBox_Question_Mark_Icon + MsgBox_Yes_No or flags = MsgBox_Question_Mark_Icon + MsgBox_Yes_No + MsgBox_Default_Second or flags = MsgBox_Yes_No)
	and (msg.jtdContains("Are you sure you want to exit")
	or   msg.jtdContains("Do you want to abandon your changes")
	or   msg.jtdContains("Resetting the OOW Creation resets all the")
	or   msg.jtdContains("Do you want to auto set the OOW Creation details")//OOW creation screen
	or   msg.jtdContains("Are you sure that you want to create the OOW for voyage")//OOW creation screen
	or   msg.jtdContains("Are you sure you want to delete  the selected item?") 
	or   msg.jtdContains("You are about to delete all of the sheets, do you wish to continue?")
	or  (msg.jtdContains("Instant Move tracked item(s)") and msg.jtdContains("to location") and msg.jtdContains("Do you wish to continue?"))
	or  (msg.jtdContains("Make EDI Bay Plan") and msg.jtdContains("Actual?"))) then
		app.logMessage(method.qualifiedName&amp;" clicking Yes button");
		return MsgBox_Return_Yes;

	elseif  (flags = MsgBox_Question_Mark_Icon + MsgBox_Yes_No or flags = MsgBox_Question_Mark_Icon + MsgBox_Yes_No + MsgBox_Default_Second or flags = MsgBox_Yes_No)
	and (msg.jtdContains("Do you want to reset all cranes")  //OOW creation screen
	or   msg.jtdContains("Check communication status with RefCon?")) then
		app.logMessage(method.qualifiedName&amp;" clicking No button");
		return MsgBox_Return_No;

	elseif (flags = MsgBox_Default_Second + MsgBox_Yes_No)
	and msg.jtdContains("You have not entered sufficient information to save this sheet.  Do you wish to delete it?") then
		app.logMessage(method.qualifiedName&amp;" clicking Yes button");
		return MsgBox_Return_Yes;
		
	elseif  (flags = MsgBox_OK_Only or flags = MsgBox_Stop_Icon or flags = MsgBox_Exclamation_Mark_Icon or flags = MsgBox_Information_Icon)
	and not msg.jtdContains("blerch") then
		app.logMessage(method.qualifiedName&amp;" clicking OK button");
		return MsgBox_Return_OK;

	else
		write method.qualifiedName&amp;" "&amp;msg&amp;" "&amp;title&amp;" "&amp;flags.String;
		app.logMessage(method.qualifiedName&amp;" "&amp;msg&amp;" "&amp;title&amp;" "&amp;flags.String);
		return inheritMethod(msg, title, flags);
	endif;/**/
end;</source>
        <Parameter name="msg" type="String"/>
        <Parameter name="title" type="String"/>
        <Parameter name="flags" type="Integer"/>
        <ReturnType type="Integer"/>
    </JadeMethod>
    <JadeMethod name="setProcessUserInfo" id="dbe80831-afb0-417f-b60f-20049c313c4a">
        <source>setProcessUserInfo();

begin
	beginTransaction;
	//process.userInfo := "Jtd - (" &amp; app.computerName.toLower &amp; ")";
	process.userInfo := getIdString&amp;"@"&amp;app.computerName.toLower;
	commitTransaction;
end;
</source>
    </JadeMethod>
    <JadeMethod name="setSequenceId" id="526affb6-2eef-4a1c-bb2a-c9ccd62e4590">
        <updating/>
        <source>setSequenceId() updating;
//	among all running apps of the same name, which one am I?
vars
	nod:Node;
	proc:Process;
	invalid:Boolean;
begin
	sequenceId:=0;
	profileSequenceId:=0;
	processId:=getInstanceIdForObject(process).Integer;
	
	on Exception do app.handleDeletedObject(exception, proc, invalid);
	
	foreach nod in system.nodes discreteLock do
		foreach proc in nod.processes discreteLock do
			if proc.persistentApp.name = process.persistentApp.name then
				if getInstanceIdForObject(proc) &lt; processId then
					sequenceId:=sequenceId+1;
					if getProcessProfileName(proc) = profileName then
						profileSequenceId:=profileSequenceId+1;
					endif;
				endif;
			endif;
		endforeach;
	endforeach;
end;</source>
    </JadeMethod>
    <JadeMethod name="showErrorWarningDialog" id="e7b0d424-483a-40cd-b1f0-ebc3c9225176">
        <source>showErrorWarningDialog(_heading : String; _passport : CM_Passport; 
					_mode : Integer) : Integer;
// ---------------------------------------------------------------------------
// Created:     30 January 2001 by cnwrs2
//
// Purpose:     
//
// Parameters:  Mode	= ErrWarnMode_Update
//						= ErrWarnMode_AddUpdate
//						= ErrWarnMode_Add + ErrWarnMode_CanAckWarnings
//
// Returns:     Nothing
//
// Changed On   Changed By  Reason
// ----------   ----------  --------------------------------------------------
//
// ---------------------------------------------------------------------------
vars
	form	: CMUI_ErrorWarningDialog;
	result	: Integer;
	err:CM_Error;
begin
	app.logMessage("Passport errors:");
	foreach err in app.myPassport.allErrors do
		app.logMessage(err.getDetailsAsString);
	endforeach;
	app.logMessage("Passport warnings:");
	foreach err in app.myPassport.allWarnings do
		app.logMessage(err.getDetailsAsString);
	endforeach;
	
	if runningBenchmark then
		//for JBL  - should never be any errors - only warnings
		if (not app.myPassport.allErrors.isEmpty) then
			app.msgBox(class.name &amp; ' ' &amp; method.name &amp; 'Errors were generated! These will now be inspected', 'Errors occurred', MsgBox_Exclamation_Mark_Icon);
			//app.myPassport.inspectModal;
			return IDCANCEL;
		endif;
		
		create form transient;
		form.setMyTerminal(_passport.myTerminal);
		form.init(_heading, _passport, _mode);
		
		form.show;
		
		form.btnOk.click(form.btnOk);
		return IDOK;
	else
		return inheritMethod(_heading, _passport, _mode);
	endif;
	
epilog
	delete form;
end;</source>
        <Parameter name="_heading" type="String"/>
        <Parameter name="_passport" type="CM_Passport"/>
        <Parameter name="_mode" type="Integer"/>
        <ReturnType type="Integer"/>
    </JadeMethod>
    <JadeMethod name="unloadAllForms" id="532b86f2-b695-41b7-ad8e-9415900c2fb4">
        <source>unloadAllForms();

vars
	idx:Integer;
	form:Form;
begin
	idx := app.formsCount;
	while idx &gt; 0 do
		form := app.forms(idx);
		if form &lt;&gt; null
		and (not form.isKindOf(CMUI_SuperAppForm2))
		and (not form.isKindOf(CMUI_Navigator))
		and (not form.isKindOf(CMS_UserMessageForm))
		and (not form.isKindOf(JtdDriverForm))
		and form &lt;&gt; self.myErrorWarningForm then
			// the error warning form is unloaded in the app.logout method.
			app.logMessage(method.qualifiedName&amp;" unloading surplus form '"&amp;form.caption&amp;"'("&amp;form.class.name&amp;")");
			form.unloadForm;
			delete form;
		endif;
		idx := idx - 1;
	endwhile;
end;</source>
    </JadeMethod>
</Class>
