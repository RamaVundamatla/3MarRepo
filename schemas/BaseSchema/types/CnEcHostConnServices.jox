<GUIClass name="CnEcHostConnServices" id="d01afd37-bc53-4353-9d4a-b97b91db08cb">
    <superclass name="Form"/>
    <transient/>
    <subclassTransientAllowed/>
    <transientAllowed/>
    <text>___________________________________________________________________________

              P R O P R I E T A R Y   N O T I C E

This software, in whole and in every part, is proprietary to Jade Software
Corporation Limited at Christchurch, New Zealand and is not to be
reproduced, used or disclosed except upon prior written permission of Jade
Software Corporation Limited.

  COPYRIGHT © JADE SOFTWARE CORPORATION LIMITED 2004 - ALL RIGHTS RESERVED
____________________________________________________________________________</text>
    <Constant name="Col_width_border" id="b8e3c6bb-5353-4eb2-a459-95ca943642c9">
        <source>2</source>
        <type name="Integer"/>
        <text>Host Connectivity Services, all tables : Width of Table border.

(Note this is provided explicity as the clientWidth
property does not always return the correct value).</text>
    </Constant>
    <Constant name="Col_width_scrollbar" id="8fff8f64-f57e-4bc5-9134-2971925187b1">
        <source>16</source>
        <type name="Integer"/>
        <text>Host Connectivity Services, all tables : Width of Scrollbar.

(Note this is provided explicity as the clientWidth
property does not always return the correct value).</text>
    </Constant>
    <Constant name="Tab_row_height_header" id="2002424f-73b2-4029-8ebd-ea2f56aa89ca">
        <source>24</source>
        <type name="Integer"/>
        <text>Host Connectivity Services : All Forms

Standard row height used for all tables for the
header row</text>
    </Constant>
    <Constant name="Tab_row_height_other" id="2f3ebd7b-754e-4b42-955e-689459fab177">
        <source>20</source>
        <type name="Integer"/>
        <text>Host Connectivity Services : All Forms

Standard row height used for all tables for those
rows containing content details</text>
    </Constant>
    <JadeMethod name="deleteExceptionHandler" id="5b703dd8-86fa-455c-b4dd-8ce470c97faa">
        <updating/>
        <source>deleteExceptionHandler(e:Exception):Integer updating;

begin
	//	if e.isKindOf(LockException) then
	if e.errorCode = 1027 then
		abortTransaction;
		app.msgBox(	'This instance is locked by another user' &amp; CrLf &amp;
					'so it can not be deleted now,  try again later.',
					self.name,
					MsgBox_OK_Only + MsgBox_Information_Icon);
		return Ex_Abort_Action;
	endif;

	// If someone else has already deleted our target instance, then thats OK!
	if e.errorCode = 4 			// Object not found
	and e.currentMethodDesc.ObjMethodCallDesc.getReceiver.isKindOf(CnEcLincIspecDefDict) then
		return Ex_Continue;
	endif;

	// Anything else, propage error throgh handlers
	return Ex_Pass_Back;
end;</source>
        <Parameter name="e" type="Exception"/>
        <ReturnType type="Integer"/>
    </JadeMethod>
    <JadeMethod name="readExceptionHandler" id="1a5c34f7-c74a-4186-be48-718452c4d654">
        <updating/>
        <source>/*******************************************************
*
* Description : Load the tabbed folder and populate the
*				various controls.
*
* Parameters :	None
*
* Return :		None
*
* History :		rmb	10/12/96	Initial version
*
*******************************************************/
readExceptionHandler(e:Exception):Integer updating;

begin
	//	if e.isKindOf(LockException) then
	if e.errorCode = 1027 then
		endLoad;
		app.msgBox(	'This instance is locked by another user' &amp; CrLf &amp;
					'so it can not be read now, try again later.',
					self.name,
					MsgBox_OK_Only + MsgBox_Information_Icon);
		return Ex_Abort_Action;
	endif;

	if e.errorCode = 4 then			// Object not found
		endLoad;
		abortTransaction;
		app.msgBox(	'This instance has now been deleted by another' &amp; CrLf &amp;
					'user, therefore it can not be displayed.',
					self.name,
					MsgBox_OK_Only + MsgBox_Information_Icon);
		return Ex_Abort_Action;
	endif;


	// Anything else, propage error throgh handlers
	return Ex_Pass_Back;
end;</source>
        <Parameter name="e" type="Exception"/>
        <ReturnType type="Integer"/>
    </JadeMethod>
    <JadeMethod name="saveExceptionHandler" id="34b46dd2-8f11-4b7c-ab48-8365a3617253">
        <updating/>
        <source>/*******************************************************
*
* Description :	Trap exceptions when saving database
*				objects (creating OR updating)
*
* Parameters :	e		Exception code
*
* Return :		Indicates following action :
*				continue | abort | propagate error
*
* Notes :		Modified from handler produced by Jade
*				Form Wizard
*
* History :		rmb	12/12/96	Initial version
*
*******************************************************/
saveExceptionHandler(e:Exception):Integer updating;
begin
	//	if e.isKindOf(LockException) then
	if e.errorCode = 1027 then			// Object locked
		abortTransaction;
		app.msgBox(	'This instance is locked by another user, try again later.',
					self.name,
					MsgBox_OK_Only + MsgBox_Information_Icon);
		return Ex_Abort_Action;
	endif;

	if e.errorCode = 1310 then		// Key already in dictionary
		abortTransaction;
		app.msgBox(	e.text(), 
					self.name, 
					MsgBox_OK_Only + MsgBox_Exclamation_Mark_Icon);
		return Ex_Abort_Action;
	endif;

	if e.errorCode = 4 then			// Object not found
		app.msgBox(	'This instance has now been deleted by another' &amp; CrLf &amp;
					'user, therefore it can not be updated.',
					self.name,
					MsgBox_OK_Only + MsgBox_Information_Icon);
		return Ex_Abort_Action;
	endif;

	// Anything else, propage error throgh handlers
	return Ex_Pass_Back;
end;</source>
        <Parameter name="e" type="Exception"/>
        <ReturnType type="Integer"/>
    </JadeMethod>
</GUIClass>
