<Class name="LOGAS400LoadSchema" id="69fb314d-bd83-4faa-934f-8270c5bafc52">
    <superclass name="LogSchema"/>
    <transient/>
    <sharedTransientAllowed/>
    <subclassSharedTransientAllowed/>
    <subclassTransientAllowed/>
    <transientAllowed/>
    <DbClassMap file="_usergui"/>
    <JadeMethod name="deleteWhenAble24" id="c18508c3-6455-44b9-9ec2-6f3c657ccc94">
        <source>deleteWhenAble24();
// ---------------------------------------------------------------------------
// Created:     21 September 2002 by cnwrs2
//
// Purpose:     
//
// Parameters:  None
//
// Returns:     Nothing
//
// Changed On   Changed By  Reason
// ----------   ----------  --------------------------------------------------
//
// ---------------------------------------------------------------------------
vars

begin

	// Do nothing as in the superschema this shows a message box.
	// We do NOT what this to show here

end;</source>
    </JadeMethod>
    <JadeMethod name="initAS400Load" id="da73c292-85a5-451e-802a-abc41846378a">
        <updating/>
        <source>initAS400Load() updating;
// ---------------------------------------------------------------------------
// Created:     21 September 2002 by cnwrs2
//
// Purpose:     
//
// Parameters:  None
//
// Returns:     Nothing
//
// Changed On   Changed By  Reason
// ----------   ----------  --------------------------------------------------
// 07 Feb 2012	cnwkpd1		PAR #28381 - Update JSM version info correctly in managed environments
// ---------------------------------------------------------------------------
vars

begin
	app.mousePointer := Window.MousePointer_HourGlass;
	app.niceName := "AS400 File Load";
	app.setupAppVersion();				// PAR #28381
	app.cnActivateKarmaControl(Cn_Fat_Client, null, null);
	
	self.initialize();
	app.handleIt();
	
	app.passBackAllExceptions := false;	//app.isDevelopmentSystem;
	app.passBackJadeExceptionsOnly := true;

	on Exception do app.generalExceptionHandler(exception) global;
	
	//self.logLevel := LogWARN;	//22 Nov 2019	NDL	TFS 47053 Part of 'initialize'
//	self.initLogFile();	//rs may 2005 CCMS10137

	if not app.doLogin() then
		terminate;
	endif;
	
	// Extra stuff here not in UiCommonSchema
	app.createMyController(CMUI_Controller);

	app.mainForm := app.loadForm(LOG_AS400FileLoadForm, LOG_Assistant, false, false, false, app.getMyTerminalDO_NOT_USE(), null).LOG_AS400FileLoadForm;
	
	// If user fails security we don't want to show a null form.
	if app.mainForm &lt;&gt; null then
		app.mainForm.show;
	endif;
	
epilog
	app.mousePointer := Window.MousePointer_Default;
	
end;</source>
    </JadeMethod>
</Class>
