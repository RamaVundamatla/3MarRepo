<Class name="JadeScript">
    <JadeMethod name="deletePFs" id="128c87d3-9201-4a66-99f6-25f3244befdf">
        <source>deletePFs();

vars

begin

WsdlProperty.instances.purge;
WsdlMethod.instances.purge;
end;
</source>
    </JadeMethod>
    <JadeMethod name="deleteProjects" id="11fa3ae9-5786-45cd-9489-c373a92396fc">
        <source>deleteProjects();

vars

begin

end;
</source>
    </JadeMethod>
    <JadeMethod name="test" id="4b77d185-f2b3-4501-b4a3-5ed3f397c15b">
        <source>test();

vars
	sa : JadeBytes;
	t,u,v,w : TimeStamp;
	i : Integer;
	outputFile : File;
	s : String;
begin
	create sa transient;
	t := app.getUTCTime;
	foreach i in 1 to 10 do
		sa.appendData(("bbbb" &amp; CrLf).Binary);
	endforeach;
	u := app.getUTCTime;
	write u-t;
	//write the file to disk
	//create outputFile;
	//outputFile.usePresentationFileSystem := true;
	//outputFile.fileName := "c:\temp.txt";
	//outputFile.mode		:= File.Mode_Output;
//	outputFile.writeString(s);
	//foreach s in sa do
	//	outputFile.writeLine(s);
	//endforeach;
	
	//outputFile.close;*/
	sa.extractToFile("c:\temp.txt", true);
	v := app.getUTCTime;
	write v-u;
epilog
	delete sa;
	delete outputFile;	
end;
</source>
    </JadeMethod>
</Class>
