<Class name="CMQF_TranshipsInTerminal" id="92df6d3d-f607-495c-acd0-abed92543b2b">
    <superclass name="CMQF_Tranship"/>
    <transient/>
    <persistentAllowed/>
    <sharedTransientAllowed/>
    <subclassPersistentAllowed/>
    <subclassSharedTransientAllowed/>
    <subclassTransientAllowed/>
    <transientAllowed/>
    <DbClassMap file="query"/>
    <JadeMethod name="filterObjects" id="9db7da62-3ada-4a59-8336-3d733b5beb71">
        <updating/>
        <source>filterObjects(_collection: Collection input; _passport: CM_Passport) updating;
/*
Purpose:			

Parameters:			

Return Value:		

Date		Who		Reason
18 June 2010	cnwdap1	Initial creation
*/
vars
	terminal	: CM_Terminal;
	ti			: CM_TrackedItem;
	breakOut	: Boolean;

begin
	//search export ti's for selected voyage
	foreach terminal in self.allTerminals do
		foreach ti in terminal.allOnsiteTIsByIdCargoType discreteLock do
			if self.checkStatusIncludesObject(ti, _passport, breakOut) then
				if not _collection.includes(ti) then
					_collection.add(ti);
					self.sumWeight := self.sumWeight + ti.totalWeight;
				endif;
			endif;
		endforeach;
	endforeach;
	
epilog

end;</source>
        <Parameter name="_collection" type="Collection" usage="input"/>
        <Parameter name="_passport" type="CM_Passport"/>
    </JadeMethod>
</Class>
