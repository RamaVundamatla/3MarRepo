<Class name="CMQF_CashTransactions" id="9c34f6d3-965a-4b92-89fe-5c6ee4665bf1">
    <superclass name="CMQF_FilterReports"/>
    <transient/>
    <persistentAllowed/>
    <sharedTransientAllowed/>
    <subclassPersistentAllowed/>
    <subclassSharedTransientAllowed/>
    <subclassTransientAllowed/>
    <transientAllowed/>
    <text>CCMS 2348 Financial Reporting, cash payments.</text>
    <DbClassMap file="query"/>
    <ImplicitInverseRef name="allCashTransactions" id="6a3d5868-f2dc-4052-b73a-700a8e05ed2c">
        <memberTypeInverse/>
        <type name="CM_CashDict"/>
        <access>readonly</access>
        <text>Query result, all cash transactions for the filter terminal, debtor and date criteria.</text>
    </ImplicitInverseRef>
    <ImplicitInverseRef name="allDebtors" id="ec37aef7-7e88-4218-ab2e-91996059e44d">
        <memberTypeInverse/>
        <type name="CM_DebtorSet"/>
        <access>readonly</access>
        <text>Filter criteria, selected debtors</text>
    </ImplicitInverseRef>
    <PrimAttribute name="billOfLading" id="d60c81dd-e07d-45fe-a190-f9dbb2d5efeb">
        <length>31</length>
        <embedded/>
        <type name="String"/>
        <access>readonly</access>
        <text>Filter criteria, bill of lading (use with voyage)</text>
    </PrimAttribute>
    <PrimAttribute name="cargoId" id="801f8305-55ec-416b-80e1-0f7809aaebf7">
        <length>21</length>
        <embedded/>
        <type name="String"/>
        <access>readonly</access>
        <text>Filter criteria, cargo id</text>
    </PrimAttribute>
    <PrimAttribute name="cashReceiptNumber" id="0940d65c-6d6d-46a1-bcba-52acd72eb0f5">
        <length>31</length>
        <embedded/>
        <type name="String"/>
        <access>readonly</access>
        <text>Filter criteria, cash receipt number</text>
    </PrimAttribute>
    <PrimAttribute name="customsEntryNumber" id="0a95c648-6eb2-4108-8666-569d9b866a97">
        <length>31</length>
        <embedded/>
        <type name="String"/>
        <access>readonly</access>
        <text>Filter criteria, customs entry number</text>
    </PrimAttribute>
    <ImplicitInverseRef name="myVoyage" id="b76c53bc-ccc8-4814-8fc6-0249890afe92">
        <embedded/>
        <type name="CM_Voyage"/>
        <access>protected</access>
    </ImplicitInverseRef>
    <PrimAttribute name="voyageCode" id="be1e841e-4257-4cf5-aca5-a23cdfb7f1d5">
        <length>21</length>
        <embedded/>
        <type name="String"/>
        <access>readonly</access>
    </PrimAttribute>
    <JadeMethod name="cloneReferences" id="641ba3e4-4325-4179-9ba4-e84fa52b2d19">
        <updating/>
        <source>cloneReferences(_obj: CMQF_CashTransactions; _transient: Boolean) updating;

vars

begin
	inheritMethod(_obj, _transient);

	self.allDebtors.clear();
	_obj.allDebtors.copy(self.allDebtors);
	self.setMyVoyage( _obj.getMyVoyage );
	
end;
</source>
        <Parameter name="_obj" type="CMQF_CashTransactions"/>
        <Parameter name="_transient" type="Boolean"/>
    </JadeMethod>
    <JadeMethod name="filterDebtorTerminal" id="98a72869-1010-4d6d-aab0-2ddc188e3ab5">
        <updating/>
        <source>filterDebtorTerminal(_drTerm : CM_DebtorTerminal; _collection : Collection input; _passport : CM_Passport; _breakOut : Boolean output) updating, protected;

/*

Purpose:			

Parameters:			

Return Value:		


Date		Who		Reason
19/05/2014	cnwjre1	34142 CCMS 2348 financial reporting remove opening, closing balance, refunds and journals as pre-payments are no longer used.
31/10/2014	cnwjre1	35124 limit to just cash payments, receipts are CMQF_CashReceipt
*/

vars

	doFromDateCheck : Boolean;
	doToDateCheck : Boolean;
	breakOut	: Boolean;
	startTS		: TimeStamp;
	
	iter : Iterator;
	
	cash : CM_Cash;
	payment : CM_CashPayment;

begin

	if _drTerm = null then
		return;
	endif;
	
	doFromDateCheck := self.startDate.isValid;
	if doFromDateCheck then
		startTS.setDate(self.startDate);
		startTS.setTime("00:00:00".Time);
	endif;
	doToDateCheck := self.endDate.isValid;
	
	iter := _drTerm.allConsolidatedCashTrx.createIterator;
	while iter.next( cash ) do
		if doFromDateCheck and cash.dateTime.Date &lt; self.startDate then
		
		elseif doToDateCheck and cash.dateTime.Date &gt; self.endDate then
			break;
		else
			if cash.isProformaPayment or
			   cash.isPayment then
				if self.checkStatusIncludesObject(cash, _passport, breakOut) then
					if not _collection.includes(cash) then
						_collection.add(cash);
					endif;
				endif;
				if breakOut then
					break;
				endif;
			endif;
		endif;
	endwhile;
	delete iter;

	iter := _drTerm.allCashPayments.createIterator;
	while iter.next( payment ) do
		if doFromDateCheck and payment.dateTime.Date &lt; self.startDate then
		
		elseif doToDateCheck and payment.dateTime.Date &gt; self.endDate then
			break;
		else
			if self.checkStatusIncludesObject(payment, _passport, breakOut) then
				if not _collection.includes(payment) then
					_collection.add(payment);
				endif;
			endif;
			if breakOut then
				break;
			endif;
		endif;
	endwhile;
	delete iter;
	
	_breakOut := breakOut;

epilog

	delete iter;

end;</source>
        <access>protected</access>
        <Parameter name="_drTerm" type="CM_DebtorTerminal"/>
        <Parameter name="_collection" type="Collection" usage="input"/>
        <Parameter name="_passport" type="CM_Passport"/>
        <Parameter name="_breakOut" type="Boolean" usage="output"/>
    </JadeMethod>
    <JadeMethod name="filterObjects" id="4ea5d59c-a3df-42b4-85e1-eb857023d61a">
        <updating/>
        <source>filterObjects(_collection: Collection input; _passport: CM_Passport) updating;

/*

Purpose:			

Parameters:			

Return Value:		


Date		Who		Reason
14 August 2012	cnwndl1	Initial creation	par 29968
20/05/2014	cnwjre1	Revised for financial reporting CCMS 2348, return cash transactions
*/

vars

	iter : Iterator;
	drTerm : CM_DebtorTerminal;
	breakOut : Boolean;
	debtor : CM_Debtor;
	term : CM_Terminal;

begin
	
	// 34442 financial selector reports
	if (not self.allQueryCriteriaHolders.isEmpty) then
		self.filterSelectorQuery(_collection, _passport);
		return;
	endif;

	if self.allTerminals.isEmpty and self.allDebtors.isEmpty then
		// no selection, the user wants them all 
		iter := CM_DebtorTerminal.instances.createIterator;
		while iter.next( drTerm ) do
			self.filterDebtorTerminal(drTerm, _collection, _passport, breakOut);
			if breakOut then
				break;
			endif;
		endwhile;
	
	elseif self.allTerminals.isEmpty then
		// debtor selection only, the user wants all terminals for these debtors 
		foreach debtor in self.allDebtors discreteLock do
			iter := debtor.allDebtorTerminals.createIterator;
			while iter.next( drTerm ) do
			self.filterDebtorTerminal(drTerm, _collection, _passport, breakOut);
				if breakOut then
					break;
				endif;
			endwhile;
			delete iter;
		endforeach;
	
	elseif self.allDebtors.isEmpty then
		// terminal selection only, the user wants all debtors for these terminals 
		foreach term in self.allTerminals discreteLock do
			iter := term.allDebtorTerminals.createIterator;
			while iter.next( drTerm ) do
				self.filterDebtorTerminal(drTerm, _collection, _passport, breakOut);
				if breakOut then
					break;
				endif;
			endwhile;
			delete iter;
		endforeach;
	
	else
		// debtor and terminal selection, the user only wants specific debtor / terminal combinations.
		foreach debtor in self.allDebtors discreteLock do
			iter := debtor.allDebtorTerminals.createIterator;
			while iter.next( drTerm ) do
				if not self.allTerminals.includes( drTerm.myTerminal ) then
					continue;
				endif;
				self.filterDebtorTerminal(drTerm, _collection, _passport, breakOut);
				if breakOut then
					break;
				endif;
			endwhile;
			delete iter;
		endforeach;
		
	endif;

epilog

	delete iter;

end;</source>
        <Parameter name="_collection" type="Collection" usage="input"/>
        <Parameter name="_passport" type="CM_Passport"/>
    </JadeMethod>
    <JadeMethod name="filterSelectorQuery" id="29166f79-2b7d-41e1-bc44-84935e4db187">
        <source>filterSelectorQuery(_collection  : Collection input;
					_passport : CM_Passport) protected;
//	34442 Oct 2014 for cash payment selector reports
vars
	criteriaHolder 		: CM_QueryCriteriaLineHolder;
	qDef				: CM_QueryDefinition;
	enqController		: CM_EnquiryController;
	queryAgent			: CM_QueryAgentFilter;
	obj : Any;
	cp : CM_CashPayment;
begin

	// Special one because we need the copy
	on CM_BreakOutException do app.myQAFilter.xHandlerBreakOut(exception);
	
	queryAgent := app.myQAFilter;
	if queryAgent = null then
		return;
	endif;
	
	qDef := app.myRoot.allQueryDefinitionObjects[CM_QueryDefinition.Cash_Query_Type, self.selectorMode];
	queryAgent.createEnqControllerForMode(self.selectorMode, CM_QueryDefinition.Cash_Query_Type, enqController);
	delete enqController.allObjects;
	create enqController.allObjects as _collection.class transient;
	
	foreach criteriaHolder in self.allQueryCriteriaHolders discreteLock do
		enqController.myCriteriaLineHolder := criteriaHolder;
		enqController.doQuery(qDef);
	endforeach;

	// TFS 37454 enqController.allObjects.copy(_collection);
	// TFS 37454 Do not include cash tax payments
	foreach obj in enqController.allObjects do
		if obj.isKindOf(CM_CashPayment) then
			cp := obj.CM_CashPayment;
			if cp.myCashPaymentForInvoice &lt;&gt; null then
				continue;
			endif;
		endif;
		_collection.add(obj);
	endforeach;
	
epilog
	if enqController &lt;&gt; null then
		delete enqController.allObjects;
	endif;
	delete enqController;
end;</source>
        <access>protected</access>
        <Parameter name="_collection" type="Collection" usage="input"/>
        <Parameter name="_passport" type="CM_Passport"/>
    </JadeMethod>
    <JadeMethod name="filterTransaction" id="cda9d8ed-5376-403f-8a1e-c5f81c4fa58a">
        <source>filterTransaction(_cash : CM_Cash; _collection : Collection input) protected;
// CCMS 2348 financial reporting, filter the cash transaction
vars

begin
	// data attribute filtering
	if _cash.matches(self.voyageCode, self.billOfLading, self.customsEntryNumber, self.cashReceiptNumber, self.cargoId) then
		_collection.add(_cash);
	endif;
end;
</source>
        <access>protected</access>
        <Parameter name="_cash" type="CM_Cash"/>
        <Parameter name="_collection" type="Collection" usage="input"/>
    </JadeMethod>
    <JadeMethod name="getMyVoyage" id="ddbc0710-153a-471d-a7fe-253ddb0ff004">
        <source>getMyVoyage():CM_Voyage;
/*

Purpose:			

Parameters:			

Return Value:		

Date		Who		Reason
14 July 2016	cnwndl1	Initial creation	TFS 2329

*/

vars

	voyage : CM_Voyage;
	edition : Integer;
	invalidObj : Boolean;

begin

	voyage := self.myVoyage;

	if voyage = null then
		return null;
	endif;
	
	on Exception do app.handleDeletedObject( exception, voyage, invalidObj );
	edition := voyage.edition;
	
	if invalidObj then
		return null;
	endif;
	
	return voyage;

epilog

end;</source>
        <ReturnType type="CM_Voyage"/>
    </JadeMethod>
    <JadeMethod name="getOIDAndDescForAttribute" id="f4a3958c-d4da-4240-b1f6-7a2cfce3975a">
        <source>getOIDAndDescForAttribute(_attrName: String; _getCalculatedDate: Boolean; _oid: String output): String;

/*

Purpose:			

Parameters:			

Return Value:		


Date		Who		Reason
14 August 2012	cnwndl1	Initial creation

*/

vars

	runDate		: Date;

begin
	runDate := app.actualTimeServer().Date;	// 28/4/16 cnwmp6 TFS 488 - Multi timezone support
	
	if _attrName = ETRow_FromDate then
	   	if _getCalculatedDate then
			return self.setDateFromTemplate(self.startDateTplType, self.startDateTplFactor, runDate).shortFormatJMT();
		else
			return self.getTemplateDateStr(self.startDateTplType, self.startDateTplFactor);
		endif;
		
	elseif _attrName = ETRow_ToDate then
		if _getCalculatedDate then
			return self.setDateFromTemplate(self.endDateTplType, self.endDateTplFactor, runDate).shortFormatJMT();
		else
			return self.getTemplateDateStr(self.endDateTplType, self.endDateTplFactor);
		endif;
		
	elseif _attrName = ETRow_BillOfLading then
		return self.billOfLading;
	elseif _attrName = ETRow_CargoId then
		return self.cargoId;
	elseif _attrName = ETRow_Number then
		return self.cashReceiptNumber;
	elseif _attrName = ETRow_CustomsEntryNumber then
		return self.customsEntryNumber;
	elseif _attrName = ETRow_Voyage then
		if self.getMyVoyage &lt;&gt; null then
			_oid := self.getMyVoyage.getOidAsString(); 
			return self.getMyVoyage.code;
		endif;
	else
		return inheritMethod(_attrName, _getCalculatedDate, _oid);
	endif;

epilog

end;</source>
        <Parameter name="_attrName" type="String"/>
        <Parameter name="_getCalculatedDate" type="Boolean"/>
        <Parameter name="_oid" type="String" usage="output"/>
        <ReturnType type="String"/>
    </JadeMethod>
    <JadeMethod name="includesObject" id="32ceb0d6-7a28-45b2-90f2-2f7dbed98966">
        <updating/>
        <source>includesObject(_cashTransaction : CM_Cash; _passport: CM_Passport; _breakOut: Boolean output): Boolean updating, protected;

vars
	querySelf : CMQF_CashTransactions;	

begin

	querySelf := self;
	querySelf.numSearched := querySelf.numSearched + 1;
	
	// data attribute filtering
	if _cashTransaction.matches(self.voyageCode, self.billOfLading, self.customsEntryNumber, self.cashReceiptNumber, self.cargoId) then
		querySelf.numFound := querySelf.numFound + 1;
		return true;
	endif;

	return false;
epilog

end;</source>
        <access>protected</access>
        <Parameter name="_cashTransaction" type="CM_Cash"/>
        <Parameter name="_passport" type="CM_Passport"/>
        <Parameter name="_breakOut" type="Boolean" usage="output"/>
        <ReturnType type="Boolean"/>
    </JadeMethod>
    <JadeMethod name="setFromTemplate" id="0b381016-73b8-482f-b0eb-453475897f24">
        <updating/>
        <source>setFromTemplate(_obj: CMQF_FilterReports; _runDate: Date; _allEventData: CM_EventDataByTypeDict) updating;
/*

Date		Who		Reason
10 September 2014	cnwer1	Initial creation PAR 34813

*/

vars
	eventData : CM_EventDataVoyage;

begin
	inheritMethod(_obj, _runDate, _allEventData);
	
	// This code sets the voyage for event driven schedules.
	// Have to do this for each appropriate class because they have different myVoyage references.
	eventData := _allEventData[EventDataVoyage].CM_EventDataVoyage;
	if eventData &lt;&gt; null and eventData.myVoyage &lt;&gt; null then
		self.setMyVoyage( eventData.myVoyage );
	endif;

epilog

end;</source>
        <Parameter name="_obj" type="CMQF_FilterReports"/>
        <Parameter name="_runDate" type="Date"/>
        <Parameter name="_allEventData" type="CM_EventDataByTypeDict"/>
    </JadeMethod>
    <JadeMethod name="setMyVoyage" id="6db62fd8-e322-44e4-ab56-e6b757da3f9c">
        <updating/>
        <source>setMyVoyage( _voyage : CM_Voyage ) updating;
/*

Purpose:			

Parameters:			

Return Value:		

Date		Who		Reason
14 July 2016	cnwndl1	Initial creation	TFS 2329

*/

vars

begin

	self.myVoyage := _voyage;

epilog

end;</source>
        <Parameter name="_voyage" type="CM_Voyage"/>
    </JadeMethod>
</Class>
