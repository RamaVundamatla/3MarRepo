<CollClass name="CM_ContentsStatusByCodeDict" id="89768871-e034-4fe9-98e9-9f0cf1ca8e51">
    <memberType name="CM_ContentsStatus"/>
    <superclass name="CM_MemberKeyDictionary"/>
    <persistentAllowed/>
    <sharedTransientAllowed/>
    <subclassPersistentAllowed/>
    <subclassSharedTransientAllowed/>
    <subclassTransientAllowed/>
    <transientAllowed/>
    <DbClassMap file="terminal"/>
    <MemberKey>
        <property name="CM_CargoStatus::code"/>
    </MemberKey>
    <JadeMethod name="getDisplayCodes" id="25a647d0-c190-4f76-8321-ba69dcfeb511">
        <source>getDisplayCodes(_short : Boolean) : String;
// ---------------------------------------------------------------------------
// Created:		13 November 2006 By Andrew Bishop cnwab1
//
// Purpose:		
//
// ---------------------------------------------------------------------------
/*

Date		Who		Reason
19 May 2010	cnwkpd1	PAR #20728 - remove isValidObject calls for performance

*/

vars
	aCT : CM_ContentsStatus;
	line : String;
	invalid : Boolean;

begin
	on Exception do app.handleDeletedObject(exception, aCT, invalid);

	foreach aCT in self discreteLock do
//		if not global.isValidObject(aCT) then
//			continue;
//		endif;
		if line = null then
			line := aCT.abbreviation;
		else
			line := line &amp; ", " &amp; aCT.abbreviation;
		endif;
	endforeach;
	
	return line;
end;</source>
        <Parameter name="_short" type="Boolean"/>
        <ReturnType type="String"/>
    </JadeMethod>
</CollClass>
