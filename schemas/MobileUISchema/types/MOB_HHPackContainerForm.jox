<GUIClass name="MOB_HHPackContainerForm" id="b3e4f99b-c3b1-4028-9452-a2ed358df724">
    <superclass name="MOB_HandHeldForm"/>
    <transient/>
    <subclassTransientAllowed/>
    <transientAllowed/>
    <PrimAttribute name="allowPackAll" id="b91ee3f9-6d71-4af3-9235-76c32fff75a3">
        <embedded/>
        <type name="Boolean"/>
        <access>readonly</access>
    </PrimAttribute>
    <PrimAttribute name="applyTwelveStwRules" id="870d56f4-5654-44e3-88f3-00eab99918c2">
        <embedded/>
        <type name="Boolean"/>
    </PrimAttribute>
    <ImplicitInverseRef name="btnDocActions" id="cd595e90-7069-4151-ad27-fc1e49ee7653">
        <embedded/>
        <type name="Button"/>
    </ImplicitInverseRef>
    <ImplicitInverseRef name="btnInYardTemp" id="85537e65-9ecc-4c9d-88dd-268a9af2cdbf">
        <embedded/>
        <type name="MOB_Button"/>
    </ImplicitInverseRef>
    <ImplicitInverseRef name="btnPackedTemp" id="e89cc9e0-2027-4736-bf0c-33f28afcfb14">
        <embedded/>
        <type name="MOB_Button"/>
    </ImplicitInverseRef>
    <ImplicitInverseRef name="docActsToDisplay" id="ecd2562d-6103-4b1c-9ae2-4b7f7def799d">
        <memberTypeInverse/>
        <type name="ObjectArray"/>
        <access>readonly</access>
    </ImplicitInverseRef>
    <ImplicitInverseRef name="fldCargo" id="65d33c9c-5bb7-4fd7-831f-012accb79b76">
        <embedded/>
        <type name="MOB_Folder"/>
    </ImplicitInverseRef>
    <ImplicitInverseRef name="frmPacked" id="34681277-bdb6-4b0f-af8a-12fb0456cbda">
        <embedded/>
        <type name="MOB_Frame"/>
    </ImplicitInverseRef>
    <ImplicitInverseRef name="frmToPack" id="81656e11-4e0e-4219-8c86-b40f23affef0">
        <embedded/>
        <type name="MOB_Frame"/>
    </ImplicitInverseRef>
    <ImplicitInverseRef name="lblCargoId" id="f4fab411-db50-494b-abba-7d293d1c695d">
        <embedded/>
        <type name="Label"/>
    </ImplicitInverseRef>
    <ImplicitInverseRef name="lblCount" id="b1cf9751-6b61-4848-9367-045adbaad9b6">
        <embedded/>
        <type name="Label"/>
    </ImplicitInverseRef>
    <ImplicitInverseRef name="lblWeight" id="e87d556d-7c82-4998-8ba0-444707e1d77c">
        <embedded/>
        <type name="Label"/>
    </ImplicitInverseRef>
    <ImplicitInverseRef name="myHandHeldCheckerAgent" id="2b3cafc2-c33a-42c9-bf04-f0523a452f51">
        <embedded/>
        <type name="MOB_HandHeldCheckerAgent"/>
        <access>readonly</access>
    </ImplicitInverseRef>
    <ImplicitInverseRef name="myPackContainer" id="6c0ed768-667c-48f3-9aad-694da8393a94">
        <embedded/>
        <type name="CM_TrackedItem"/>
    </ImplicitInverseRef>
    <PrimAttribute name="packedSize" id="bc813a27-4c96-435a-b98a-c11d32c6bcbf">
        <embedded/>
        <type name="Integer"/>
        <access>readonly</access>
    </PrimAttribute>
    <ImplicitInverseRef name="scrPacked" id="1937bace-c2c1-4ab7-9ad9-948f38397b4e">
        <embedded/>
        <type name="VScroll"/>
    </ImplicitInverseRef>
    <ImplicitInverseRef name="scrToPack" id="9d293214-a17e-4e03-b30d-907e2166bc06">
        <embedded/>
        <type name="VScroll"/>
    </ImplicitInverseRef>
    <ImplicitInverseRef name="shtPacked" id="a7016b52-72c0-4423-b0ee-ecde94d6a82f">
        <embedded/>
        <type name="MOB_Sheet"/>
    </ImplicitInverseRef>
    <ImplicitInverseRef name="shtToPack" id="c2014ccf-4de9-425e-a9e6-d785de9fd82f">
        <embedded/>
        <type name="MOB_Sheet"/>
    </ImplicitInverseRef>
    <PrimAttribute name="stwRuleNum" id="3503ca3c-737d-4f3f-b238-26948bf8524a">
        <embedded/>
        <type name="Integer"/>
    </PrimAttribute>
    <PrimAttribute name="toPackSize" id="8382fe85-fc65-48b3-9b89-6093e6755716">
        <embedded/>
        <type name="Integer"/>
        <access>readonly</access>
    </PrimAttribute>
    <ImplicitInverseRef name="txtCargoId" id="0e0265c6-f499-44aa-b62a-95893981637f">
        <embedded/>
        <type name="MOB_TextBox"/>
    </ImplicitInverseRef>
    <JadeMethod name="btnDocActions_click" id="1c509ee0-f9be-40bd-b94f-ca4cee611b9b">
        <controlMethod name="Button::click"/>
        <methodInvocation>event</methodInvocation>
        <updating/>
        <source>btnDocActions_click(btn: Button input) updating;
/*

Purpose:			

Parameters:			

Return Value:		

Date		Who		Reason
28 February 2017	cnwndl1	Initial creation

*/

vars

	obj : Object;
	docAction : CM_DocumentActions;
	
	reserveDetail : CM_ReserveDetail;
	resDetailSet : CM_ReserveDetailSet;
	bookingItem : CM_BookingRefItem;
	bookingItemSet : CM_BookingRefItemSet;

begin

	create resDetailSet transient;
	create bookingItemSet transient;
	
	foreach obj in self.docActsToDisplay do
		docAction := obj.CM_DocumentActions;
		if docAction.isBookingItemAction then
			bookingItem := docAction.CM_BookingItemActions.myBookingItem;
			bookingItemSet.add( bookingItem );
		elseif docAction.isReserveDetailAction then
			reserveDetail := docAction.CM_ReserveDetailActions.myReserveDetail;
			resDetailSet.add( reserveDetail );
		endif;
	endforeach;
	
	self.myMobileGenericAgent.setAllReserveDetails( resDetailSet );
	self.myMobileGenericAgent.setAllBookingRefItems( bookingItemSet );
	self.myMobileGenericAgent.setStringField1( self.fldCargo.topSheet.name );
	
	self.gotoForm( MOB_HHDocumentActions, false );

epilog

	delete resDetailSet;
	delete bookingItemSet;

end;</source>
        <Parameter name="btn" type="Button" usage="input"/>
    </JadeMethod>
    <JadeMethod name="btnInYardTemp_click" id="a1a2bc0c-5cd1-4d9b-9ac4-85ffd353012e">
        <controlMethod name="Button::click"/>
        <methodInvocation>event</methodInvocation>
        <updating/>
        <source>btnInYardTemp_click(btn: Button input) updating;

/*

Purpose:			

Parameters:			

Return Value:		


Date		Who		Reason
04 July 2011	cnwndl1	Initial creation	Par 26476

*/

vars

begin

	self.clickInYardItem( btn.userObject );

epilog

end;</source>
        <Parameter name="btn" type="Button" usage="input"/>
    </JadeMethod>
    <JadeMethod name="btnPackedTemp_click" id="954e0267-b373-4ff1-81d7-0569b566581c">
        <controlMethod name="Button::click"/>
        <methodInvocation>event</methodInvocation>
        <updating/>
        <source>btnPackedTemp_click(btn: Button input) updating;

/*

Purpose:			

Parameters:			

Return Value:		


Date		Who		Reason
05 July 2011	cnwndl1	Initial creation

*/

vars

begin

	doUnPack( btn.userObject );

epilog

end;</source>
        <Parameter name="btn" type="Button" usage="input"/>
    </JadeMethod>
    <JadeMethod name="clickInYardItem" id="c830e704-2337-4b95-9226-6c9c4848ded4">
        <updating/>
        <source>clickInYardItem( obj : Object ) updating;

/*

Purpose:			

Parameters:			

Return Value:		


Date		Who		Reason
04 July 2011	cnwndl1	Initial creation	Par 26476

*/

vars

	ti : CM_TrackedItem;
	bulkRelease : CM_BulkRelease;
	reserveDetail : CM_ReserveDetail;
	reason : String;
//	formClassSet : ClassSet;
	
//	hasActions : Boolean;
//	resDetailSet : CM_ReserveDetailSet;
//	bookingItemSet : CM_BookingRefItemSet;
//	opt1Str : String;
//	opt2Str : String;
//	opt3Str : String;
//	opt4Str : String;
//	returnValue : Integer;
//	doActions : Boolean;
//	doPack : Boolean;
//	doChecks : Boolean;

begin

	app.myMobileAgent.setMyProxy(null);
	bulkRelease := self.myMobileGenericAgent.myBulkRelease;	//01 Sep 2014	Par 34717 / CCMS 2384
	reserveDetail := self.myMobileGenericAgent.myReserveSelection;	//11 Dec 2015	NDL	Par 37288

	if obj = null then
		// searching for one
		self.myMobileGenericAgent.setMyNextTiToAction(null);
		self.myMobileGenericAgent.setMyDeliveryRequest(null);
		app.myMobileCargoFindAgent.setSearchSite(TI_SITE_ONSITE);
		app.myMobileCargoFindAgent.setHidePackItems(true);
		app.myMobileCargoFindAgent.setHideOnTransportTIs(true);			// TFS 38111 13/04/2018 jlgmd1
		if self.txtCargoId.text &lt;&gt; null 								// 36111
		and self.txtCargoId.text &lt;&gt; lblCargoId.caption.toUpper then     //cnwmc5 - 8 Dec 2015 - PAR 37230
			app.myMobileCargoFindAgent.setID(self.txtCargoId.text);
			app.myMobileCargoFindAgent.setDoSearchImmediate(true);
		else
			//16 Jul 2014	NDL	Par 34571
			if self.myPackContainer.myVoyage.isNullVoyage then
				app.myMobileCargoFindAgent.setMyVoyage(null);
			else
				app.myMobileCargoFindAgent.setMyVoyage( self.myPackContainer.myVoyage );
			endif;
		endif;
		//app.myMobileCargoFindAgent.setMyBookingRef( self.myPackContainer.bookingReference );
		self.gotoForm( MOB_HHCargoFind, false );
		return;
		
	elseif CM_TrackedItem.hasInstance( obj ) then
		ti := obj.CM_TrackedItem;
		
		//22 Jul 2016	NDL	TFS 2403
		if self.txtCargoId.text &lt;&gt; lblCargoId.caption.toUpper
		and self.txtCargoId.text.trimBlanks &lt;&gt; null
		and ti.id[1:self.txtCargoId.text.length] &lt;&gt; self.txtCargoId.text
		then
			//we were probably doing a find, didn't find a matching id in the list
			//and have automatically pressed the first button, which would happen
			//if the scanner was contfigured to tab/return automatically.
			reason := app.getErrorString(Item_selec_does_not_match_text);
			if app.msgBox( reason, app.getErrorString(Warning), MsgBox_Yes_No) &lt;&gt; MsgBox_Return_Yes then
				self.txtCargoId.setFocus();
				return;
			endif;
			reason := null;
		endif; 
		
		//01 Sep 2014	Par 34717 / CCMS 2384
		if bulkRelease &lt;&gt; null then
		//	if self.myHandHeldCheckerAgent.getReserveDetailCargoMatches( ti, 1, reason ) = null then
			if reserveDetail = null then	//11 Dec 2015	NDL	Par 37288
				if self.myMobileGenericAgent.getReserveDetailCargoMatches( ti, ti.totalQuantity.max(1), ti.totalWeight, reason ) = null then	//10 Nov 2015	NDL	Par 37092
					self.msgBox( reason, app.getErrorString(Error), MsgBox_OK_Only, true );
					return;
				endif;
			endif;
		endif;
		
		if not self.validatePreCargoChecks( ti ) then
			return;
		endif;
		
		//05 Jan 2016	NDL	TFS 3157
	//	create resDetailSet transient;
	//	create bookingItemSet transient;
	//	if ti.hasDocumentActions( resDetailSet, bookingItemSet ) then
	//		hasActions := true;
	//	endif;
		
		
	//	if not hasActions then	//05 Jan 2016	NDL	TFS 3157
		
			self.myMobileGenericAgent.setMyNextTiToAction(ti);
		
			//if self.myTerminal.allCargoChecks[CARGO_CHECK_CONTEXT_PACK].allCargoCheckItems.size = 0 then
			if self.myTerminal.hasNoCargoCheckItemsFor(CARGO_CHECK_CONTEXT_PACK) then	//31 May 2012	NDL Par 29038
				//16 Apr 2018	NDl	TFS 38083
			//	create formClassSet transient;
			//	formClassSet.add( MOB_HHPackContainerForm );
			//	formClassSet.add( MOB_HHPackContainerFormSTW );
			//	self.goBackToFirstOf( formClassSet );
			//	return;
				self.goBackToFirst( MOB_HHPackContainerForm );
				return;
			else
				self.myMobileGenericAgent.setChecksState(self.myMobileGenericAgent.PackCargo);
				self.gotoForm(MOB_HHGateCheckCargo, false);
				return;
			endif;
		
	/*	else
		
			//05 Jan 2016	NDL	TFS 3157
			doActions := false;
			doPack := false;
			doChecks := false;
		
			opt1Str := app.getErrorString( Pack );
			opt2Str := app.getErrorString( Perform_document_actions );
			opt3Str := app.getErrorString( Perform_cargo_checks );
			opt4Str := app.getErrorString(None_above_cancel);
			if self.myTerminal.hasNoCargoCheckItemsFor(CARGO_CHECK_CONTEXT_PACK) then 
				returnValue := self.optBox(opt1Str, opt2Str, opt3Str, opt4Str, 3, app.getErrorString(Select_option) );
				if returnValue = 1 then
					doPack := true;
				elseif returnValue = 2 then
					doActions := true;
				elseif returnValue = 3 then
					doChecks := true;
				else
					//bail
					return;
				endif;
			else
				returnValue := self.optBox(opt1Str, opt2Str, opt4Str, null, 2, app.getErrorString(Select_option) );
				if returnValue = 1 then
					doPack := true;
				elseif returnValue = 2 then
					doActions := true;
				else
					//bail
					return;
				endif;
			endif;
		
			if doActions then
				self.myMobileGenericAgent.setAllReserveDetails( resDetailSet );
				self.myMobileGenericAgent.setAllBookingRefItems( bookingItemSet );
				self.myMobileGenericAgent.setStringField1( self.fldCargo.topSheet.name );
				self.myMobileGenericAgent.setMyNextTiToAction( ti );
				self.gotoForm( MOB_HHDocumentActions, false );
				
			elseif doPack then
				self.myMobileGenericAgent.setMyNextTiToAction(ti);
				self.goBackToFirst( MOB_HHPackContainerForm );
				return;
				
			elseif doChecks then
				self.myMobileGenericAgent.setMyNextTiToAction(ti);
				self.myMobileGenericAgent.setChecksState(self.myMobileGenericAgent.PackCargo);
				self.gotoForm(MOB_HHGateCheckCargo, false);
				return;
				
			endif;
			
		endif; */
		
	
	endif;

epilog

//	delete resDetailSet;
//	delete bookingItemSet;
//	delete formClassSet;

end;</source>
        <Parameter name="obj" type="Object"/>
    </JadeMethod>
    <JadeMethod name="doFindCargoButton" id="1c9b76ed-171e-42bd-91b7-cfe1699f9d53">
        <updating/>
        <source>doFindCargoButton() updating;

/*

Purpose:			

Parameters:			

Return Value:		


Date		Who		Reason
04 July 2011	cnwndl1	Initial creation	Par 26476

*/

vars

	str : String;
//	booking : CM_BookingRef;
//	bookingItem : CM_BookingRefItem;
	ti : CM_TrackedItem;
	theTI : CM_TrackedItem;
	buttonNumber : Integer;
	menuButton : MOB_Button;
	value : Real;
//	bulkRelease : CM_BulkRelease;
//	yardLocation : CM_LocationsController;
	obj : Object;

begin

	str := self.txtCargoId.text.trimBlanks.toUpper;
	if str = null then
		return;
	endif;
	
	if str = self.lblCargoId.caption.trimBlanks.toUpper then
		return;
	endif;
	
	
	//22 Dec 2015	NDL	Par 37349
	if self.toPackSize &gt; 0 then
		foreach buttonNumber in 1 to  self.toPackSize do
			menuButton := self.btnInYardTemp.getControl(buttonNumber);
			if menuButton = null then
				continue;
			endif;
			obj := menuButton.userObject;
			if obj = null or not obj.isKindOf(CM_TrackedItem) then
				value := value + menuButton.height;
				continue;
			endif;
			ti :=  obj.CM_TrackedItem;
			if ti.id = str then
				theTI := ti;
				break;
			endif;
		endforeach;
	endif;
	
	if theTI &lt;&gt; null then
		self.scrToPack.value := value.Integer;
		menuButton := self.btnInYardTemp.getControl(buttonNumber);
		self.setMenuScrollStart( self.scrToPack, self.btnInYardTemp, self.frmToPack, toPackSize );
		menuButton.setFocus();
	endif;
	
epilog

end;</source>
    </JadeMethod>
    <JadeMethod name="doPack" id="cfac546c-d815-4f74-8a93-eebbb06a58d0">
        <updating/>
        <source>doPack( _ti : CM_TrackedItem input ) updating;

/*

Purpose:			

Parameters:			

Return Value:		


Date		Who		Reason
04 July 2011	cnwndl1	Initial creation	Par 26476

*/

vars
	
	hideMoveConfirmation:Boolean;
	
begin
	if _ti = null then
		return;
	endif;
	
	//epr par 34472 3/7/14
	hideMoveConfirmation := app.myPassport.myTerminal.getConfig(HhMoveConfirmation).Boolean;	//15 Dec 2015	NDL	Par 37288
	 self.myMobileGenericAgent.doPackCargo(self.myPackContainer,_ti,hideMoveConfirmation);
epilog
	
end;</source>
        <Parameter name="_ti" type="CM_TrackedItem" usage="input"/>
    </JadeMethod>
    <JadeMethod name="doPackAll" id="eb4443f2-fe48-46b6-af35-6ee40fc9ba67">
        <updating/>
        <source>doPackAll() protected, updating;
/*

Purpose:			

Parameters:			

Return Value:		

Date		Who		Reason
10 November 2014	cnwndl1	Initial creation	Par 35146

*/

vars

	buttonNumber : Integer;
	menuButton : MOB_Button;
	ti : CM_TrackedItem;
	complete : Boolean;

begin

	if self.toPackSize &lt; 1 then
		return;
	endif;
	
	foreach buttonNumber in 1 to self.toPackSize reversed do
		menuButton:= null;
		ti := null;
		menuButton := self.btnInYardTemp.getControl( buttonNumber ).MOB_Button;
		if menuButton = null then
			continue;
		endif;
		if menuButton.userObject = null or not menuButton.userObject.isKindOf(CM_TrackedItem) then
			continue;
		endif;
		ti := menuButton.userObject.CM_TrackedItem;
		if ti = null or ti.myParent = self.myPackContainer then
			continue;
		endif;
		
		if not self.myMobileGenericAgent.doPackCargo( self.myPackContainer, ti, true ) then
			return;
		endif;
		
		if app.myPassport.hasErrors then
			return;
		endif;
	
	endforeach;
	
	self.handleBtnOne();
	
	complete := true;

epilog

	if not complete then
		self.setupForm();
	endif;

end;</source>
        <access>protected</access>
    </JadeMethod>
    <JadeMethod name="doResizeControls" id="f52cf540-da1f-4437-a356-b9bb8561d1f6">
        <updating/>
        <source>doResizeControls(_initialSetup: Boolean) updating, protected;
/*

Purpose:			

Parameters:			

Return Value:		

Date		Who		Reason
19 February 2015	cnwndl1	Initial creation

*/

vars

	folderHeight : Real;
	stdHeight : Real;
	top : Real;
	thirdWidth : Real;
	stdWidth : Real;
	
	buttonWidth : Real;
	menuButton : MOB_Button;
	index : Integer;
	buttonHeight : Integer;
	buttonTop : Integer;

begin

	self.setResizeableMarginAndFontSize();
	stdHeight := self.getStdControlHeight;
	stdWidth := self.clientWidth - 4;

//	folderHeight := self.height - self.getCMDHeight() - (stdHeight *2)+  self.getSessionAgent().deviceHeightOffset;
	folderHeight := self.clientHeight - self.getCMDHeight() - (stdHeight *2); //26 Jun 2017	NDL	TFS 4647
	
	//28 Feb 2017	NDL	TFS 3157
	if self.docActsToDisplay.isEmpty then
		self.btnDocActions.visible := false;
	else
		self.btnDocActions.visible := true;
		self.btnDocActions.height := self.btnOne.height;
		folderHeight := folderHeight - self.btnDocActions.height - self.resizedMargin;
	endif;
	
	self.fldCargo.move(	self.resizedMargin,
						self.resizedMargin,
						stdWidth + 2,
						folderHeight
						);
						
	self.frmPacked.move(	0,
							0,
							self.shtPacked.width - self.resizedMargin,
							self.shtPacked.height// - self.resizedMargin
							);
								
	self.txtCargoId.move(	0,
							1,
							self.shtToPack.width - self.resizedMargin,
							stdHeight
							);
	self.txtCargoId.text := lblCargoId.caption.toUpper;
							
	top := self.txtCargoId.height + self.resizedMargin;
	self.frmToPack.move(	0,
							top,
							self.shtToPack.width - self.resizedMargin,
							self.shtToPack.height - top - self.resizedMargin
							);
							
	top := self.fldCargo.top + self.fldCargo.height; // + self.resizedMargin;
	
	if self.btnDocActions.visible then
		self.btnDocActions.move(	self.resizedMargin,
									top,
									self.btnOne.width,
									self.btnOne.height
									);
		top := top + self.btnDocActions.height + self.resizedMargin;
		
	endif;
	
	if _initialSetup then
		self.lblWeight.visible := true;
	endif;
	self.lblWeight.move(	self.resizedMargin,
							top,
							stdWidth,	//thirdWidth * 2,
							stdHeight );
							
	if _initialSetup then
		self.lblCount.visible := true;
	endif;
	top := self.lblWeight.top + self.lblWeight.height;
	self.lblCount.move(	self.resizedMargin,	//self.lblWeight.left + self.lblWeight.width,
						top,
						stdWidth,	//thirdWidth,
						stdHeight );
						
	
	if self.toPackSize &gt; 0 then
	
		buttonHeight := self.getScrollingButtonHeight();
		self.btnInYardTemp.height := buttonHeight;
		buttonWidth := self.getButtonWidthSetScrollBar( self.toPackSize, self.frmToPack, self.btnInYardTemp.height, self.scrToPack );
	
		buttonTop := self.resizedMargin - 1;
		foreach index in 1 to self.toPackSize do
			menuButton := self.btnInYardTemp.getControl( index ).MOB_Button;
			if menuButton &lt;&gt; null then
				menuButton.width := buttonWidth;
				menuButton.height := buttonHeight;
				menuButton.top := buttonTop;
				menuButton.fontSize := self.btnInYardTemp.fontSize;	//07 Apr 2015	NDL	Par 35990
				buttonTop := buttonTop + buttonHeight + self.resizedMargin;
			endif;
		endforeach;
	endif;
	
	if self.packedSize &gt; 0 then
	
		buttonHeight := self.getScrollingButtonHeight();
		self.btnPackedTemp.height := buttonHeight;
		buttonWidth := self.getButtonWidthSetScrollBar( self.packedSize, self.frmPacked, self.btnPackedTemp.height, self.scrPacked );
	
		buttonTop := self.resizedMargin - 1;
		foreach index in 1 to self.packedSize do
			menuButton := self.btnPackedTemp.getControl( index ).MOB_Button;
			if menuButton &lt;&gt; null then
				menuButton.width := buttonWidth;
				menuButton.height := buttonHeight;
				menuButton.top := buttonTop;
				menuButton.fontSize := self.btnPackedTemp.fontSize;	//07 Apr 2015	NDL	Par 35990
				buttonTop := buttonTop + buttonHeight + self.resizedMargin;
			endif;
		endforeach;
		
	endif;
						
	inheritMethod(_initialSetup);

epilog

end;</source>
        <access>protected</access>
        <Parameter name="_initialSetup" type="Boolean"/>
    </JadeMethod>
    <JadeMethod name="doUnPack" id="f5f2a5b2-d9de-45d8-b4ef-60d2ff194b59">
        <updating/>
        <source>doUnPack( obj : Object ) updating;

/*

Purpose:			

Parameters:			

Return Value:		


Date		Who		Reason
06 July 2011	cnwndl1	Initial creation

*/

vars
	success : Boolean;
	ti : CM_TrackedItem;
	formClassSet : ClassSet;
begin

	if CM_TrackedItem.hasInstance( obj ) then
		ti := obj.CM_TrackedItem;
	endif;
	
	if ti = null then
		return;
	endif;
	
	//epr par 34472 3/7/14
	success := self.myMobileGenericAgent.doUnpackCargoAgain(ti);
														
	if success then
		//self.goBackToFirst( MOB_HHPackContainerForm );
		//16 Apr 2018	NDL	TFS 38083
		create formClassSet transient;
		formClassSet.add( MOB_HHPackContainerForm );
		formClassSet.add( MOB_HHPackContainerFormSTW );
		self.goBackToFirstOf( formClassSet );
		return;
	endif;
													
epilog

	delete formClassSet;

end;</source>
        <Parameter name="obj" type="Object"/>
    </JadeMethod>
    <JadeMethod name="getUltimateParentIfNotPackedIn" id="7c0eadbf-b309-4258-9f9a-da2893bd8b9d">
        <source>getUltimateParentIfNotPackedIn( _ti : CM_TrackedItem; _packedIn : CM_TrackedItem ):CM_TrackedItem protected;
/*

Purpose:			

Parameters:			

Return Value:		

Date		Who		Reason
11 July 2014	cnwndl1	Initial creation	Par 34020

*/

vars

	potentialChildTi : CM_TrackedItem;
	parentTi : CM_TrackedItem;

begin

	if _ti = null then
		return null;
	endif;
	
	potentialChildTi := _ti;
	while potentialChildTi.myParent &lt;&gt; null do
		parentTi := potentialChildTi.myParent;
		if parentTi = _packedIn then
			// its already packed in this container
			parentTi := null;
			break;
		elseif parentTi.isContainer then
			// its already packed in another container
			parentTi := null;
			break;
		endif;
		potentialChildTi := parentTi;
	endwhile;
	
	return parentTi;

epilog

end;</source>
        <access>protected</access>
        <Parameter name="_ti" type="CM_TrackedItem"/>
        <Parameter name="_packedIn" type="CM_TrackedItem"/>
        <ReturnType type="CM_TrackedItem"/>
    </JadeMethod>
    <JadeMethod name="handleBtnOne" id="beea6f9d-99fa-4618-b19f-7115f66ddac4">
        <updating/>
        <source>handleBtnOne() updating, protected;

/*

Purpose:			

Parameters:			

Return Value:		


Date		Who		Reason
04 July 2011	cnwndl1	Initial creation	Par 26476

*/

vars

begin
	
	self.gotoForm( MOB_HHPackComplete, false );
	return;

epilog

end;</source>
        <access>protected</access>
    </JadeMethod>
    <JadeMethod name="handleBtnThree" id="d16917ab-6c70-4d5e-b115-0009553ac0e1">
        <updating/>
        <source>handleBtnThree() updating, protected;
/*

Purpose:			

Parameters:			

Return Value:		

Date		Who		Reason
10 November 2014	cnwndl1	Initial creation	Par 35146

*/

vars

begin

	self.doBackButtonClick();

epilog

end;</source>
        <access>protected</access>
    </JadeMethod>
    <JadeMethod name="handleBtnTwo" id="d1b7e5fc-067f-4fa3-a3f6-b3baceaa9780">
        <updating/>
        <source>handleBtnTwo() updating, protected;

/*

Purpose:			

Parameters:			

Return Value:		


Date		Who		Reason
04 July 2011	cnwndl1	Initial creation	Par 26476

*/

vars

begin

	if self.allowPackAll then
		self.doPackAll();	//10 Nov 2014	NDL	Par 35146
		
	else
		self.doBackButtonClick();
	
	endif;
	
epilog

end;</source>
        <access>protected</access>
    </JadeMethod>
    <JadeMethod name="scrPacked_change" id="7162f998-7a88-4598-8c57-987d78bea9c1">
        <controlMethod name="ScrollBar::change"/>
        <methodInvocation>event</methodInvocation>
        <updating/>
        <source>scrPacked_change(scroll: ScrollBar input) updating;

/*

Purpose:			

Parameters:			

Return Value:		


Date		Who		Reason
04 July 2011	cnwndl1	Initial creation	Par 26476

*/

vars

begin

	self.setMenuScrollStart( scroll, self.btnPackedTemp, self.frmPacked, self.packedSize );

epilog

end;</source>
        <Parameter name="scroll" type="ScrollBar" usage="input"/>
    </JadeMethod>
    <JadeMethod name="scrToPack_change" id="cdfb0ca8-deca-4ba7-a831-4aa2379eba78">
        <controlMethod name="ScrollBar::change"/>
        <methodInvocation>event</methodInvocation>
        <updating/>
        <source>scrToPack_change(scroll: ScrollBar input) updating;

/*

Purpose:			

Parameters:			

Return Value:		


Date		Who		Reason
04 July 2011	cnwndl1	Initial creation

*/

vars

begin

	self.setMenuScrollStart( scroll, self.btnInYardTemp, self.frmToPack, self.toPackSize );

epilog

end;</source>
        <Parameter name="scroll" type="ScrollBar" usage="input"/>
    </JadeMethod>
    <JadeMethod name="setWeightCountCaptions" id="78a22e4a-e348-4c03-a88c-5542b52d8157">
        <source>setWeightCountCaptions();

/*

Purpose:			

Parameters:			

Return Value:		


Date		Who		Reason
04 July 2011	cnwndl1	Initial creation

*/

vars

	weightStr : String;
	tiMaster : CM_TIMaster;
	tonnes : Integer;
	colour : Integer;
	prod : CM_Product;
	ti : CM_TrackedItem;
	bulkRelease : CM_BulkRelease;
	childReserve : CM_ReserveDetail;
	totalPackedCount : Integer;
	totalWtPacked : Decimal [14,4];
	totalVolPacked : Decimal [14,4];
	totalChildrenReq : Integer;
	totalWeightReq : Decimal [14,4];
	totalVolumeReq : Decimal [14,4];
	cargoType : CM_CargoType;
	measure	: CM_MeasurementType;
	reqStr :String;

begin

	colour := Black;
	
	if self.myMobileGenericAgent.myPackStockBookingRef &lt;&gt; null then
		weightStr := myMobileGenericAgent.myPackStockBookingRef.getPackAsString &amp; "    " &amp; app.getErrorString(ParentWt) &amp; " ";
	else
		weightStr := app.getErrorString(ParentWt) &amp; " ";
	endif;
	//weightStr := weightStr &amp; self.myPackContainer.totalWeight.Integer.String;
	// cnwrd1 ccms 1321 imperial measurements
	prod := self.myPackContainer.getProductForMeasurementConv();
	weightStr := weightStr &amp; self.myMobileDataAgent.getMeasureConvertedForDisplay(prod,Type_Weight,self.myPackContainer.totalWeight) &amp; self.myMobileDataAgent.getMeasureLabelUnit(prod,Type_Weight);
	
	tiMaster := app.myRoot.allTiMasters.getAtKey( self.myPackContainer.cargoType,  self.myPackContainer.id);
	if tiMaster &lt;&gt; null then
		if tiMaster.isKindOf(CM_TIMasterContainer) then
			if tiMaster.CM_TIMasterContainer.maxKilos &gt; 0 then
				tonnes := ( tiMaster.CM_TIMasterContainer.maxKilos/1000 ).Integer;
				if tonnes &gt; 0 then
					//weightStr := weightStr &amp; " (" &amp; tonnes.String &amp; "t" &amp; ")";
					weightStr := weightStr &amp; " (" &amp; self.myMobileDataAgent.getMeasureConvertedForDisplay(null,Type_Weight,tiMaster.CM_TIMasterContainer.maxKilos) &amp; self.myMobileDataAgent.getMeasureLabelUnit(null,Type_Weight) &amp; ")";
					if tiMaster.CM_TIMasterContainer.maxKilos &lt; self.myPackContainer.totalWeight.Integer then
						colour := Red;
					endif;
				endif;
			endif;
		endif;
	endif;
	
	self.lblWeight.caption :=  weightStr;	
	self.lblWeight.foreColor := colour;
	
	// show how much requested too 18 Jan 2016	NDL	Par 37422
	bulkRelease := self.myMobileGenericAgent.myBulkRelease;
	if bulkRelease &lt;&gt; null and bulkRelease.isPackingType then
		foreach ti in self.myPackContainer.allChildren do
			//13 Jun 2016	NDL	TFS 2133
			if not ti.allBulkReleasesAssigned.includes( bulkRelease ) then
				continue;
			endif;
			cargoType := ti.myCargoType;
			if cargoType &lt;&gt; null and cargoType.isTrackByVolume then
				totalVolPacked := totalVolPacked + ti.totalVolume;
			elseif cargoType &lt;&gt; null and cargoType.isTrackByWeight then
				totalWtPacked := totalWtPacked + ti.totalWeight;
			else
				totalPackedCount := totalPackedCount + ti.totalQuantity.max(1);
			endif;
		endforeach;
	
		foreach childReserve in bulkRelease.allReservePackingChildren do
			cargoType := childReserve.getCargoType;
			if cargoType &lt;&gt; null and cargoType.isTrackByVolume then
				totalVolumeReq := totalVolumeReq + (childReserve.weightRequested / childReserve.getDensity);
			elseif cargoType &lt;&gt; null and cargoType.isTrackByWeight then
				totalWeightReq := totalWeightReq + childReserve.weightRequested;
			else
				totalChildrenReq := totalChildrenReq + childReserve.totalRequested;
			endif;
		endforeach;
		
		reqStr := null;
		if totalVolumeReq &gt; 0 or totalWeightReq &gt; 0 or totalChildrenReq &gt; 0 then
			if totalVolumeReq &gt; 0 then
				measure := app.getDefaultMeasurementForType(Type_Volume);
				if totalVolPacked = 0 then
					reqStr := app.getErrorString(VolumeAbbrev) &amp; " 0/" &amp; app.convertMeasurementForDisplay(measure.shortName, totalVolumeReq) &amp; "(" &amp; measure.shortName &amp; ")";
				else
					reqStr := app.getErrorString(VolumeAbbrev) &amp; " " &amp; app.convertMeasurementForDisplay(measure.shortName, totalVolPacked) &amp; "/" &amp; app.convertMeasurementForDisplay(measure.shortName, totalVolumeReq) &amp; "(" &amp; measure.shortName &amp; ")";
				endif;
			endif;
			if totalWeightReq &gt; 0 then
				if reqStr &lt;&gt; null then
					reqStr := reqStr &amp; ", ";
				endif;
				measure := app.getDefaultMeasurementForType(Type_Weight);
				if totalWtPacked = 0 then
					reqStr := reqStr &amp; app.getErrorString(WeightAbbrev) &amp; " 0/" &amp; app.convertMeasurementForDisplay(measure.shortName, totalWeightReq) &amp; "(" &amp; measure.shortName &amp; ")";
				else
					reqStr := reqStr &amp; app.getErrorString(WeightAbbrev) &amp; " " &amp; app.convertMeasurementForDisplay(measure.shortName, totalWtPacked) &amp; "/" &amp; app.convertMeasurementForDisplay(measure.shortName, totalWeightReq) &amp; "(" &amp; measure.shortName &amp; ")";
				endif;
			endif;
			if totalChildrenReq &gt; 0 then
				if reqStr &lt;&gt; null then
					reqStr := reqStr &amp; ", ";
				endif;
				reqStr := reqStr &amp; app.getErrorString(CountAbbrev) &amp; " " &amp; totalPackedCount.String &amp; "/" &amp; totalChildrenReq.String;
			endif;
			
		elseif totalVolPacked &gt; 0 or totalWtPacked &gt; 0 or totalPackedCount &gt; 0 then	// None requested but some packed
			if totalVolPacked &gt; 0 then
				measure := app.getDefaultMeasurementForType(Type_Volume);
				reqStr := app.getErrorString(VolumeAbbrev) &amp; " " &amp; app.convertMeasurementForDisplay(measure.shortName, totalVolPacked);
			endif;
			if totalWtPacked &gt; 0 then
				if reqStr &lt;&gt; null then
					reqStr := reqStr &amp; ", ";
				endif;
				measure := app.getDefaultMeasurementForType(Type_Weight);
				reqStr := reqStr &amp; app.getErrorString(WeightAbbrev) &amp; " " &amp; app.convertMeasurementForDisplay(measure.shortName, totalWtPacked);
			endif;
			if totalPackedCount &gt; 0 then
				if reqStr &lt;&gt; null then
					reqStr := reqStr &amp; ", ";
				endif;
				reqStr := reqStr &amp; app.getErrorString(CountAbbrev) &amp; " " &amp; totalPackedCount.String;
			endif;
			
		else	//Nothing requested and nothing packed.
			reqStr := reqStr &amp; "0";
			
		endif;
		
		self.lblCount.caption := app.getErrorString(Child) &amp; ": " &amp; reqStr;
		
	else
		//01 Sep 2014	Par 34717 / CCMS 2384
		foreach ti in self.myPackContainer.allChildren do
			totalPackedCount := totalPackedCount + ti.totalQuantity.max(1);
		endforeach;
		self.lblCount.caption := app.getErrorString(Child) &amp; " " &amp; app.getErrorString(CountAbbrev) &amp; " " &amp; totalPackedCount.String;
		
	endif;
	
	

epilog

end;</source>
    </JadeMethod>
    <JadeMethod name="setupControls" id="fa5e3ed2-64cc-4a2e-b581-1480a8cb7e0f">
        <updating/>
        <source>setupControls() protected, updating;

/*

Purpose:			

Parameters:			

Return Value:		


Date		Who		Reason
04 July 2011	cnwndl1	Initial creation	Par 26476

*/

vars

	sheetName : String;

begin
	
	sheetName := self.myMobileGenericAgent.stringField1;
	
	self.docActsToDisplay.clear();
	self.myMobileGenericAgent.setAllReserveDetails( null );
	self.myMobileGenericAgent.setAllBookingRefItems( null );
	self.myMobileGenericAgent.setStringField1(null);
	
	self.setupToPackBtns();
	self.setupPackedBtns();
	self.setWeightCountCaptions();
	
	self.doResizeControls(true);
	
	if sheetName = self.shtPacked.name then
		self.fldCargo.querySheetChg(self.fldCargo,self.shtPacked);
		self.fldCargo.topSheet := self.shtPacked;
	elseif sheetName = self.shtToPack.name then
		self.fldCargo.querySheetChg(self.fldCargo,self.shtToPack);
		self.fldCargo.topSheet := self.shtToPack;
	endif;
	
	self.txtCargoId.setFocus();
	

epilog

end;</source>
        <access>protected</access>
    </JadeMethod>
    <JadeMethod name="setupForm" id="cd98e6d9-e803-4b07-b99f-f9f9b39997fd">
        <updating/>
        <source>setupForm() updating, protected;

/*

Purpose:			

Parameters:			

Return Value:		


Date		Who		Reason
04 July 2011	cnwndl1	Initial creation	Par 26476

*/

vars

	isFromDocAction : Boolean;	//05 Jan 2017	NDL	TFS 3157
	sheetName : String;	//05 Jan 2017	NDL	TFS 3157
	str : String;	//19 May 2017	NDL	TFS 4016

begin
	
	self.myHandHeldCheckerAgent := app.myHandHeldCheckerAgent;
	self.myPackContainer := self.myMobileGenericAgent.myPackTi;
	self.caption := app.getErrorString(Pack) &amp; " " &amp; self.myPackContainer.id;
	
	if app.myMobileCargoFindAgent = null then
		create app.myMobileCargoFindAgent transient;
	endif;
	
	//05 Jan 2017	NDL	TFS 3157
	if self.myMobileGenericAgent.allReserveDetails &lt;&gt; null and self.myMobileGenericAgent.allBookingRefItems &lt;&gt; null then
		isFromDocAction := ( self.myMobileGenericAgent.allReserveDetails.size + self.myMobileGenericAgent.allBookingRefItems.size ) &gt; 0 and ( self.myMobileGenericAgent.stringField1 &lt;&gt; null );
	endif;
	
	//19 May 2017	NDL	TFS 4016
	self.applyTwelveStwRules :=  self.myTerminal.getConfig(UseAltSelectRulesTL).Boolean and app.isCargoGroupEnabled;
	if self.applyTwelveStwRules then
		str := self.myMobileGenericAgent.stringField1;
		if str &lt;&gt; null and str.isInteger then
			self.stwRuleNum := str.Integer;
			self.myMobileGenericAgent.setStringField1( null );
		endif;
	endif;
	
	// returning from a search initiated from the 'clickInYardItem'
	if app.myMobileCargoFindAgent.myTrackedItemToProcess &lt;&gt; null then
		self.myMobileGenericAgent.setMyNextTiToAction(app.myMobileCargoFindAgent.myTrackedItemToProcess);
		app.myMobileCargoFindAgent.clearHistory( true );
		self.myMobileGenericAgent.setMyDeliveryRequest(null);
		
		if not self.validatePreCargoChecks( self.myMobileGenericAgent.myNextTiToAction ) then
			self.myMobileGenericAgent.setMyNextTiToAction( null );
			
		else
		
			//if self.myHandHeldCheckerAgent.myTerminal.allCargoChecks[CARGO_CHECK_CONTEXT_PACK].allCargoCheckItems.size &lt;&gt; 0 then
			if not self.myMobileGenericAgent.myTerminal.hasNoCargoCheckItemsFor(CARGO_CHECK_CONTEXT_PACK) then	//31 May 2012	NDL Par 29038
				//requires checks first
				self.myMobileGenericAgent.setChecksState(self.myMobileGenericAgent.PackCargo);
				self.gotoForm(MOB_HHGateCheckCargo, false);
				return;
			else
				// drop thru to the next code
			endif;
		
		endif;
	endif;
	
	//returning from 'MOB_HHGateCheckCargo' when we have done a 'Next' (initiated from code immediately above or from the 'clickInYardItem' )
	if self.myMobileGenericAgent.myNextTiToAction &lt;&gt; null then
		//epr par 34472 3/7/14
		self.doPack(self.myMobileGenericAgent.myNextTiToAction);
		self.myMobileGenericAgent.setMyNextTiToAction(null);
	endif;
	
	self.allowPackAll := false;

	inheritMethod();
	
	//self.setupCmdButtons( 2, app.getErrorString(Complete), app.getErrorString(Back), null, null, null );
	//10 Nov 2014	NDL	Par 35146
	
	if app.getMyUser.canDo(self.myTerminal, PmHHAllowLoadAll) then
		self.allowPackAll := true;
	endif;
	
	if self.allowPackAll then
		self.setupCmdButtons( 3, app.getErrorString(Complete), "Pack All", app.getErrorString(Back), null, null );
	else
		self.setupCmdButtons( 2, app.getErrorString(Complete), app.getErrorString(Back), null, null, null );
	endif;
	
	self.setupControls();
	
	if isFromDocAction then
		sheetName := self.myMobileGenericAgent.stringField1;
		if sheetName = self.shtToPack.name then
			self.fldCargo.querySheetChg(self.fldCargo,self.shtToPack);
			self.fldCargo.topSheet := self.shtToPack;
		elseif sheetName = self.shtPacked.name then
			self.fldCargo.querySheetChg(self.fldCargo,self.shtPacked);
			self.fldCargo.topSheet := self.shtPacked;
		endif;
	endif;

epilog

end;</source>
        <access>protected</access>
    </JadeMethod>
    <JadeMethod name="setupPackedBtns" id="1155197c-2627-4038-81bb-9273c1e96f2a">
        <updating/>
        <source>setupPackedBtns() updating;

/*

Purpose:			

Parameters:			

Return Value:		


Date		Who		Reason
04 July 2011	cnwndl1	Initial creation	Par 26476
01 Nov	2016	cnwma1	TFS #2857:PAR38311 STWR - Unhandled exception when doing a Pack All
*/

constants

	TAB_INDEX_START = 1000;
	
vars

	errMess : String;
	buttonWidth : Real;
	buttonNumber : Integer;
	menuButton : MOB_Button;
	buttonOffset : Integer;
	ti  : CM_TrackedItem;
	buttonHeight : Integer;
	
	bookingRefItem : CM_BookingRefItem;
	resDetail : CM_ReserveDetail;
	resDetailSet : CM_ReserveDetailSet;
	bookingItemSet : CM_BookingRefItemSet;
	docActions : CM_DocumentActions;
	taskAndStopStr : String;
	
begin
	
	// get the container
	if self.myPackContainer = null then
		errMess := "developer error,  no Container";
		self.msgBox( errMess, app.getErrorString(Error), MsgBox_OK_Only, true);
		return;
	endif;
	
		
	// create the buttons on the form
	self.packedSize := self.myPackContainer.allChildren.size;
	buttonHeight := self.getScrollingButtonHeight();
	self.btnPackedTemp.height := buttonHeight;
	buttonWidth := self.getButtonWidthSetScrollBar( self.packedSize, self.frmPacked, self.btnPackedTemp.height, self.scrPacked );

	//05 Jan 2016	NDL	TFS 3157/2001
	create resDetailSet transient;
	create bookingItemSet transient;

	foreach ti in self.myPackContainer.allChildren do
		
		//05 Jan 2016	NDL	TFS 3157/2001
		docActions := null;
		resDetailSet.clear;
		bookingItemSet.clear;
		if ti.hasDocumentActions( resDetailSet, bookingItemSet ) then
			if not bookingItemSet.isEmpty then
				bookingRefItem := bookingItemSet.first;
				if bookingRefItem &lt;&gt; null then
					docActions := bookingRefItem.myBookingRefItemActions;
				endif;
			elseif not resDetailSet.isEmpty then
				resDetail := resDetailSet.first;
				if resDetail &lt;&gt; null then
					docActions := resDetail.myReserveDetailActions;
				endif;
			endif;
		endif;	
		
		buttonNumber := buttonNumber + 1;
		
		menuButton := self.btnPackedTemp.getControl( buttonNumber ).MOB_Button;  //1/11/2016 cnwma1  	TFS #2857 PAR #38311
		if menuButton &lt;&gt; null then												 //1/11/2016 cnwma1  	TFS #2857 PAR #38311
			menuButton.visible := false;									     //1/11/2016 cnwma1  	TFS #2857 PAR #38311
			delete menuButton;													 //1/11/2016 cnwma1  	TFS #2857 PAR #38311
		endif;																	 //1/11/2016 cnwma1  	TFS #2857 PAR #38311
		
		menuButton := self.btnPackedTemp.loadControl( buttonNumber ).MOB_Button;
		menuButton.move(0, buttonOffset, buttonWidth, buttonHeight-4 );
		
		menuButton.userObject := ti;
		//menuButton.caption := ti.cargoTypeDescr &amp; " - " &amp; ti.id;
		//01 Sep 2014	Par 34717 / CCMS 2384
		if ti.canSplitTotalQuantity and ti.isBulkByQuantity then
			menuButton.caption := ti.cargoTypeDescr &amp; " - " &amp; ti.id &amp; "(" &amp; ti.totalQuantity.String &amp; ")";
		//elseif ti.isBulkByWeight and ti.isBulkByVolume then
		elseif ( ti.isBulkByWeight or ti.isBulkByVolume ) then	//10 Dec 2015	NDL	Par 37288
			menuButton.caption := ti.cargoTypeDescr &amp; " - " &amp; ti.id &amp; "(" &amp; ti.CM_Bulk.getBulkTrackByDisplayForWeight(true) &amp; ")";
		else
			menuButton.caption := ti.cargoTypeDescr &amp; " - " &amp; ti.id;
		endif;
		
		//05 Jan 2016	NDL	TFS 3157/2001
		if docActions &lt;&gt; null then
			taskAndStopStr := docActions.getTaskAndStopStrForTi(ti,true);
			if taskAndStopStr &lt;&gt; null then
				menuButton.caption := menuButton.caption &amp; " " &amp; taskAndStopStr;
				self.docActsToDisplay.addExclusive( docActions );
			endif;
		endif;
		
		self.addHazDetailsToButton( menuButton, ti, CrLf ); //30 May 2017	NDL	TFS 4587
		
		menuButton.enabled := true;
		
		buttonOffset := buttonOffset + buttonHeight-2;
		menuButton.tabIndex := TAB_INDEX_START + buttonNumber;
		
	endforeach;
	
	self.btnPackedTemp.visible := false;

epilog

	delete resDetailSet;
	delete bookingItemSet;

end;</source>
    </JadeMethod>
    <JadeMethod name="setupToPackBtns" id="fe9a7dac-e048-4604-af4d-3b22fc3b516b">
        <updating/>
        <source>setupToPackBtns() updating;

/*

Purpose:			

Parameters:			

Return Value:		


Date		Who		Reason
04 July 2011	cnwndl1	Initial creation		Par 26476

*/

constants

	TAB_INDEX_START = 1000;
	
vars

	booking : CM_BookingRef;
	bookingItem : CM_BookingRefItem;	
	errMess : String;
	buttonWidth : Real;
	tiArray : CM_TIArray;
	buttonNumber : Integer;
	menuButton : MOB_Button;
	buttonOffset : Integer;
	ti : CM_TrackedItem;
	ultParent : CM_TrackedItem;
	bulkRelease : CM_BulkRelease;
	yardLocation : CM_LocationsController;
	bulkReleaseMode : Boolean;
	buttonHeight : Integer;
	allowMoreCargo : Boolean;
	reserveSelection : CM_ReserveSelection;
	
	bookingRefItem : CM_BookingRefItem;
	resDetail : CM_ReserveDetail;
	resDetailSet : CM_ReserveDetailSet;
	bookingItemSet : CM_BookingRefItemSet;
	docActions : CM_DocumentActions;
	taskAndStopStr : String;
	
begin

	//10 Nov 2014	NDL	Par 35146
	if self.toPackSize &gt; 0 then
		foreach buttonNumber in 1 to self.toPackSize do
			menuButton := self.btnInYardTemp.getControl( buttonNumber ).MOB_Button;
			if menuButton &lt;&gt; null then
				menuButton.visible := false;
				delete menuButton;
			endif;
		endforeach;
		self.toPackSize := 0;
		buttonNumber := 0;
	endif;
	
	allowMoreCargo := self.myTerminal.getConfig(AllowPackMoreCargo).Boolean;	//07 Dec 2015	NDL	Par 36866
	
	// get the booking / booking item
	booking := self.myMobileGenericAgent.myPackStockBookingRef;
	if booking &lt;&gt; null then
		bookingItem := self.myMobileGenericAgent.myPackStockBookingItem;
	endif;
	//01 Sep 2014	Par 34717 / CCMS 2384
	bulkRelease := self.myMobileGenericAgent.myBulkRelease;
	yardLocation := self.myMobileGenericAgent.myYardLocation;
	
	create tiArray transient;
	
	if bookingItem &lt;&gt; null then
		foreach ti in bookingItem.allTIs do
			// 11 Nov 2014	NDL	Par 35206
			if not ti.isOnSite then
				continue;
			endif;
			if ti.myParent = null then
				tiArray.add( ti );
			else
				//11 July 2014	NDL	Par 34020
				ultParent := self.getUltimateParentIfNotPackedIn( ti, self.myPackContainer );
				if ultParent &lt;&gt; null then
					tiArray.addExclusive( ultParent );
				endif;
			endif;
		endforeach;
		
	elseif booking &lt;&gt; null then
		foreach ti in booking.allTIs do
			// 11 Nov 2014	NDL	Par 35206
			if not ti.isOnSite then
				continue;
			endif;
			if ti.myParent = null then
				tiArray.add( ti );
			else
				//11 July 2014	NDL	Par 34020
				ultParent := self.getUltimateParentIfNotPackedIn( ti, self.myPackContainer );
				if ultParent &lt;&gt; null then
					tiArray.addExclusive( ultParent );
				endif;
			endif;
		endforeach;
	
	elseif bulkRelease &lt;&gt; null then	//21 Aug 2014	NDL	Par 34717  CCMS 2384	
		bulkReleaseMode := true;
		reserveSelection := self.myMobileGenericAgent.myReserveSelection;	//11 Dec 2015	NDL	Par 37288
		//19 May 2017	NDL	TFS 4016
		if self.applyTwelveStwRules then
			self.myMobileGenericAgent.getCargoBasedOnSTW12Rules( bulkRelease, reserveSelection, yardLocation, null, self.stwRuleNum, tiArray );
		else
			self.getBRPropsectiveChildCargo( bulkRelease, reserveSelection, yardLocation, null, tiArray );
		endif;	
	endif;
		
	// create the buttons on the form
	self.toPackSize := tiArray.size;
	//if app.myRoot.getConfig(AllowPackMoreCargo).Boolean and not bulkReleaseMode then
	if allowMoreCargo and not bulkReleaseMode then	//04 Dec 2015	NDL	Par 36866
		self.toPackSize := self.toPackSize + 1;
	endif;

	buttonHeight := self.getScrollingButtonHeight();
	self.btnInYardTemp.height := buttonHeight;
	buttonWidth := self.getButtonWidthSetScrollBar( self.toPackSize, self.frmToPack, self.btnInYardTemp.height, self.scrToPack );

	//05 Jan 2016	NDL	TFS 3157/2001
	create resDetailSet transient;
	create bookingItemSet transient;

	foreach ti in tiArray do
	
		//05 Jan 2016	NDL	TFS 3157/2001
		docActions := null;
		resDetailSet.clear;
		bookingItemSet.clear;
		if ti.hasDocumentActions( resDetailSet, bookingItemSet ) then
			if not bookingItemSet.isEmpty then
				bookingRefItem := bookingItemSet.first;
				if bookingRefItem &lt;&gt; null then
					docActions := bookingRefItem.myBookingRefItemActions;
				endif;
			elseif not resDetailSet.isEmpty then
				resDetail := resDetailSet.first;
				if resDetail &lt;&gt; null then
					docActions := resDetail.myReserveDetailActions;
				endif;
			endif;
		endif;
		
		buttonNumber := buttonNumber + 1;
		menuButton := self.btnInYardTemp.loadControl( buttonNumber ).MOB_Button;
		menuButton.move(0, buttonOffset, buttonWidth, buttonHeight-4 );
		
		menuButton.userObject := ti;
		menuButton.enabled := true;
	//	menuButton.caption := ti.cargoTypeDescr &amp; " - " &amp; ti.id;
	//01 Sep 2014	Par 34717 / CCMS 2384
		if ti.canSplitTotalQuantity and ti.isBulkByQuantity then
			menuButton.caption := ti.cargoTypeDescr &amp; " - " &amp; ti.id &amp; "(" &amp; ti.totalQuantity.String &amp; ")" 
										&amp; CrLf &amp; ti.myPrimaryActualLocation.getFullId;
		//elseif ti.isBulkByWeight and ti.isBulkByVolume then
		elseif ( ti.isBulkByWeight or ti.isBulkByVolume ) then	//10 Dec 2015	NDL	Par 37288
			menuButton.caption := ti.cargoTypeDescr &amp; " - " &amp; ti.id &amp; "(" &amp; ti.CM_Bulk.getBulkTrackByDisplayForWeight(true) &amp; ")" 
										&amp; CrLf &amp; ti.myPrimaryActualLocation.getFullId;
		else
			menuButton.caption := ti.cargoTypeDescr &amp; " - " &amp; ti.id &amp; CrLf &amp; ti.myPrimaryActualLocation.getFullId;
		endif;
		
		//05 Jan 2016	NDL	TFS 3157/2001
		if docActions &lt;&gt; null then
			taskAndStopStr := docActions.getTaskAndStopStrForTi(ti,true);
			if taskAndStopStr &lt;&gt; null then
				menuButton.caption := menuButton.caption &amp; " " &amp; taskAndStopStr;
				self.docActsToDisplay.addExclusive( docActions );
			endif;
		endif;
		
		self.addHazDetailsToButton( menuButton, ti, " " ); //30 May 2017	NDL	TFS 4587
		
		buttonOffset := buttonOffset + buttonHeight-2;
		menuButton.tabIndex := TAB_INDEX_START + buttonNumber;
		
	endforeach;
	
	//if app.myRoot.getConfig(AllowPackMoreCargo).Boolean and not bulkReleaseMode then
	if allowMoreCargo and not bulkReleaseMode then	//07 Dec 2015	NDL	Par 36866
		buttonNumber := buttonNumber + 1;
		menuButton := self.btnInYardTemp.loadControl( buttonNumber ).MOB_Button;
		menuButton.move(0, buttonOffset, buttonWidth, buttonHeight-4 );
		
		menuButton.enabled := true;
		menuButton.caption := "Find Cargo to Pack...";
		
		buttonOffset := buttonOffset + buttonHeight-2;
		menuButton.tabIndex := TAB_INDEX_START + buttonNumber;
	endif;

	self.btnInYardTemp.visible := false;

epilog

	delete resDetailSet;
	delete bookingItemSet;
	delete tiArray;

end;</source>
    </JadeMethod>
    <JadeMethod name="translateForm" id="d337e3be-4644-4106-af04-f71c350345cd">
        <updating/>
        <source>translateForm(locale : CM_Locale) updating;
	// This method implements user-defined locale-based text translation for forms.
	// Created on 20 March 2017 09:31:04.
	//3/11/2019	jlgns1	TFS 3827 XLAT_FormCaptions need to be sorted out
vars
	
begin
	// Determine the locale for translations.
	inheritMethod(locale);
	
	// The following code invokes text translation for controls defined on this form.
	self.translateCaption(Xlat_0016612, locale);
	btnDocActions.translateCaption(Xlat_0021924, locale);
	btnInYardTemp.translateCaption(Xlat_0016613, locale);
	btnPackedTemp.translateCaption(Xlat_0016614, locale);
	lblCargoId.translateCaption(Xlat_0016615, locale);
	lblCount.translateCaption(Xlat_0016616, locale);
	lblWeight.translateCaption(Xlat_0016617, locale);
	shtPacked.translateCaption(Xlat_0016618, locale);
	shtToPack.translateCaption(Xlat_0016619, locale);
end;</source>
        <Parameter name="locale" type="CM_Locale"/>
    </JadeMethod>
    <JadeMethod name="txtCargoId_change" id="fcce8e14-bc5e-40d1-bd13-28375c7b5433">
        <controlMethod name="TextBox::change"/>
        <methodInvocation>event</methodInvocation>
        <updating/>
        <source>txtCargoId_change(textbox: TextBox input) updating;

/*

Purpose:			

Parameters:			

Return Value:		


Date		Who		Reason
04 July 2011	cnwndl1	Initial creation	Par 26476

*/

vars

begin

	app.convertScannedCargoId( self.myTerminal, textbox, false, null );	//06 Jul 2016	NDL	TFS 2275
	
	self.doFindCargoButton();
	
epilog

end;</source>
        <Parameter name="textbox" type="TextBox" usage="input"/>
    </JadeMethod>
    <JadeMethod name="txtCargoId_gotFocus" id="dbada9bc-1d92-4559-8c1e-8f3deaa4ae9b">
        <controlMethod name="TextBox::gotFocus"/>
        <methodInvocation>event</methodInvocation>
        <updating/>
        <source>txtCargoId_gotFocus(textbox: TextBox input) updating;

/*

Purpose:			

Parameters:			

Return Value:		


Date		Who		Reason
04 July 2011	cnwndl1	Initial creation	Par 26476

*/

vars

begin

	if txtCargoId.text = lblCargoId.caption.toUpper then
		txtCargoId.selStart := 0;
		txtCargoId.selLength := lblCargoId.caption.length;
	endif;

epilog

end;</source>
        <Parameter name="textbox" type="TextBox" usage="input"/>
    </JadeMethod>
    <JadeMethod name="txtCargoId_lostFocus" id="f3c38994-3988-4840-87d7-887753a09a09">
        <controlMethod name="TextBox::lostFocus"/>
        <methodInvocation>event</methodInvocation>
        <updating/>
        <source>txtCargoId_lostFocus(textbox: TextBox input) updating;

/*

Purpose:			

Parameters:			

Return Value:		


Date		Who		Reason
04 July 2011	cnwndl1	Initial creation	Par 26476

*/

vars

begin

	app.convertScannedCargoId( self.myTerminal, textbox, false, null );	//06 Jul 2016	NDL	TFS 2275
	
	if txtCargoId.text.trimBlanks = null then
		txtCargoId.text := lblCargoId.caption.toUpper;
	endif;
	
	txtCargoId.selStart := 0;
	txtCargoId.selLength := 0;

epilog

end;</source>
        <Parameter name="textbox" type="TextBox" usage="input"/>
    </JadeMethod>
    <JadeMethod name="validatePreCargoChecks" id="21156547-16af-4cff-b854-3af76215e240">
        <source>validatePreCargoChecks( _myChildCargo : CM_TrackedItem ):Boolean;

/*

Purpose:			

Parameters:			

Return Value:		


Date		Who		Reason
06 July 2011	cnwndl1	Initial creation

*/

vars

	msg : String;

begin

	if _myChildCargo = null then
		msg := app.getErrorString(No_Cargo_To_Pack);
		self.msgBox(msg, app.getErrorString(Error), MsgBox_OK_Only,false);
		return false;
	endif;
	if myPackContainer = null then
		msg := app.getErrorString(No_Pack_Container);
		self.msgBox(msg, app.getErrorString(Error), MsgBox_OK_Only,false);
		return false;
	endif;

	if not myPackContainer.myVoyage.isNullVoyage then  //36111      
		if not _myChildCargo.myVoyage.isNullVoyage then        // 33622 
			if myPackContainer.myVoyage &lt;&gt; _myChildCargo.myVoyage then
				if myPackContainer.myVoyage &lt;&gt; null then
					msg := app.getErrorString2(Not_marked_voyage, _myChildCargo.id, myPackContainer.myVoyage.code);
				else
					msg := app.getErrorString2(Marked_voyage, _myChildCargo.id, _myChildCargo.myVoyage.code);
				endif;
				self.msgBox(msg, app.getErrorString(Error), MsgBox_OK_Only, false);
				return false;
			endif;
			
			if myPackContainer.dischargePort &lt;&gt; _myChildCargo.dischargePort then
				if myPackContainer.dischargePort &lt;&gt; null then
					msg := app.getErrorString2(Not_marked_discharge, _myChildCargo.id,  myPackContainer.dischargePort);
				else
					msg := app.getErrorString2(Marked_discharge, _myChildCargo.id, _myChildCargo.dischargePort);
				endif;
				self.msgBox(msg, app.getErrorString(Error), MsgBox_OK_Only, false);
				return false;
			endif;
		endif;
	endif;
	
	if _myChildCargo = myPackContainer then
		self.msgBox(app.getString(Cannot_pack_cargo_inside_itsel), 
				app.getString(Physically_impossible), MsgBox_Exclamation_Mark_Icon,false);
		return false;	
	endif;
	
	if _myChildCargo.myBookingRef &lt;&gt; null then							//child ti has a booking
		if _myChildCargo.myBookingRef.isStockBookingRef then				//child ti is stock booking
			if myPackContainer.myBookingRef &lt;&gt; null and not myPackContainer.myBookingRef.isStockBookingRef then
				if _myChildCargo.myBookingRefItem = null or not _myChildCargo.myBookingRefItem.allLinkedExportBookings.includes( myPackContainer.myBookingRef ) then
					self.msgBox(app.getString(CargoNotPartExBook), app.getString(Wrong_booking), MsgBox_Exclamation_Mark_Icon, false);
					return false;
				endif;
			endif;
		endif;
	endif;
	
	if myPackContainer.hasChild(_myChildCargo) then
		app.msgBox('Item ' &amp; _myChildCargo.id &amp; ' has already been packed into container ' &amp; myPackContainer.id, 'Item already packed', MsgBox_Exclamation_Mark_Icon);
		return false;
	endif;
	
	return true;

epilog

end;</source>
        <Parameter name="_myChildCargo" type="CM_TrackedItem"/>
        <ReturnType type="Boolean"/>
    </JadeMethod>
</GUIClass>
