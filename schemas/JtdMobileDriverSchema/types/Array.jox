<CollClass name="Array">
    <JadeMethod name="jtdGetRandomMember" id="6d24b146-29fa-4ca5-9f7f-1507c3bae9d6">
        <source>jtdGetRandomMember() : MemberType;

vars
	needEndLoad : Boolean;
	memberObj : MemberType;
	indx, selfSize : Integer;
	
begin
	if process.isInTransactionState or process.isInLoadState then
		needEndLoad := false;
	else
		beginLoad;
		needEndLoad := true;
	endif;

	sharedLock(self);

	selfSize := self.size;
	if selfSize &lt; 1 then
		return null;
	endif;

	indx := app.random(selfSize);
	if indx &lt; 1 then
		// belts and braces
		indx := 1;
	elseif indx &gt; selfSize then
		// belts and braces
		indx := selfSize;
	endif;

	return self[indx];	
	
epilog
	if needEndLoad then
		endLoad;
	endif;
end;</source>
        <ReturnType type="MemberType"/>
    </JadeMethod>
</CollClass>
