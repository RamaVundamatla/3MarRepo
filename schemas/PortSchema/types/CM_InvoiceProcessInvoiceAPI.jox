<Class name="CM_InvoiceProcessInvoiceAPI" id="ea829032-9859-4052-8624-1d9d2f7a52c8">
    <superclass name="CM_InvoiceProcess"/>
    <persistentAllowed/>
    <sharedTransientAllowed/>
    <subclassPersistentAllowed/>
    <subclassSharedTransientAllowed/>
    <subclassTransientAllowed/>
    <transientAllowed/>
    <DbClassMap file="backgroundprocess"/>
    <JadeMethod name="getNextValidStatuses" id="c7b732de-d390-4857-9002-601d5c6b66f7">
        <source>getNextValidStatuses(_invoice: CM_Invoice; _nextStatuses: CM_InvoiceStatusByPriority input; _includeAll: Boolean);
/*

Purpose:			

Parameters:			

Return Value:		

Date		Who		Reason
28 November 2023	navra1	Initial creation for  TFS 54090 - EUP | EUP | Interface - MTN Invoicing Oracle &lt;&lt;54090&gt;&gt;(CR - 61386/Task - 61564)

*/

vars

begin

	if _includeAll then
		self.allStatuses.copy(_nextStatuses);
		return;
	endif;

	/* // 26/01/2023	navkc1	TFS CR 55305 TASK 55537
	currentStatus := _invoice.status;
		
	//if currentStatus = null then
	//	_nextStatuses.add(self.allStatusesById[CM_Invoice.INVOICE_OPEN]);
	//endif;
	if currentStatus = CM_Invoice.INVOICE_OPEN then
		_nextStatuses.add(self.allStatuses[CM_Invoice.INVOICE_PENDING]);
		_nextStatuses.add(self.allStatuses[CM_Invoice.INVOICE_VOID]);
	elseif currentStatus = CM_Invoice.INVOICE_PENDING then
		_nextStatuses.add(self.allStatuses[CM_Invoice.INVOICE_POSTED]);
		_nextStatuses.add(self.allStatuses[CM_Invoice.INVOICE_VOID]);		
	elseif currentStatus = CM_Invoice.INVOICE_POSTED then // if invoice needs to be returned
			_nextStatuses.add(self.allStatuses[CM_Invoice.INVOICE_POSTED_REVERSE]);			
	elseif currentStatus = CM_Invoice.INVOICE_SUSPENSE then
		_nextStatuses.add(self.allStatuses[CM_Invoice.INVOICE_VOID]);
	elseif currentStatus = CM_Invoice.INVOICE_VOID then
		_nextStatuses.add(self.allStatuses[CM_Invoice.INVOICE_PENDING]);
	endif; // other statuses eg. Posted Reversed, Posted Credit can't be changed	
	*/
	
	self.getStatuses(_nextStatuses, _invoice.status, _invoice.previousStatus, _invoice.bank); // 26/01/2023	navkc1	TFS CR 55305 TASK 55537

epilog

end;</source>
        <Parameter name="_invoice" type="CM_Invoice"/>
        <Parameter name="_nextStatuses" type="CM_InvoiceStatusByPriority" usage="input"/>
        <Parameter name="_includeAll" type="Boolean"/>
    </JadeMethod>
    <JadeMethod name="getStatuses" id="c0b45ddd-1cea-409c-b3a7-9ffe13f1bff7">
        <source>getStatuses(_nextStatuses: CM_InvoiceStatusByPriority input; _status: Integer; _prevStatus: Integer; _bank: CM_Bank);
/*

Purpose:			

Parameters:			

Return Value:		

Date		Who		Reason
28 Nov 2023	navra1	Initial creation for  TFS 54090 - EUP | EUP | Interface - MTN Invoicing Oracle &lt;&lt;54090&gt;&gt;(CR - 61386/Task - 61564)
19 Sep 2024	jlier1 65963 - 65949 NSW | Dynamic Integration Development
*/

vars

begin

	if _status = CM_Invoice.INVOICE_OPEN then	
		_nextStatuses.add(self.allStatuses[CM_Invoice.INVOICE_PENDING]);
		_nextStatuses.add(self.allStatuses[CM_Invoice.INVOICE_VOID]);
	elseif _status = CM_Invoice.INVOICE_PENDING then	
		_nextStatuses.add(self.allStatuses[CM_Invoice.INVOICE_POSTED]);		
		_nextStatuses.add(self.allStatuses[CM_Invoice.INVOICE_VOID]);		
	elseif _status = CM_Invoice.INVOICE_POSTED_PENDING then
		_nextStatuses.add(self.allStatuses[CM_Invoice.INVOICE_VOID]);		
		if app.myCurrentUserCan(_bank.terminal, PmInvoiceAdmin) then
			_nextStatuses.add(self.allStatuses[CM_Invoice.INVOICE_POSTED]);
			_nextStatuses.add(self.allStatuses[CM_Invoice.INVOICE_POSTED_ERROR]);
		endif;
	elseif _status = CM_Invoice.INVOICE_POSTED_ERROR then
		_nextStatuses.add(self.allStatuses[CM_Invoice.INVOICE_VOID]);				
		_nextStatuses.add(self.allStatuses[CM_Invoice.INVOICE_POSTED]);		
	elseif _status = CM_Invoice.INVOICE_POSTED then // if invoice needs to be returned		
		_nextStatuses.add(self.allStatuses[CM_Invoice.INVOICE_POSTED_REVERSE]);			
	elseif _status = CM_Invoice.INVOICE_SUSPENSE then
		_nextStatuses.add(self.allStatuses[CM_Invoice.INVOICE_VOID]);
	elseif _status = CM_Invoice.INVOICE_VOID then
		_nextStatuses.add(self.allStatuses[CM_Invoice.INVOICE_PENDING]);
	elseif _status = CM_Invoice.INVOICE_POSTED_CREDIT_PENDING then	
		_nextStatuses.add(self.allStatuses[CM_Invoice.INVOICE_POSTED_CREDIT]);
		
	elseif _status = CM_Invoice.INVOICE_POSTED_CREDIT_ERROR then	
		_nextStatuses.add(self.allStatuses[CM_Invoice.INVOICE_POSTED_CREDIT]);
	else
	
	endif; // other statuses eg. Posted Reversed, Posted Credit can't be changed	
	
epilog

end;</source>
        <Parameter name="_nextStatuses" type="CM_InvoiceStatusByPriority" usage="input"/>
        <Parameter name="_status" type="Integer"/>
        <Parameter name="_prevStatus" type="Integer"/>
        <Parameter name="_bank" type="CM_Bank"/>
    </JadeMethod>
    <JadeMethod name="isInvoiceAPIProcess" id="2da64d77-a89d-4347-b569-b0db3d9ef0ac">
        <source>isInvoiceAPIProcess() : Boolean;
/*

Purpose:			

Parameters:			

Return Value:		

Date		Who		Reason
28 November 2023	navra1	Initial creation for  TFS 54090 - EUP | EUP | Interface - MTN Invoicing Oracle &lt;&lt;54090&gt;&gt;(CR - 61386/Task - 61564)

*/

vars

begin
	return true;
epilog

end;</source>
        <ReturnType type="Boolean"/>
    </JadeMethod>
</Class>
