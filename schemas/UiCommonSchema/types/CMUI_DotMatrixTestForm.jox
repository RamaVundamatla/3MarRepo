<GUIClass name="CMUI_DotMatrixTestForm" id="040fce88-fa9e-4dbc-b243-a287e0b8a845">
    <superclass name="CMUI_DotMatrixBaseForm"/>
    <transient/>
    <subclassTransientAllowed/>
    <transientAllowed/>
    <JadeMethod name="getPageHeight" id="76e23e81-3c87-435c-bc1d-2218aeead7ee">
        <source>getPageHeight(): Integer;
/*

Purpose:			

Parameters:			

Return Value:		

Date		Who		Reason
18 July 2014	cnwndl1	Initial creation

*/

vars

begin

	return 2970;

epilog

end;</source>
        <ReturnType type="Integer"/>
    </JadeMethod>
    <JadeMethod name="makeLabelsAvailable" id="f907c4b4-3dd4-445e-a701-1d0c37bbab29">
        <source>makeLabelsAvailable() protected;
/*

Purpose:			

Parameters:			

Return Value:		

Date		Who		Reason
18 July 2014	cnwndl1	Initial creation

*/

vars

begin

	self.allLabelsArray.add( self.lblA );
	self.allLabelsArray.add( self.lblB );
	self.allLabelsArray.add( self.lblC );
	self.allLabelsArray.add( self.lblD );
	self.allLabelsArray.add( self.lblE );
	self.allLabelsArray.add( self.lblF );
	self.allLabelsArray.add( self.lblG );
	self.allLabelsArray.add( self.lblH );
	self.allLabelsArray.add( self.lblI );
	self.allLabelsArray.add( self.lblJ );
	self.allLabelsArray.add( self.lblK );
	self.allLabelsArray.add( self.lblL );
	self.allLabelsArray.add( self.lblM );
	self.allLabelsArray.add( self.lblN );
	
	self.lblA.alignment := Label.Alignment_Right_Top;
	self.lblB.alignment := Label.Alignment_Right_Top;
	self.lblC.alignment := Label.Alignment_Right_Top;
	self.lblD.alignment := Label.Alignment_Left_Top;
	self.lblE.alignment := Label.Alignment_Left_Top;
	self.lblF.alignment := Label.Alignment_Left_Top;
	self.lblG.alignment := Label.Alignment_Left_Top;
	self.lblH.alignment := Label.Alignment_Left_Top;
	self.lblI.alignment := Label.Alignment_Left_Top;
	self.lblJ.alignment := Label.Alignment_Left_Top;
	self.lblK.alignment := Label.Alignment_Left_Top;
	self.lblL.alignment := Label.Alignment_Left_Top;
	self.lblM.alignment := Label.Alignment_Left_Top;
	self.lblN.alignment := Label.Alignment_Left_Top;

epilog

end;</source>
        <access>protected</access>
    </JadeMethod>
    <JadeMethod name="populateLineUp" id="6aa39f06-0388-4769-8f92-e3751c9975e1">
        <updating/>
        <source>populateLineUp(): String updating, protected;
/*

Purpose:			

Parameters:			

Return Value:		

Date		Who		Reason
18 July 2014	cnwndl1	Initial creation

*/

vars

	repDetails : String;
	str : String;
	obj : Object;
	label : CMUI_Label;

begin
	repDetails := self.class.name &amp; "?DOT?"
					&amp; self.baseLeftOffset.String
					&amp; "?" &amp; self.baseTopOffset.String
					&amp; "?" &amp; app.printer.drawFontSize.String
					&amp; "?" &amp; app.printer.drawFontName;
	
	str := "TEST000001, " &amp; "TEST000002, " &amp; "TEST000003, " &amp; "TEST000004, " &amp; "TEST000005, " &amp; "TEST000006";
	
	foreach obj in self.allLabelsArray do	
		label := obj.CMUI_Label;
		label.caption := str;
		//label.caption := label.name;
	endforeach;
	
	return repDetails;

epilog

end;</source>
        <access>protected</access>
        <ReturnType type="String"/>
    </JadeMethod>
    <JadeMethod name="repositionLabels" id="9765a9a3-61e5-4dcc-976d-b7e2af4dde0b">
        <source>repositionLabels() protected;
/*

Purpose:			

Parameters:			

Return Value:		

Date		Who		Reason
18 July 2014	cnwndl1	Initial creation

*/

vars

	obj : Object;
	counter : Integer;
	topMMs : Real;
	leftMMs : Real;
	rightMMs : Real;
	bottomMMs : Real;
	label : CMUI_Label;
	
begin

	foreach obj in self.allLabelsArray do
		counter := counter + 1;
		
		if counter.isOdd then
			leftMMs := 20;
			rightMMs := 83 + counter;
			topMMs := bottomMMs + 2;
			bottomMMs := topMMs + 20;
		else
			leftMMs := 120;
			rightMMs := 183 + counter;
		endif;
		
		label := obj.CMUI_Label;
		self.moveLabel( label, topMMs, leftMMs, rightMMs, bottomMMs );
		
	endforeach;

epilog

end;</source>
        <access>protected</access>
    </JadeMethod>
    <JadeMethod name="setDataFieldsFromObject" id="b34bb4de-3993-46be-addc-df6656705ca6">
        <updating/>
        <source>setDataFieldsFromObject(_obj: CM_Object; _lineUp: Boolean; _showLabelArea: Boolean) updating;
/*

Purpose:			

Parameters:			

Return Value:		

Date		Who		Reason
18 July 2014	cnwndl1	Initial creation

*/

vars

begin

	if _showLabelArea then
		//app.printer.print( self.testFrame );
		self.printLabelFieldAreaBoundary();	//21 July 2014	NDL Par 34611
	endif;
		
	self.populateLineUp();
	return;
	
epilog

end;</source>
        <Parameter name="_obj" type="CM_Object"/>
        <Parameter name="_lineUp" type="Boolean"/>
        <Parameter name="_showLabelArea" type="Boolean"/>
    </JadeMethod>
</GUIClass>
