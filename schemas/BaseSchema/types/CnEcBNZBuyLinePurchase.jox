<Class name="CnEcBNZBuyLinePurchase" id="d33f5a43-e1e5-483b-998f-cc29778ef99a">
    <superclass name="CnEcBNZBuyLineMsg"/>
    <persistentAllowed/>
    <sharedTransientAllowed/>
    <subclassPersistentAllowed/>
    <subclassSharedTransientAllowed/>
    <subclassTransientAllowed/>
    <transientAllowed/>
    <text>___________________________________________________________________________

              P R O P R I E T A R Y   N O T I C E

This software, in whole and in every part, is proprietary to Jade Software
Corporation Limited at Christchurch, New Zealand and is not to be
reproduced, used or disclosed except upon prior written permission of Jade
Software Corporation Limited.

  COPYRIGHT © JADE SOFTWARE CORPORATION LIMITED 2004 - ALL RIGHTS RESERVED
____________________________________________________________________________</text>
    <DbClassMap file="basemsg"/>
    <JadeMethod name="buildMessage" id="4f7aa748-916b-4a20-8109-03445a387104">
        <source>buildMessage(pMessage: String io): String;

vars
	errorMessage	: String;
	
begin
	errorMessage := self.checkData();
	if errorMessage &lt;&gt; "" then
		return errorMessage;
	endif;
	
	pMessage := self.messType &amp; self.MESSAGE_DELIMITER;
	pMessage := pMessage &amp; self.oPSId.String &amp; self.MESSAGE_DELIMITER;
	pMessage := pMessage &amp; self.seqNum.String &amp; self.MESSAGE_DELIMITER;
	pMessage := pMessage &amp; "" &amp; self.MESSAGE_DELIMITER;
	pMessage := pMessage &amp; self.amount.String &amp; self.MESSAGE_DELIMITER;
	pMessage := pMessage &amp; self.cardNum.String &amp; self.MESSAGE_DELIMITER;
	pMessage := pMessage &amp; self.expiry.format("yyMM") &amp; self.MESSAGE_DELIMITER;
	pMessage := pMessage &amp; self.currency.String;
	
	return null;
end;</source>
        <Parameter name="pMessage" type="String" usage="io"/>
        <ReturnType type="String"/>
    </JadeMethod>
    <JadeMethod name="checkData" id="85e15885-b1d2-44dd-904f-3b733a8bb2d3">
        <source>checkData(): String;

vars
	errorMessage : String;
	
begin
	return inheritMethod();	// Remove this line when coding complete.
		
	errorMessage := errorMessage &amp; checkOPSId();
	errorMessage := errorMessage &amp; checkSeqNum();
	errorMessage := errorMessage &amp; checkAmount();
	errorMessage := errorMessage &amp; checkCardNum();
	errorMessage := errorMessage &amp; checkExpiry();
	errorMessage := errorMessage &amp; checkCurrency();
epilog
	return errorMessage;	
end;</source>
        <ReturnType type="String"/>
    </JadeMethod>
    <JadeMethod name="create" id="c97f172f-f862-44e8-adbb-0bafd75a1794">
        <updating/>
        <source>create() updating;

begin
	self.messType := "P";
	
	self.setCurrency("");
	
	self.setOPSId();
	self.setSequenceNumber();
	self.setMyMgr();
end;</source>
    </JadeMethod>
    <JadeMethod name="getTrxString" id="1e611c4e-e32b-4e48-87b0-10f2599513be">
        <source>getTrxString(): String;

begin
	return "Purchase";
end;</source>
        <ReturnType type="String"/>
    </JadeMethod>
    <JadeMethod name="setRespApprovalNumber" id="fe093bf5-ed35-4c61-8638-1382b584fb2e">
        <updating/>
        <source>setRespApprovalNumber() updating;

	// Only valid if respCode = "00" , "OK" or "97"
begin
	if self.respText.length &gt; 6 then // shouldn't ever be if we get a successful Purchase response.
		self.respApprovalNumber := self.respText[1:6];
	else
		self.respApprovalNumber := self.respText;
	endif;
end;</source>
    </JadeMethod>
    <JadeMethod name="updateMe" id="5f5d34cd-7d1b-4dcf-8878-44d21c1ca395">
        <updating/>
        <source>updateMe(	pAmount : Integer;
			pCardNum : String;
			pExpiry : Date) updating;

begin
	if self.amount &lt;&gt; pAmount then
		self.amount := pAmount;
	endif;
	
	if self.cardNum &lt;&gt; pCardNum then
		self.cardNum := pCardNum;
	endif;
	
	if self.expiry &lt;&gt; pExpiry then
		self.expiry := pExpiry;
	endif;
end;</source>
        <Parameter name="pAmount" type="Integer"/>
        <Parameter name="pCardNum" type="String"/>
        <Parameter name="pExpiry" type="Date"/>
    </JadeMethod>
</Class>
