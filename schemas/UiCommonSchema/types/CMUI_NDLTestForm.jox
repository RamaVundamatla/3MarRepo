<GUIClass name="CMUI_NDLTestForm" id="f433f1b5-3e3d-48b2-8877-ff0add4c785c">
    <superclass name="CMUI_Modal"/>
    <transient/>
    <subclassTransientAllowed/>
    <transientAllowed/>
    <ImplicitInverseRef name="cMUI_Label3" id="ab8bd788-16bb-4c62-b136-a77c65c0979b">
        <embedded/>
        <type name="CMUI_Label"/>
    </ImplicitInverseRef>
    <JadeMethod name="populateCarriers" id="932f7ec3-edfe-44ee-9867-b6a4cd87eb2e">
        <updating/>
        <source>populateCarriers() updating;
/*

Purpose:			

Parameters:			

Return Value:		

Date		Who		Reason
20 August 2015	cnwndl1	Initial creation

*/

vars

begin

	if self.myAssistant = null then
		create self.myAssistant transient;
	endif;

	//self.myAssistant.comboBoxCarrier( self.cMUI_FilterComb1.myCombo, null );
	

epilog

end;</source>
    </JadeMethod>
    <JadeMethod name="setupForm" id="22907469-f639-4199-8bfe-8ba197a9d991">
        <updating/>
        <source>setupForm() updating;
/*

Purpose:			

Parameters:			

Return Value:		

Date		Who		Reason
20 August 2015	cnwndl1	Initial creation

*/

vars

begin

//	self.cMUI_FilterComb1.setPopulateMethod( CMUI_NDLTestForm::populateCarriers.name );
	self.populateCarriers();

	inheritMethod();

epilog

end;</source>
    </JadeMethod>
    <JadeMethod name="translateForm" id="e7e0a6cf-4f5b-4391-9e34-1def945a288c">
        <updating/>
        <source>translateForm() updating;
	// This method implements user-defined locale-based text translation for forms.
	// Created on 21 August 2015.
	//3/11/2019	jlgns1	TFS 3827 XLAT_FormCaptions need to be sorted out
vars
	locale : CM_Locale;
begin
	// Determine the locale for translations.
	locale := app.myCurrentLocale;
	if locale = null then
		locale := app.myRoot.getLocale(StandardTextLocale.padLeadingWith("0", 4), false);
		// if no locale the bail out at this point
		if locale = null then
			return;
		endif;
	endif;
	// The inheritMethod call invokes text translation for controls defined on superforms.
	inheritMethod;
	
	// The following code invokes text translation for controls defined on this form.
	self.translateCaption(Xlat_0020800, locale);
	cMUI_Label3.translateCaption(Xlat_0020801, locale);
end;</source>
    </JadeMethod>
</GUIClass>
