<GUIClass name="FrmServerFileBrowser" id="d6a4a3cb-2bfb-4a09-84ef-fa13c6377f7e">
    <superclass name="Form"/>
    <transient/>
    <subclassTransientAllowed/>
    <transientAllowed/>
    <Constant name="Dummy" id="d8f3dd5e-4963-4b2f-9b2d-52f7855f3c97">
        <source>"Dummy"</source>
        <type name="String"/>
    </Constant>
    <ImplicitInverseRef name="bCancel" id="4dcf6af2-f55e-4077-a4b5-337468ce18c6">
        <embedded/>
        <type name="Button"/>
    </ImplicitInverseRef>
    <ImplicitInverseRef name="bOK" id="12a94c51-b294-492d-98d3-ac2e5032d62d">
        <embedded/>
        <type name="Button"/>
    </ImplicitInverseRef>
    <PrimAttribute name="dirSep" id="fdc6d82f-4b0a-4466-b697-5fd05921d50f">
        <length>2</length>
        <embedded/>
        <type name="String"/>
        <access>protected</access>
    </PrimAttribute>
    <ImplicitInverseRef name="dirTreeList" id="83cd4cd2-a3d2-418b-beb4-4cb18a9e4a5d">
        <embedded/>
        <type name="ListBox"/>
    </ImplicitInverseRef>
    <ImplicitInverseRef name="folder" id="de813b64-0d59-4c01-858f-30d5e617bc5d">
        <embedded/>
        <type name="FileFolder"/>
        <access>protected</access>
    </ImplicitInverseRef>
    <ImplicitInverseRef name="lblHeading" id="28f4218f-0f4b-404b-ad9d-48633f97cf81">
        <embedded/>
        <type name="Label"/>
    </ImplicitInverseRef>
    <ImplicitInverseRef name="lblPathName" id="cba58e22-4a42-44f6-bd37-f8a1868efa1e">
        <embedded/>
        <type name="Label"/>
    </ImplicitInverseRef>
    <ImplicitInverseRef name="myParentForm" id="2aac6dc0-8f63-4e9f-b728-59a5e82ec214">
        <embedded/>
        <type name="Form"/>
    </ImplicitInverseRef>
    <PrimAttribute name="selectedItem" id="539aa81d-9e64-4863-98bc-3fb36bb05fca">
        <type name="String"/>
    </PrimAttribute>
    <JadeMethod name="addDirectoryNode" id="72954bb6-661d-4891-ae34-e29c5825158c">
        <source>addDirectoryNode(index : Integer; level : Integer; name : String);
vars
	i : Integer;
begin
	if index = 0 then
		dirTreeList.addItem(name);
		i := dirTreeList.newIndex;
	else
		dirTreeList.addItemAt(name, index);
		i := index;
	endif;

	dirTreeList.itemLevel[i] := level;
	dirTreeList.itemPictureType[i] := ListBox.ItemPictureType_Closed;
end;</source>
        <Parameter name="index" type="Integer"/>
        <Parameter name="level" type="Integer"/>
        <Parameter name="name" type="String"/>
    </JadeMethod>
    <JadeMethod name="addDirectoryNodes" id="0dd8f52d-a302-4378-bda4-80ef2e693b0c">
        <source>addDirectoryNodes(startIndex : Integer; level : Integer; dirName : String);
vars
	index :		Integer;
	dirList :	String;
	pos :		Integer;
	dir :		String;
	length :	Integer;
	delims :	String;
	dName :		String;
begin
	dirList := folder._getServerDirectoryList(dirName, folder.mask);
	// directory names are delimited by null
	delims[1] := null;
	pos := 1;
	index := startIndex;
	length := dirList.length;
	while pos &gt; 0 and pos &lt; length do
		dir := dirList.scanUntil(delims, pos);
		addDirectoryNode(index, level, dir);
		index := index + 1;
		pos := pos + 1;
		if dirList[pos] = "Y" or hasFile(dirName &amp; dirSep &amp; dir) then
			// "Y" =&gt; has subdirectory
			addDummyNode(index, level+1);
			index := index + 1;
		endif;
		pos := pos + 1;
	endwhile;

	addFileNodes(index, level, dirName);
end;</source>
        <Parameter name="startIndex" type="Integer"/>
        <Parameter name="level" type="Integer"/>
        <Parameter name="dirName" type="String"/>
    </JadeMethod>
    <JadeMethod name="addDriveNode" id="6155578b-059d-4669-ad2b-ac5984eb6606">
        <source>addDriveNode(drive : String);
vars
	i : Integer;
begin

	dirTreeList.addItem(drive);
	i := dirTreeList.newIndex;

	dirTreeList.itemLevel[i] := 1;
	dirTreeList.itemPictureType[i] := ListBox.ItemPictureType_Leaf;

end;</source>
        <Parameter name="drive" type="String"/>
    </JadeMethod>
    <JadeMethod name="addDummyNode" id="d3211a30-eaf5-4ddb-b73b-7f2749bd97bb">
        <source>addDummyNode(index : Integer; level : Integer);
begin
	if index = 0 then
		dirTreeList.addItem(Dummy);
		dirTreeList.itemLevel[dirTreeList.newIndex] := level;
	else
		dirTreeList.addItemAt(Dummy, index);
		dirTreeList.itemLevel[index] := level;
	endif;
end;</source>
        <Parameter name="index" type="Integer"/>
        <Parameter name="level" type="Integer"/>
    </JadeMethod>
    <JadeMethod name="addFileNode" id="aaa4047b-89e0-48db-9437-77febaa7a20b">
        <source>addFileNode(index : Integer; level : Integer; name : String);
vars
	i : Integer;
begin
	if index = 0 then
		dirTreeList.addItem(name);
		i := dirTreeList.newIndex;
	else
		dirTreeList.addItemAt(name, index);
		i := index;
	endif;

	dirTreeList.itemLevel[i] := level;
end;</source>
        <Parameter name="index" type="Integer"/>
        <Parameter name="level" type="Integer"/>
        <Parameter name="name" type="String"/>
    </JadeMethod>
    <JadeMethod name="addFileNodes" id="a45e2d2d-4f1b-464f-9ca0-1fccba6699fb">
        <source>addFileNodes(startIndex : Integer; level : Integer; dirName : String);

vars
	index :		Integer;
	fileList :	String;
	pos :		Integer;
	fileName : 	String;
	length :	Integer;
	delims :	String;
begin
	fileList := folder.getServerFileList(dirName, folder.mask);
	// file names are delimited by Tab
	delims[1] := Tab;
	pos := 1;
	index := startIndex;
	length := fileList.length;
	while pos &gt; 0 and pos &lt; length do
		fileName := fileList.scanUntil(delims, pos);
		addFileNode(index, level, fileName);
		index := index + 1;
		if pos = 0 then
			break;
		endif;
		pos := pos + 1;
	endwhile;
end;</source>
        <Parameter name="startIndex" type="Integer"/>
        <Parameter name="level" type="Integer"/>
        <Parameter name="dirName" type="String"/>
    </JadeMethod>
    <JadeMethod name="bCancel_click" id="74b6fac0-8681-4f1b-acf6-43b93be0eaa3">
        <controlMethod name="Button::click"/>
        <methodInvocation>event</methodInvocation>
        <updating/>
        <source>bCancel_click(btn: Button input) updating;

vars

begin
	selectedItem := null;
    unloadForm;
end;</source>
        <Parameter name="btn" type="Button" usage="input"/>
    </JadeMethod>
    <JadeMethod name="bOK_click" id="4217ceaa-b253-47b1-86c3-c59b08530ee7">
        <controlMethod name="Button::click"/>
        <methodInvocation>event</methodInvocation>
        <updating/>
        <source>bOK_click(btn: Button input) updating;

vars

begin
     unloadForm;
end;</source>
        <Parameter name="btn" type="Button" usage="input"/>
    </JadeMethod>
    <JadeMethod name="collapseNode" id="d8c77002-5c3f-4d8b-ac1c-318289e1719a">
        <source>collapseNode(lb : ListBox; index : Integer);
vars
	i : Integer;
	lvl : Integer;

begin
	if not lb.itemHasSubItems(index) or not lb.itemExpanded[index] then
		return;
	endif;

	lvl := lb.itemLevel[index];
	i := index + 1;
	while i &lt;= lb.listCount and lb.itemLevel[i] &gt; lvl do
		lb.removeItem(i);
	endwhile;

	addDummyNode(i, lvl + 1);

	lb.itemExpanded[index] := false;
	if lvl &gt; 1 then
		lb.itemPictureType[index] := ListBox.ItemPictureType_Closed;
	endif;

end;</source>
        <Parameter name="lb" type="ListBox"/>
        <Parameter name="index" type="Integer"/>
    </JadeMethod>
    <JadeMethod name="dirTreeList_click" id="39513665-1825-4ece-8736-d440a1ce5d92">
        <controlMethod name="ListBox::click"/>
        <methodInvocation>event</methodInvocation>
        <updating/>
        <source>dirTreeList_click(listbox: ListBox input) updating;

vars

begin
   selectedItem := getPathFromNode(listbox.listIndex);
   lblPathName.caption := selectedItem;
end;</source>
        <Parameter name="listbox" type="ListBox" usage="input"/>
    </JadeMethod>
    <JadeMethod name="dirTreeList_dblClick" id="52b18949-d853-4171-992a-682a0fdff68e">
        <controlMethod name="ListBox::dblClick"/>
        <methodInvocation>event</methodInvocation>
        <updating/>
        <source>dirTreeList_dblClick(listbox: ListBox input) updating;

vars

begin
	if listbox.itemExpanded[listbox.listIndex] then
		collapseNode(listbox, listbox.listIndex);
	else
		expandNode(listbox, listbox.listIndex);
	endif;

	dirTreeList_click(listbox);
end;</source>
        <Parameter name="listbox" type="ListBox" usage="input"/>
    </JadeMethod>
    <JadeMethod name="dirTreeList_keyDown" id="9251ad9d-c971-4151-a5b1-3cf14bdd66d9">
        <controlMethod name="ListBox::keyDown"/>
        <methodInvocation>event</methodInvocation>
        <updating/>
        <source>dirTreeList_keyDown(listbox:ListBox input; keyCode:Integer io; shift:Integer) updating;

begin
//	write keyCode;
	if keyCode=38 and shift=2 then  // Ctrl+Up
		listbox.topIndex:=listbox.topIndex-2;
		keyCode:=0;

	elseif keyCode=40 and shift=2 then  // Ctrl+Down
		listbox.topIndex:=listbox.topIndex+2;
		keyCode:=0;
		
	elseif keyCode=39 then  // right arrow
		if listbox.itemExpanded[listbox.listIndex] = false then
			expandNode(listbox, listbox.listIndex);
		else
			listbox.listIndex:=listbox.listIndex+1;
		endif;

	elseif keyCode=37 then  // left arrow
		if listbox.itemExpanded[listbox.listIndex] = true then
			collapseNode(listbox, listbox.listIndex);
		else
			while listbox.itemLevel[listbox.listIndex]&gt;1 do
				listbox.listIndex:=listbox.listIndex-1;
				if listbox.itemExpanded[listbox.listIndex] = true then
					break;
				endif;
			endwhile;
			listbox.itemSelected[listbox.listIndex]:=true;
		endif;
	endif;
end;</source>
        <Parameter name="listbox" type="ListBox" usage="input"/>
        <Parameter name="keyCode" type="Integer" usage="io"/>
        <Parameter name="shift" type="Integer"/>
    </JadeMethod>
    <JadeMethod name="dirTreeList_pictureClick" id="c3092271-1e47-429a-9f0c-6dd2fa5df23d">
        <controlMethod name="ListBox::pictureClick"/>
        <methodInvocation>event</methodInvocation>
        <updating/>
        <source>dirTreeList_pictureClick(listbox: ListBox input; picIndex: Integer; whatClicked: Integer) updating;

vars

begin
	if whatClicked = listbox.PictureClick_PlusMinus then
		if listbox.itemExpanded[picIndex] then
			collapseNode(listbox, picIndex);
		else
			expandNode(listbox, picIndex);
		endif;
	endif;

	listbox.listIndex := picIndex;
	dirTreeList_click(dirTreeList);
end;</source>
        <Parameter name="listbox" type="ListBox" usage="input"/>
        <Parameter name="picIndex" type="Integer"/>
        <Parameter name="whatClicked" type="Integer"/>
    </JadeMethod>
    <JadeMethod name="expandNode" id="b5a22093-3576-4bbc-a0eb-768806a8cc97">
        <source>expandNode(lb : ListBox input; index : Integer);
vars
    pathName : String;
    level    : Integer;
begin
    pathName := getPathFromNode(index);
	// Only expand an item if it has subitems and it isn't already expanded
    if lb.itemHasSubItems(index)  and not lb.itemExpanded[index] then
		if lb.itemText[index + 1] = Dummy then
			// Remove the placeholder entry
			lb.removeItem(index + 1);
            level := lb.itemLevel[index] + 1;
            addDirectoryNodes(index + 1, level, pathName);

			lb.itemExpanded[index] := true;
			lb.topIndex:=index-2;
			if level &gt; 2 then
				lb.itemPictureType[index] := ListBox.ItemPictureType_Open;
			endif;
		endif;
	endif;
end;</source>
        <Parameter name="lb" type="ListBox" usage="input"/>
        <Parameter name="index" type="Integer"/>
    </JadeMethod>
    <JadeMethod name="getNodeFromPath" id="7339f2dd-41b8-491b-be79-8f95ccc8f5ea">
        <updating/>
        <source>getNodeFromPath(lb:ListBox input; s:String; level:Integer) updating;

vars
	i,j,k:Integer;
	t:String;
begin
	i:=1;
	t:=s.scanUntil("\:/",i).toUpper;
	if i = 0 and t = null then return; endif;
	if level = 1 then
		t:=t&amp;":\";
	endif;
	
	j:=lb.listIndex;
	k:=lb.listCount;
	while j &lt;= k do
		if lb.itemLevel[j] = level and lb.itemText[j].toUpper &gt;= t then
			lb.listIndex:=j;
			lb.itemSelected[j]:=true;
			if lb.itemText[j].toUpper &gt; t and not lb.itemHasSubItems(j) then
				break;
			endif;
			if i &lt;&gt; 0 then
				t:=s.scanWhile("\:/",i);
			endif;
			if i &lt;&gt; 0 and lb.itemHasSubItems(j) then
				expandNode(lb, j);
				getNodeFromPath(lb, s[i:end], level+1);
			endif;
			break;
		endif;
		j:=j+1;
	endwhile;
end;</source>
        <Parameter name="lb" type="ListBox" usage="input"/>
        <Parameter name="s" type="String"/>
        <Parameter name="level" type="Integer"/>
    </JadeMethod>
    <JadeMethod name="getPathFromNode" id="f913645d-789d-4a91-b4df-597159f2db05">
        <source>getPathFromNode(index : Integer) : String;
vars
	pathName :	String;
	s :			String;
	level :		Integer;
	i :			Integer;
begin
	level := dirTreeList.itemLevel[index] + 1;
	foreach i in index to 1 step -1 do
		if dirTreeList.itemLevel[i] &lt; level then
			s := dirTreeList.itemText[i];
			if s[s.length] &lt;&gt; dirSep and pathName &lt;&gt; null then
				s := s &amp; dirSep;
			endif;
		    pathName := s &amp; pathName;

			level := dirTreeList.itemLevel[i];
		endif;
		if level &lt; 2 then
			break;
		endif;
	endforeach;

	return pathName;
end;</source>
        <Parameter name="index" type="Integer"/>
        <ReturnType type="String"/>
    </JadeMethod>
    <JadeMethod name="hasFile" id="09992f8c-a02c-480e-92bc-2544b9a70271">
        <executionLocation>server</executionLocation>
        <source>hasFile(dirname : String) : Boolean serverExecution;
vars
	ff : FileFolder;
	fn  : FileNode;
begin
	create ff;
	ff.fileName := dirname;
	ff.mask := "*.*";

	foreach fn in ff.files do
		if fn.isKindOf(File) then
			return true;
		endif;
	endforeach;

	return false;

epilog
	delete ff;
end;</source>
        <Parameter name="dirname" type="String"/>
        <ReturnType type="Boolean"/>
    </JadeMethod>
    <JadeMethod name="hasSubdirectory" id="f150df0a-b730-40a8-aa6b-b1d71d6ecf64">
        <source>hasSubdirectory(dirname : String) : Boolean;
vars
	ff : FileFolder;
	fn  : FileNode;
begin
	create ff;
	ff.fileName := dirname;
	ff.mask := "*.*";

	foreach fn in ff.files do
		if fn.isKindOf(FileFolder) then
			return true;
		endif;
	endforeach;

	return false;

epilog
	delete ff;
end;</source>
        <Parameter name="dirname" type="String"/>
        <ReturnType type="Boolean"/>
    </JadeMethod>
    <JadeMethod name="init" id="b9330827-1216-4a03-b375-627253e94902">
        <updating/>
        <source>init(title : String; heading : String; startFolder : String) updating;

vars

begin
	caption := title;
	lblHeading.caption := heading;
	lblPathName.caption := selectedItem;
end;</source>
        <Parameter name="title" type="String"/>
        <Parameter name="heading" type="String"/>
        <Parameter name="startFolder" type="String"/>
    </JadeMethod>
    <JadeMethod name="initDirectoryTree" id="d6c3f751-afcf-44e7-a56a-36558e50bd9c">
        <updating/>
        <source>initDirectoryTree() updating;

vars

begin
	dirTreeList.hasTreeLines := true;
	dirTreeList.hasPictures := true;
	dirTreeList.hasPlusMinus := true;

	if node._isUnixServerPlatform then
		initDirectoryTreeUnix();
	else
	    initDirectoryTreeWindows();
	endif;

    dirTreeList.listIndex := 1;
	if selectedItem &lt;&gt; null then
		getNodeFromPath(dirTreeList, selectedItem, 1);
    endif;
    dirTreeList_click(dirTreeList);
end;</source>
    </JadeMethod>
    <JadeMethod name="initDirectoryTreeUnix" id="d1fd6cbd-6b39-4391-b4fe-b688bcff6225">
        <source>initDirectoryTreeUnix();
begin

	// add root node
	addDirectoryNode(1, 1, "/");
	// add first level of directories from root
	addDirectoryNodes(2, 2, "/");

	// expand root
	dirTreeList.itemExpanded[1] := true;

end;</source>
    </JadeMethod>
    <JadeMethod name="initDirectoryTreeWindows" id="689e5e30-091c-4346-b5e9-78f999873789">
        <updating/>
        <source>initDirectoryTreeWindows() updating;

vars
	driveList : Integer;
	i 		  : Integer;
	drivestr  : String;
begin
	// obtain bit mask of fixed disk and network drives on the server
	//driveList := app._getServerLogicalDriveMask();	
	drivestr := "?:\";

	// add a node for each drive
	foreach i in 0 to 26 do
		//if driveList.bitAnd(2 ^ i) &lt;&gt; 0 then
			drivestr[1] := ('A'.Integer + i).Character;
			addDriveNode(drivestr);
			addDummyNode(0, 2);
		//endif;
	endforeach;

end;</source>
    </JadeMethod>
    <JadeMethod name="load" id="d46ba568-3fc2-41d6-b526-ca29ea7c46f7">
        <controlMethod name="Form::load"/>
        <methodInvocation>event</methodInvocation>
        <updating/>
        <source>load() updating;

vars
    f 		: FileNode;
    cls 	: Class;
begin
	centreWindow;
	if node._isUnixServerPlatform then
		dirSep := "/";
	else
	    dirSep := "\";
	endif;
	
	create folder;
	initDirectoryTree;
	dirTreeList.setFocus;
end;</source>
    </JadeMethod>
    <JadeMethod name="unload" id="48207b21-6821-47d1-b705-0b8fec9fa796">
        <controlMethod name="Form::unload"/>
        <methodInvocation>event</methodInvocation>
        <updating/>
        <source>unload() updating;

vars

begin
	delete folder;
end;</source>
    </JadeMethod>
</GUIClass>
