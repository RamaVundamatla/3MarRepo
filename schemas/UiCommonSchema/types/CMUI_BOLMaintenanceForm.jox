<GUIClass name="CMUI_BOLMaintenanceForm" id="a43e0595-2a31-4d7c-8b46-a626b270fd46">
    <superclass name="CMUI_DocketForm"/>
    <transient/>
    <subclassTransientAllowed/>
    <transientAllowed/>
    <JadeMethod name="create" id="fbc7fa81-87f9-4d63-b798-baaefec915ab">
        <updating/>
        <source>create() updating;
//18 March 2010	cnwml2


vars

begin

// 30787
	self.permissionView := PmAdmBOLView;
	self.permissionMaint := PmAdmBOLMaint;
epilog

end;</source>
    </JadeMethod>
    <JadeMethod name="translateForm" id="d9939397-290e-4d5d-b762-5acd88a23c73">
        <updating/>
        <source>translateForm() updating;
	// This method implements user-defined locale-based text translation for forms.
	// Created on 13 April 2013.
	//3/11/2019	jlgns1	TFS 3827 XLAT_FormCaptions need to be sorted out
vars
	locale : CM_Locale;
begin
	// Determine the locale for translations.
	locale := app.myCurrentLocale;
	if locale = null then
		locale := app.myRoot.getLocale(StandardTextLocale.padLeadingWith("0", 4), false);
		// if no locale the bail out at this point
		if locale = null then
			return;
		endif;
	endif;
	// The inheritMethod call invokes text translation for controls defined on superforms.
	inheritMethod;
	
	// The following code invokes text translation for controls defined on this form.
	self.translateCaption(Xlat_0018619, locale);
end;</source>
    </JadeMethod>
</GUIClass>
